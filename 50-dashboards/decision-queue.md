---
type: dashboard
status: active
created: 2026-07-05
tags: [dashboard, decisions, knowledge-loop-os]
---

# Decision Queue

The single place every open decision lives. One row per open ask — not one note per ask scattered across `06-reviews/`. Loops **append rows here** when they defer or propose; Jesse clears rows in a batch. This is the metered resource: the queue, not agent effort, is what the system is built to keep small.

## Operating rules

- **One row per open decision.** `id` is `DQ-NNN` (monotonic, never reused). The `source` column links the note that holds the full context; this table holds only the one-line ask.
- **Risk tier** — `low` (reversible content), `med` (structure/schema/convention), `high` (pricing, SOP, safety, field-execution, customer-facing, or heater-card facts). Tier sets how carefully the row is cleared, not whether it queues.
- **Cap = 10 open rows.** When more than 10 rows are `open`, proposal-generating loops (idea research, non-urgent review flags) **pause and say so** rather than adding to a jammed queue. Urgent operational asks still queue.
- **Aging = 60 days.** A row untouched for 60 days flips to `status: expired` — never deleted, the source note stays, but it stops counting against the cap and stops pretending it will be decided.
- **Closing a row is human-gated.** Jesse checks the box (or an agent does, only after Jesse approves). Applying whatever the row asked for follows the existing ceremony for that content type.
- **One Closed table, one format** (2026-08-15). Every closed row goes to `## Closed` in the `id | opened | closed | source | ask | outcome` shape, whatever the reason it closed — decided, rejected, or aged out to `expired`. An expired row records that in its `outcome`; it does not get its own table. This replaces a two-table split that had persisted since launch and made every closure an uncued judgment call about which table it belonged in.

## Open

| id | opened | source | ask | risk | age (d) | status |
|---|---|---|---|---|---|---|
| DQ-016 | 2026-08-15 | [[idea-sharepoint-projection-drift-check]] | Build the SharePoint projection drift check as a `health.md` row. Design is settled by the 2026-08-11 evidence: it must REST-read live library content, because `sharepoint_export.py --check` was green throughout while the library itself held the wrong file. Presence-only and columns-complete checks both pass the real case. | med | 0 | open |
| DQ-017 | 2026-08-15 | [[2026-07-28-idea-research-rollup-per-rig-coilset-grain]] | Open the bundled heater-card schema decision in its own session rather than waiting for a schema change to carry it: the per-coilset re-grain, the Pig Specifications `Condition` column, and the vault-stats layer's two tracks. **Ordering constraint from DQ-012, load-bearing:** the Syncrude ~6 ft/hr figure must be *reproduced* from per-coilset footage before the fill/flush question is asked, and it may not enter any benchmark until both are answered. — **Constraint DISCHARGED 2026-08-20, see [[2026-08-20-syncrude-geometry-per-pass-misread]].** The figure reproduced (`2,311 ÷ 8 ÷ 48 = 6.0`) and both its inputs were wrong: the 2,311 ft was one pass misread as the whole heater (true heater total 17,893 ft), and the 48 hrs is the outlier coil set. Figure **struck**, fill/flush question **moot**, and the per-coil 3-vs-4 radiant split this row was waiting on **does not exist** — it was an artifact of the same division error. The remaining schema bundle (per-coilset re-grain, Pig Specs `Condition` column, vault-stats two tracks) is untouched and still open. **Folded in 2026-08-21:** the Rig-In column's mixed-method problem ([[2026-08-16-idea-research-rollup-rig-in-column-is-mixed-method]]) — outlier rows need classifying and the footnote convention needs to become a rollup-visible qualifier. It is a subset of this same schema decision, not a separate one; queuing it separately is how the queue stopped being the single place every open decision lives. **Two more folded in 2026-08-21 on the DQ-026/DQ-027 close:** (a) from DQ-026, whether `Condition` splits into separate **callout-type** and **coil-condition** columns — the ruling there kept one column deliberately because splitting moves the lint-locked `DURATIONS-HEADER` on ~40 cards and most historical rows would land `unknown`, so the split rides with this re-grain or not at all; (b) from DQ-027, the **outlier-recording convention** — the rule now tells an estimator to check per-coilset spread, but there is nowhere on a card to *mark* an outlier set until this row's per-coilset grain exists. Neither blocks anything today; both are consequences this row now owns. **A third arrived 2026-08-21 on the DQ-020 close:** [[idea-rig-out-vs-rig-in-mirror-rule]] is **gated on this row** — the rollup already carries ~14 rows with both Rig-In and Rig-Out and they do not support the mirror rule uniformly, but no numeric correction may be derived from that column until this row settles its method. Reading those rows back to their source receipts is allowed now and is what discharges the gate. — **PHASE 1 RULED 2026-08-21 (Jesse); row stays open.** The bundle finally had a forcing function: **CAD26001 mobs 08-25** on `7-1-F-1`, the one card carrying per-coilset history, with Jesse on nights as operator — so the *capture-spec* half was ruled before mob and the migration/tooling half was left to be ruled after the job, when there are two cards of structured data instead of one. **Ruled:** a new **optional `## Coilset Durations` section** — additive, so `## Task Durations` and its lint-locked `DURATIONS-HEADER` are untouched, no existing card changes, and `estimating_rollup.py` is unaffected until deliberately extended. Columns `Job # · Coils · Rig · Mode · Circuit ft · Rig-In · Pig · Smart Pig · Rig-Over · Rig-Out · Total · Coil condition · Flag`, coil-condition vocabulary `light/moderate/heavy/unknown` (Jesse). **This dissolves Q2 rather than deciding it** — DQ-026 kept `Condition` unsplit because splitting moves a lint-locked header across 41 cards and most historical rows would land `unknown`; a *new* section carrying its own coil-condition column costs neither, so `Condition` stays job class, unrenamed. **Q4 (DQ-027's deferred outlier-recording half) lands as the `Flag` column** — `clean/outlier/blended/unknown`, with CAD25004's 48-hr set as the worked case. **The spec was dry-run before it was ruled, and the dry-run earned its keep:** filling CAD25004's three sets reconciles exactly to the parent row (14/119/18/13, Total 164) and reproduces the card's 47/64/124 ft/hr rates from `Circuit ft ÷ Pig` with no ÷Mode approximation; CAD24002's two sets reconcile exactly too. It also found three cells with no home *before* the spec went to the field — Stand-By is per-rig not per-set (CAD25004's 192 split 84/108 with TM6's 108 unallocated across two sets) so the column was dropped; Rig-Over was missing and had to be added or CAD24002 would not sum; and `Circuit ft` had to be pinned to **footage one pig travels**, not the set total, which is the same per-pass-vs-heater-total error that produced the struck ~6 ft/hr figure. **Applied:** `04-knowledge/_canonical-heater-card.md`, `templates/_heater-template.md`, `usadebusk-vault-ingest` SKILL.md (card shape + a capture-at-ingest step, since retrofitting a parser onto committed prose is the documented failure mode this bundle exists to avoid), and a field capture sheet at `00-inbox/cad26001-coilset-capture-sheet.md`. **Caught in the same pass — DQ-026 drift the 2026-08-21 close missed:** `usadebusk-vault-ingest` still described `Condition` as "coil state" and asserted "a crashed furnace runs significantly dirtier than routine service fouling," the exact claim DQ-026 struck. Corrected to job-class framing. **Verified:** lint 0 errors, and `estimating-actuals-rollup.md` regenerates byte-identical — the new section adds a surface without moving a figure. **Still open on this row:** Q1's migration and tooling (Phase 2 — populate 7-1-F-1, fix `table_rows()`'s never-reset `seen_header` so a second table in a parsed section can't be silently ingested, add a `COILSET-HEADER` lock + fixture), Q3 (Rig-In method), Q5 Track 2, and the mirror-rule gate. **Q3's planned evidence step is cancelled and replaced, 2026-08-21.** The plan had Jesse pulling the USA25051 H19/H20 report to classify the last two unmethodded Rig-In rows; he ruled instead that *"that 25051 project was chaotic — not a great project to get reliable data from,"* and that H-20 ran a different configuration. Both cards corroborate hard: H-20's USA25051 hours have **two sources that do not reconcile under any concurrency assumption**, flagged on the card since 2026-07-22 and read past by me when I proposed the pull; H-20 is a **vertical 4-coil hydro heater** against H-19's **horizontal 2-coil charge heater**, never two samples of one configuration; H-20's radiant looping is a per-job election that changed between its own two jobs; and H-19's USA26038 rig-out of 2 h is truncated because the rigging moved to H-20 rather than demobbing. All four HF Sinclair rows are now quarantined for rule-derivation on their own row notes — they stay in the table as billing actuals. **Consequence for the gated mirror-rule idea:** a recount of the rollup (my derivation, not a stated figure) finds only **5 clean pairs**, not the ~14 the seed claimed — HF-0012 11→6, HP-0002 2→6, HP-0025 6→6, F-501 7→14, F-802 4→20 — and three of those five are one CHS turnaround. The seed's "facility-shaped split" framing does not survive that; what remains is a difference between three jobs, equally consistent with job shape, rig count or condition. Seed updated rather than closed. — **PHASE 3 RULED AND APPLIED 2026-08-21 (Jesse); Q3 and the mirror-rule gate both close.** Ruled **mark, don't suppress**: `estimating_rollup.py` gains a `Rig method` column classifying every row, with the figures left visible. **Four of six values are derived from markers the cards already carried** — `combined` from `combined-heaters`, `2-rig-sum` from `hours-blended` + `Rigs > 1`, `blended` from single-rig `hours-blended`, `see card` from a `*` on any rig cell. Only the HF Sinclair quarantine needed a new token, and it reuses the existing comma-append mechanism: `, rig-quarantined`, documented in the exemplar beside `hours-blended` and `combined-heaters`. **Building it corrected the plan twice, and both corrections matter.** (a) The plan classified `*` as "bundled rigging figure" — wrong. `*` means *the card footnotes this cell*, and the reasons genuinely differ: Valero bundles rig-in + rig-over + rig-out into one "Rigging" number, while the Flint Hills pair (01-BA-105 / 02-BA-201) is one continuous rig-over where each heater's missing side is **absent by design and correct per schema**. Collapsing those into one label would have invented a defect where the convention was working. Resolved to `see card` rather than a guess. (b) The plan had the column default to `clean`; **HF-0011 killed that** — its Rig-Out reads `3`, the *billed* figure, with a further **8 hrs customer-signed and never billed**, and no token on that row says so. The default is now `unmarked`, meaning *no method problem is recorded*, explicitly not *verified*. A `clean` default would have swept that row into a rule. **The derivable population is 5 pairs across 3 jobs, computed by the script rather than asserted** — 6 rows classify `unmarked` but HP-0007 records no Rig-In, so `unmarked` and `derivable` are not the same set. **Mirror-rule gate discharged with a no-change finding:** `usadebusk-estimating` already holds the right position — SKILL.md:68 names F-802 as the counter-example and says one row is not enough to move the rule while rig-out stays the exposure to state, and five pairs from three jobs reaches the same verdict with a larger n. The seed's facility-shaped-split hypothesis is withdrawn: facility, job shape, rig count and condition are perfectly confounded across three jobs. Seed resolved, `revisit-trigger:` retired, successor condition (~10 complete `unmarked` pairs) stated but deliberately not made a trigger. **No skill edit, so no `f6-duration-mobdemob` replay owed.** **Verified:** job-class means byte-identical (crash 6 rows 13–67 mean 33 · first 1 row 75 · routine 5 rows 47–259 mean 99), proving the comma-append reading was right; all 25 rows classify; lint 0 errors. **DQ-017 now has only Phase 2 left** — migrate `7-1-F-1`, fix `table_rows()`, add `COILSET-HEADER` + fixture — which waits on CAD26001 returning. **Q5 Track 2 also largely answered, 2026-08-21.** Bases was tried and it satisfies — `50-dashboards/heater-fleet.base`, four views, zero build cost, no Dataview. The try surfaced that the frontmatter it queries was not clean: `heater-type` held 11 values against a spec of 4 (itself missing `coker`), and `verified` — absent from the exemplar while lint checked its presence — held prose on ten cards. Both migrated and lint-locked the same day (`9718b8f`), and the ordering is the lesson: built first, the base would have needed `if()` chains to collapse `vacuum heater` into `vacuum` and parse a date out of a forty-word sentence, all to be deleted after. **Still open from Track 2:** the frontmatter-promotion half (multi-ID/single-ID, smart-pig and filtration elections), correctly gated behind "only if Bases is not enough" — not reached, because it was enough. **So DQ-017 is now down to Q1's Phase 2 alone:** populate `7-1-F-1`'s `## Coilset Durations` from CAD26001, hand-migrate the two prior Syncrude jobs, fix `table_rows()`'s never-reset `seen_header`, and add a `COILSET-HEADER` lock with a fixture. Nothing else on this row is outstanding, and Phase 2 cannot start until the job returns. **Q5 Track 1 (five cross-field lint rules) is split out of this row entirely** — no dependency on the durations schema. — **STATUS UPDATE 2026-09-03 (not a close).** Phase 2's stated precondition was "two cards of structured data instead of one," and CAD26001 has now delivered the **first populated `## Coilset Durations`** — two rows on [[7-1-F-1]], TM5 and TM6, producing the ft/hr answer the section was built for: **124 / 144** against CAD25004's single-circuit 124. That is one card and one job, so the precondition is arguably still unmet, which is why the CAD26001 close-out scoped Phase 2 out. **The row stays open and its remaining scope is unchanged** — populate the two prior Syncrude jobs by hand, fix `table_rows()`'s never-reset `seen_header` so a second table in a parsed section cannot be silently ingested, and add a `COILSET-HEADER` lock with a fixture. Recorded because the row still read as though nothing had landed; see [[2026-09-03-cad26001-report-session-open-items]] item 3. | high | 0 | open |
| DQ-019 | 2026-08-16 | [[2026-08-16-prestaged-pointer-dead-severity-tiering]] | POINTER-DEAD sits at `warning` tier and folds into a single undifferentiated "Lint warnings: 51 (backlog) ok" count on `health.md` — two of three 2026-08-01 findings sat dead 9 days unnoticed, caught only by reading raw lint output. Promote to lint `error`, give it its own dashboard row, add a procedural reminder at the folder-reorg event that causes it, or leave as-is (3 findings in ~4 weeks, all same-day-fixed once surfaced)? | med | 0 | open |

| DQ-024 | 2026-08-19 | [[2026-08-19-prestaged-treat-gas-loss-nominal-basis]] | [[F-501]]'s Field Notes characterise Treat Gas as "a uniform 9–14.5%" loss showing "general thinning" — percentages computed against 0.375 in, a figure Steady Flux `26-0663-002 Rev. A` labels **minimum wall** in Table 4 and **nominal wall** in its results header. If nominal schedule wall, the A53/A106 −12.5% as-new floor is 0.328 in and the worst reading (0.321 in at A_7_TG) is 2.1% under it rather than 14.4%; if genuinely minimum wall, the characterisation stands and the 9Cr sections are understated instead. The card records the section only as "Carbon steel" with no grade, so the tolerance rule is not established for this pipe. Qualify the card now, ask Steady Flux/ExxonMobil for the basis first, or leave as written — and separately, settle a reading convention for loss baselines that the vault does not currently have anywhere. | high | 0 | open |

| DQ-025 | 2026-08-20 | [[2026-08-20-prestaged-ut-data-loss-air-and-fouling]] | Jesse's field knowledge — air and fouling are the two standing causes of UT data loss, many coils have no high-point bleeders, and the USADebusk bleeding method does not always work — sits in `00-inbox/` with no durable home. The vault states none of it anywhere (zero grep hits across `04-knowledge/`, `06-reviews/`, `01-context/`), and `manual/14` §14.3 carries only the fouling half, framed as a pre-run prerequisite rather than a limit on reading a returned report. Two consequences are unstated: a noisy C-scan is **not** evidence of poor cleaning even though a clean one is evidence of good cleaning (trapped air gives the same signature and is a venting limitation, not a cleaning failure), and residual fouling can produce a reading that looks valid and reads thin. Where does the general fact land — a `04-knowledge/` note, a `manual/14` amendment, [[F-501]] only, or nowhere yet — and does F-501's B_8_C entry (0.224", 43.9% loss, presented uncaveated) get a validity caveat while the question sits open with Steady Flux? | high | 0 | open |

| DQ-029 | 2026-08-21 | [[2026-08-18-idea-research-researched-status-outlives-build]] | The Terminal-Note Sweep allowlist and `vault_lint.py`'s status vocabulary have drifted **in both directions**, and DQ-018 deliberately fixed only one side of it. `executed` and `spec-complete` sit on the sweep allowlist while being outside `ALLOWED_STATUS` entirely — recorded at [[2026-08-18-idea-research-researched-status-outlives-build]], latent rather than active only because they appear solely in `archive/`, which `SKIP_SCAN` excludes. Meanwhile seven `TERMINAL_STATUS` values are absent from the allowlist, and **the obvious fix is wrong**: deriving the allowlist from `TERMINAL_STATUS` would sweep `awarded` and `lost`, which are quote outcomes rather than closed commitments, and `decided-blocked`/`approved-blocked`, which mean decided-but-blocked and should stay visible. So this is a vocabulary question — which terminal statuses mean *finished and filed* versus *finished and still load-bearing* — not a list-sync chore. Reconcile the two, or state the rule that keeps them deliberately different? | med | 0 | open |


**6 open rows** as of 2026-09-03 — four under the cap of 10. DQ-030 was opened and closed the same day (2026-09-03): the ruling session ran straight through into applying it. Three closed that day: DQ-026 and DQ-027 together (both from the one 2026-08-20 Syncrude session, both one-question rulings), then DQ-020. (The footer here previously read "10 open rows … at cap" while the generated `health.md` counted 9, and it sat *inside* the Open table above the DQ-029 row, breaking it. Corrected and moved below the table 2026-08-21.)

## Closed

| id | opened | closed | source | ask | outcome |
|---|---|---|---|---|---|
| DQ-030 | 2026-09-03 | 2026-09-03 | [[2026-09-03-fouling-terminology-vocabulary]] | Rule on the fouling terminology standard term by term, gated on discussing the terms first — *"I don't want to use terms that my techs don't understand."* | **Ruled and applied same session.** Two rulings overturned the proposal. **(a) Composition is not provable and that generalizes** — Jesse: *"The pigging tech's have no way of proving / analyzing certain assumptions. We have theories, but most can't be proven."* Reframed as **our evidence is hydraulic and mechanical, not material**: a pig run establishes what the coil and the tools did, never what the deposit is. Composition joined morphology, formation mechanism and metallurgical condition in one not-assertable tier — the packet had wrongly carried `scale`/`inorganic deposit` as observable. **(b) The three-register split was withdrawn.** Jesse: one vocabulary, the customer knows the terms, techs can learn them, and the report problems were *"more about verbosity and related concerns."* Correct — four of his five CAD26001 corrections are about how much was said, not which words; the fifth is an invented coinage. Replaced by one vault-wide vocabulary, the evidence tiers (which constrain truth, not audience), an **expectation-vs-finding split** that keeps `hard coke`/`pitch` load-bearing in estimating while barring them from reports as findings, and a separate report scope rule written against the five corrections. Shift logs keep the preserve-crew-words carve-out. **Default nouns are `fouling`/`deposit`**; `coke` only where composition was established. **`soft coke` retired** and armed as a `DEAD-STRING` lint error at zero backlog (HEATER-TYPE-VOCAB precedent) — it caught a stale SharePoint projection on first run. **The session's most valuable output was Jesse's, not the research:** large pieces fractured off the wall and recovered in the launcher/receiver can be inspected, and layering is visible in them — moving layered fouling from cited theory (Jegla/Atkins; Quest's case study) into our own evidence. Nothing asked for it, so it was being seen and discarded; `/log` and `/report` now capture it. Quest vocabulary adopted per option B — take their nouns, answer their verification argument rather than avoid it. API RP 573 purchase declined. Applied across 17-glossary, industry-foundation, manual 02/03/09/10, the heater-card exemplar and template, five skills, and vault_lint. Verified: self-test 20/20, lint 0 errors, renderer diff empty. **Left open deliberately:** the summary-structure half of [[idea-job-report-summary-quality]], which needs its back-test. |
| DQ-020 | 2026-08-17 | 2026-08-21 | [[2026-08-17-prestaged-usa26041-estimating-feedback]] | Should any of USA26041's three model-level findings (pig-unit real-cost mix, rig-out duration correction for the six-launcher/triple-mode profile, per-diem calendar-day basis) modify `usadebusk-estimating` now, or does n=1 (Condition `first`) mean all three sit as revisit-triggered observations until a second data point? | **Ruled: all three options rejected as framed; three-way split** (Jesse, 2026-08-21). A, B and C all assume the question is "how many data points do we have," and on two of the three findings it is not. **Per diem — no change.** Option B rested on the "24 person-days entitled vs 15 billed = $1,350 unbilled" figure, which is **the phantom gap Jesse overturned on 2026-08-15** (reported at $1,800 then $1,350, wrong both times; correction filed in `04-knowledge/concepts/business-normal-facts.md` — mob/demob per diem bills inside the lump sum). The review note cited the superseded `change-log.md` entry, not the correction two rows below it. Stripped of the phantom the finding is 12 quoted vs 15 billed on the T&M portion — three person-days from a longer calendar span, a duration miss with per diem downstream. `1 PD per 12-hr shift` stands. **Pig unit — no skill change, data preserved.** The blended $103.50 sits above every one of the six real costs (max $81.40), but $103.50 was only ever a DSP26071 quote line and never a skill default, so there is no value to correct; the upstream question (does a size-mix pig cost belong in the skill at all) stays open. Six receipt costs filed as actuals on [[F-501]]'s Pig Specifications table before the inbox note aged out. **Rig-out — re-classified, NOT n=1.** Both the source and the review note held it at n=1 without cross-tabulating the rollup, which already carries ~14 rows with both Rig-In and Rig-Out; they break the mirror rule hard at ExxonMobil (F-501 7→14, F-802 4→20) and HF Sinclair (H-19 10→30, H-20 22→38) while four CHS rows run the other way. Filed as [[idea-rig-out-vs-rig-in-mirror-rule]], **gated on DQ-017** because the Rig-In column is the known mixed-method column. **Net: `usadebusk-estimating` unchanged by this row** — one Lane 1 card edit, one gated seed. |
| DQ-026 | 2026-08-20 | 2026-08-21 | [[2026-08-20-syncrude-geometry-per-pass-misread]] | **`crash` is a mobilization label, not a fouling label** (Jesse, 2026-08-20): it means the facility hit operational trouble and needed us cleaning on a moment's notice, unscheduled. Usually the coil is dirty; not necessarily, and not by definition. The rollup currently reports `crash` at a mean of 33 ft/hr against `routine` at 99 and its own prose reads that gap as fouling ("a crashed furnace runs significantly dirtier"), so six rows are mixing urgency with condition and the number is used to price emergency work. Split into separate callout-type and coil-condition columns, suppress the `crash` mean until condition is recorded independently, or leave as-is and fix only the prose? | **Ruled: re-label, keep the class** (Jesse, 2026-08-21). The causal fouling claim is struck everywhere it was stated as live guidance; `crash` is redefined as a **job class / callout label** — an unscheduled mobilization — and the column now explicitly does not record how dirty the coil was. The 6-row mean **stays published** and stays the basis for pricing an emergency quote, because emergency jobs are what carry whatever the gap actually reflects; what changes is that nothing may cite it as evidence about coil condition. **The gap is also confounded and that is now stated in the rollup** — four of the six crash rows are multi-mode (2–3) large vacuum heaters, so ÷Mode normalization is doing work a fouling reading would credit to coke. **No schema change:** the column keeps the name `Condition` (a now-acknowledged misnomer) because splitting it moves the lint-locked `DURATIONS-HEADER` on ~40 cards and most historical rows would land as `unknown` — that belongs to DQ-017's re-grain bundle, and the exemplar now says so inline. **Applied to:** `04-knowledge/_canonical-heater-card.md` (vocabulary + naming note), `templates/_heater-template.md`, `tools/estimating_rollup.py` (segmentation prose, section heading `by coil condition` → `by job class`, new confound callout), the regenerated `04-knowledge/estimating-actuals-rollup.md`, `01-context/estimating-approach.md`, and `usadebusk-estimating` SKILL.md (config repo). **Deliberately not edited:** [[2026-07-22-duration-model-capture]] and [[2026-07-26-idea-research-pig-actuals-maturation]] repeat the fouling reading — they are dated insight notes recording what was believed then, superseded by the source note, not live guidance. **Also caught on verification:** the same causal claim was live in a second generator — `tools/pig_usage_rollup.py` ("a crashed furnace eats more pigs than routine service") plus its bore-vs-condition confound paragraph. Both corrected to job-class framing and that rollup regenerated; figures unchanged. The claim had propagated to every consumer of the `Condition` column, not just the durations rollup. **Verified:** every figure in both regenerated rollups is byte-identical (durations: crash 6 rows 13–67 mean 33, first 1 row 75, routine 5 rows 47–259 mean 99); only prose moved. |
| DQ-027 | 2026-08-20 | 2026-08-21 | [[2026-08-20-syncrude-geometry-per-pass-misread]] | **Outlier rule — does it enter the duration model?** Jesse, 2026-08-20: coils on one heater clean within a few hours of each other; one coil running 12–24 hrs longer than its siblings means a problem specific to that coil that decoke, or corrupt data, and the estimate comes off the average of the coils that cluster, never the outlier. Nothing in `01-context/estimating-approach.md` or the `usadebusk-estimating` skill tells an estimator to check per-coil spread before deriving a rate — which is how the Syncrude ~6 ft/hr figure came to be built on the one anomalous coil set. Add it to the duration model (Lane 4, config repo), record it as a heater-card convention only, or leave it as tacit field knowledge? | **Ruled: into the duration model now; recording convention deferred** (Jesse, 2026-08-21). The rule is written into `01-context/estimating-approach.md` (Duration model) and `usadebusk-estimating` SKILL.md as a new **Outlier check** bullet beside the Calibration check, referenced again from the actuals-govern paragraph so it fires at both the "read the rollup" step and the "let an actual govern" step. Rule as written: check the per-coilset spread before deriving a rate; take it off the sets that **cluster**, never the outlier; and **state in the duration math which set you excluded and why** — an unstated exclusion is indistinguishable from cherry-picking. The worked case is cited in all three places (CAD25004: 48 / 35 / 36 pig hrs, same rig config and mode; the 35 and 36 agree within 3%, the 48 sits 13 hrs off, and the struck ~6 ft/hr came off the 48). A pointer also went into the rollup's "Reading this", noting the spread is **not** visible from that table — its rows are per-job heater totals, so the check has to go to the card's Field Notes or the job report. **Deferred into DQ-017, not blocking:** the *recording* half — marking an outlier coil set at capture time — has nowhere to live until the per-coilset re-grain exists, since Task Durations is one row per job per heater. |
| DQ-018 | 2026-08-16 | 2026-08-21 | [[2026-08-15-retirement-sweep-what-else-has-outlived-its-reason]] | A retired note lands on `closed-unactioned`, which is terminal to lint but **absent from the Terminal-Note Sweep's allowlist** (`executed · resolved · complete · superseded · spec-complete`), so a correctly-retired commitment stays in `00-inbox/` forever — four already do. Is that deliberate (a retirement should stay visible) or an oversight? If an oversight, the fix is one status added to the allowlist in [[vault-capture-loop-spec]]. **Now five, 2026-08-16:** [[2026-08-16-steady-flux-bore-profile-request-owed]] retired the same day it was opened (Jesse handled it by text) and is already stuck. Evidence for the row, not a decision on it. | **A approved, B rejected, and C — not in the row — applied alongside it.** (Jesse, 2026-08-21.) **A:** `closed-unactioned` is added to the Terminal-Note Sweep allowlist in `04-knowledge/vault-capture-loop-spec.md`. **B rejected on the list's own contents:** `superseded` was already there and is the same shape — a commitment that ended without being done — so keeping `closed-unactioned` off was an omission, not a policy of keeping retirements visible. **C:** the backlog was cleared by hand in the same pass, because **A alone is inert**. The sweep runs at the end of inbox ingestion and the capture loop was disabled 2026-08-21, so nothing would have executed the new rule. The count had grown from four at the time of asking to **seven**: `2026-07-28-replay-ordering-discipline`, `2026-08-01-thesis-v2-rerun-owed`, `2026-08-16-report-gold-two-values`, `2026-08-16-steady-flux-bore-profile-request-owed`, `idea-context-packet-builder-skill`, `idea-portfolio-revival-pass`, `idea-rig-layout-diagram`. All seven were verified tracked, free of `revisit-trigger:`, and free of live backticked path references before moving, per the spec's own three safety rules; all seven confirmed still tracked after. Lint held at 0 errors — the real gate, since DEAD-LINK became an *error* on 2026-08-21 and the seven carry live inbound wikilinks (`idea-rig-layout-diagram` has seven), which stay green only because `check_dead_links()` includes `archive/` in its resolution set deliberately. **Half the stated harm was already gone:** the same day's `inbox_stats()` fix stopped counting terminal-status notes toward inbox age, so these seven were inflating the raw count only. **Not folded in:** the allowlist-versus-`ALLOWED_STATUS` drift runs both directions — `executed` and `spec-complete` sit on the allowlist while being outside `ALLOWED_STATUS` — and several `TERMINAL_STATUS` values would be wrong to sweep (`awarded`/`lost` are quote outcomes, `decided-blocked`/`approved-blocked` mean decided-but-blocked). Queued as DQ-029 rather than settled here. Inbox 55 → 48. |
| DQ-028 | 2026-08-21 | 2026-08-21 | [[2026-08-17-idea-research-frozen-baselines-unexercised-defects]]; [[2026-08-19-idea-research-backtest-visually-broken-document]]; [[2026-08-21-idea-research-stated-justifications-unmeasured]] | Do we build any more checking tools, and which? Three idea-research notes each proposing one — a frozen-baseline audit, tier-one structural assertions for the document back-test, and a sweep of layout-rule because-clauses. | **Two approved, one rejected.** The row's own framing was corrected before ruling: only one of the three was a build. The frozen-baseline note says "bounded one-shot investigation, not a build" and the because-clause note says "no standing machinery," so the "more tooling, default no" premise was wrong. **Built:** `assert_structure.py` in the fieldpm skill (config repo `0f64003`) — BORDERS and KPI-RULE, verified against the real 2026-08-16 incident, clean on all three fixtures. Three false-positive classes were found and exempted by running it, including a width check that fired on 17 of 18 tables of an accepted report and was removed after measuring. **Done:** the widths claim measured (true, but not the house convention it implied) and the LAYOUT-rule class in `_canonical-job-sheet.md` amended so a rule stating a reason about rendered output records the date it was last checked — "verifiable is not verified." **Rejected:** the frozen-baseline audit, on priority rather than merit — it audits regression machinery while the others guard a customer-facing document. **Left open:** the remaining ~12 because-clauses, an afternoon's read, deliberately not half-done. |
| DQ-023 | 2026-08-18 | 2026-08-18 | [[2026-08-18-prestaged-duplicate-workup-authoritative-copy]] | The remainder of DQ-022. Which copy governs on the two remaining historical bids that each hold two disagreeing workups — the **Valero McKee 4-heater July 2026 TA decoke** and the **ExxonMobil Baytown Pipestill 3 furnace decoke, Jan 2026** — and should a sweep be built for divergent pairs (Option D, never addressed in DQ-022)? | **All three parts closed 2026-08-18, and the headline is that this was largely not a defect class.** **(1) Valero McKee** — never a duplicate rendering. The `Bids\DSP25138 2025-11\` copy (2025-11-05) carried Mob at $0.00 and 110-H101 at 18 pigging hours against the facility-root copy's $71,753.74 and 36 hrs + 4 rig-over, and the quotation `.docx`/`.pdf` were generated beside the root copy the next evening: an abandoned draft and a finished bid. Jesse confirmed and the draft went to the Recycle Bin. Its quote number stays unverified and explicitly parked — 25099 is Suncor Commerce City's, 25138 has a better claim from PBF Martinez F-41A/F-41B, and nothing depends on it. **(2) ExxonMobil Baytown PS3** — no action needed, exactly as the review note predicted. The SharePoint job register puts USA#26007 at a contracted $162,576.42, matching the `Jobs\USA26007 PS3 2026-02\` copy on every total it carries; the `_History\` copy at $169,947.76 is a superseded revision sitting where superseded revisions belong. Both copies internally consistent, both reading `DSP#: 25070`. Job facts filed on the Baytown facility note. **(3) Option D sweep — closed as not worth building.** The evidence killed it: of the three pairs that opened DQ-022, one was a draft vs a finished bid, one was a legitimate `_History\` archive, and only the ExxonMobil F-201 pair was a genuine problem — and that one a store sweep could not have caught, because the divergence was between the store and Jesse's company laptop, not between two files in the store. A sweep would have fired on all three and been right about none of them in the way that mattered. The off-system habit is Jesse's to fix; the reading caveat lives in both scripts' docstrings. **Reopen if a divergent pair ever proves to be a real pricing defect** — that has not happened yet. |
| DQ-022 | 2026-08-18 | 2026-08-18 | [[2026-08-18-prestaged-duplicate-workup-authoritative-copy]] | Three bids hold two workup copies each that disagree on total — DSP26085 by $6,180.00, DSP25138 by $96,343.64, DSP25070 by $7,371.34. Per bid, which copy is authoritative and does the stray get deleted (the DSP26071.1 precedent) or kept as a labelled revision? | **A approved with edits, B and C rejected, D never addressed (carried to DQ-023).** Jesse ruled **$46,657.08 authoritative** on DSP26085 — the opposite of what the review note argued. The note's case rested on four artifacts agreeing at $40,477.08, but all four were downstream of the same stale workup, so agreement was circular, not corroborating. Opening both sheets settles it in one pass: hours identical at 6/18/6/6, the full $6,180.00 on equipment **rates** (pumper flat $450 → tiered $500/$650/$600/$500, support $30 → $35, filtration $150 → $200). `DSP26085.md` had already flagged the flat $450 as anomalous against DSP26071 — the correction was in the note, one section above the table it contradicted. `Bids` copy sent to the Recycle Bin; quote note rebuilt from the survivor; `backtest_workup.py` `prefer` **removed** so a future duplicate raises instead of resolving silently (3/3 pass). **Closed out fully the same day, and the premise corrected:** Jesse rebuilt the quotation's pricing block and Section 8 rate card and moved the workup back to `Bids\`; both verified against a LibreOffice render. The "never sent" reading was wrong — the revised quote **was** sent, from Jesse's work laptop, so ExxonMobil has held $46,657.08 all along and the copy in CANON was the pre-adjustment original. That is the finding worth carrying: for ~1 month the canonical store's quotation for a live bid was superseded, nothing marked it, and both `presend_gate.py` and `backtest_workup.py` reconcile against CANON so neither can see it. Raised as a new class in DQ-023. |
| DQ-021 | 2026-08-17 | 2026-08-17 | [[2026-08-17-rig-out-gate-honored-inconsistently]] | `manual/14` §14.6 carries a **WARNING** that customer written acceptance of the inspection data is required before any rig-out activity, and §10.5 states the reason — rig-out ahead of acceptance forecloses a re-run. Valero H-102B honored it (Quest gave final clearance before rig-out); USA26041 did not (rig-out proceeded on the preliminary, ~48 hrs before `26-0663-002 Rev. A` issued), and the B_8_C question a re-run could have settled is still open with the vendor. Six jobs carried an elected smart pig; only these two record the sequence, so n=2 opposite. Tighten practice to wait for the final and price the standby, or amend the manual to accept a preliminary? Not proposed — SOP and field-execution content. | **Neither fork — the note's premise was wrong, and the manual's error was larger than the gate.** (Jesse, 2026-08-17.) Rig-out never waited on the final report and the manual never said it did: it says *inspection data*, and the note substituted "final report." The gate is real and correctly placed but belongs **before dewatering**, not before rig-out — acceptance is given against the vendor's preliminary digital report, which confirms both that usable data was captured and that no fouling remains. A non-acceptance releases nothing: pigging continues and the tool runs again. The flagged inconsistency was therefore not a WARNING being ignored, it was rig-out correctly not being gated. **Two deeper errors surfaced and were corrected in the same pass.** Rig-out was mis-scoped — §11.3 folded inspection acceptance and dewatering into the rig-out sequence, when rig-out is the physical demob that follows them. And the role boundary was wrong across §7.1, §7.2, §8.2 and §11.3, all of which had USADebusk installing and removing launchers, receivers and jumper spools and reconnecting customer flanges: USADebusk supplies and owns the launchers and receivers but does not bolt the facility's heater flanges — the customer's pipe-fitters hang them, remove them and reconnect the heater piping, because the units weigh over 400 lbs and the fitters have crane and forklift access USADebusk does not. USADebusk's boundary is the 3" Fig. 200 connection on the launcher/receiver. `usadebusk-estimating` had the role boundary right throughout (its pipefitter adder prices exactly this), so no bid was mispriced and the drift was confined to the procedural layer. **Queue hygiene, recorded 2026-08-21:** this row sat `open` for four days after being ruled. Nothing checks whether a queue row's source note is already resolved — `unqueued_decisions()` checks only the reverse direction, open notes absent from the queue. |
| DQ-001 | - | 2026-07-20 | - | What is the third-party markup default? | Third-party markup is one of **5% / 10% / 15%**, set by the specific project/facility contract — **no default** (the "10% baseline (no contract)" framing was wrong; 10% is a valid tier, not a fallback). Corrected in the `usadebusk-estimating` skill, `01-context/company-context.md`, and `04-knowledge/concepts/estimating-pricing.md`. Decided by Jesse (in-session). |
| DQ-002 | - | 2026-07-22 | [[2026-07-22-duration-model-capture]] | Capture the duration model from Jesse and apply gap-fills A–F to the three sources. | Duration model captured from Jesse and gap-fills A–F applied: pigging rate is per single unlooped coil; coker/crude/**vacuum** + multiple-tube-size derates; rig-in 4/6/8/12 tier scale with rig-out matching; rig-over = `ceil(passes÷mode)−1` (~1 hr launchers pre-installed / ~2 hr waiting on fitters). Applied to `usadebusk-estimating` skill, `01-context/estimating-approach.md`, `04-knowledge/concepts/estimating-pricing.md`. Decided by Jesse (in-session). |
| DQ-003 | 2026-07-28 | 2026-07-28 | [[2026-07-28-prestaged-stale-editor-buffer-guard]] | Given the 2026-07-19 silent-revert incident on B-101.md, add a mechanical diff-gate or disable Obsidian auto-format, or is the adopted `git diff -w` habit sufficient? | **A + B approved, C dropped.** A applied narrowed — WORD-DELTA gains `--worktree`, since the incident file was never staged and the staged-only rule could not see it. B (Source mode) is a manual toggle left with Jesse and carried as a `revisit-trigger:` on the source note. |
| DQ-004 | 2026-07-30 | 2026-08-01 | [[2026-07-30-prestaged-portfolio-revival-still-worth-doing]] | Is the deferred full portfolio-revival pass (re-verify Knowledge Loop OS C/D/F, re-run thesis experiment under v2 scoring, route findings) still worth doing as-scoped, worth narrowing to just the thesis re-run, or worth dropping? | **B approved — narrowed to the thesis v2 re-run alone.** C/D/F re-verification and findings-routing dropped as bookkeeping with no forcing function. The re-run was approved but unexecuted (frontier-model calls across a 30-item corpus), filed as owed work at [[2026-08-01-thesis-v2-rerun-owed]]. **Retired unrun 2026-08-15** — the pre-registered verdict turned out to be determined by data already in hand: B pass@1 at 0.962 sits within 10pp of any value A can take (ceiling is 3.8pp above it), so the frozen thresholds return "inconclusive, items too easy" whatever the run produces, and v2 rescoring can only move scores upward. The v1 headline stays non-citable — that was the thing needing resolution, and a ruling retires it as cleanly as a measurement would. Reviving the thesis means building harder items, not re-running this corpus. |
| DQ-005 | 2026-07-31 | 2026-08-01 | [[2026-07-31-prestaged-routine-service-derate-seed-data]] | Is the 5-row routine ft/hr spread (47-259, mean 99) enough to act on with a service-based derate or revisit trigger, or should it stay tracked with no action until more routine rows accumulate? | **B approved — revisit trigger, no numeric derate.** No figure enters any pricing document at n=5; the qualitative derate list in `estimating-pricing.md` is unchanged. C rejected on the note's own counter-argument (HP-0007's 51 ft/hr is routine, not hard, and would sit inside an illustrative "hard" range by coincidence off a two-row bucket). The threshold — 10 routine mode-normalized rows — is machine-checked: `tools/vault_health.py` gained a `routine-rows` token, since an unrecognized `[machine: …]` token silently degrades to event-shaped and would have looked checked without being checked. |
| DQ-006 | 2026-08-02 | 2026-08-15 | [[2026-08-02-prestaged-over-wide-tables-remainder]] | Should `_directory.md`'s narrative-width Facilities table be trimmed to an index (pushing prose to each `_facility.md`), and should the new 11-column Tube Geometry header get a `TUBE-GEOM-HEADER` lint lock like `DURATIONS-HEADER`? | **(1) A approved, B rejected. (3) C approved, D rejected.** `_directory.md` stays rich — only the linter is complaining, and splitting costs a second file open on every read to recover the "why". `TUBE-GEOM-HEADER` built on `check_durations_header()`'s anchor-and-compare shape, shipped with fixture T-300. All 41 cards plus exemplar and template already carried the identical 11-column header, so the lock fires on zero existing files: pure drift protection with no backlog, a better position than `DURATIONS-HEADER` had when written. |
| DQ-007 | 2026-08-06 | 2026-08-15 | [[2026-08-06-prestaged-checkbox-delta-trigger]] | Should `CHECKBOX-DELTA` gain an unprompted trigger (fold `--worktree` into the daily loops, add an ungated PreToolUse hook, add a session-start check) or stay manual-only? | **A approved with edits; B, C, D rejected.** The note's flagged risk was measured rather than argued — 43 daily-loop commits replayed gave WORD-DELTA **43/43** and CHECKBOX-DELTA **0/43**, so plain A would have shipped a 100%-firing rule beside a 0%-firing one and the noise would have taken the signal with it. The 100% is structural: 31 commits are the loops regenerating `INDEX.md`/`health.md`, and all 12 remaining are a seed status flip losing one word. Real content loss across all 43: zero. So the diff rules now skip generated paths, and both loop specs move to `--worktree`. B rejected as an ungated hook on an unmeasured fire rate — the exact mistake WORD-DELTA's own back-test exists to prevent. |
| DQ-008 | 2026-08-07 | 2026-08-15 | [[2026-08-07-prestaged-f4-instruction-density-second-fixture]] | Should the F4 (SOP) instruction-density arm test run now to corroborate the F5 finding, get filed as owed work like the three existing `-owed` notes, or be dropped at n=1? | **A approved and executed the same session; B and C rejected.** F4 replicates F5 on every axis against a structural rather than numeric pass bar. Arm B failed (invented the document number, section order and footer). Arm C matched arm A on document number, title-block grid, section order, phase structure, footer wording and the 4.250" max pig OD, and produced 13 judgment calls to A's 11. Both stripped guardrails held without their instruction text. Finding is now **n=2** across two skills and two pass-bar shapes. Still does not license a trim pass across nine skills. Write-up at `~/.claude/regression/f4-instruction-density-arm-test-2026-08-15.md`. |
| DQ-009 | 2026-08-08 | 2026-08-15 | [[2026-08-08-prestaged-f6-rig-tier-decision]] | Should F6's ambiguous launcher-access wording be amended to make the rig-in tier deterministic, ruled as judgment (naming the expected tier on the current wording), or left open as bounded, documented noise? | **All three options rejected — they shared a false premise.** Each assumed the F6 fixture wording was ambiguous. The real defect was that the rig-in tier rule listed only two drivers (connection elevation, run distance) when there are three: **mode — pumps utilized**, which sets circuit lines and therefore hose count, and the three **multiply rather than offset**. That gap produced the whole 12-vs-10 divergence and four inconsistent replays. Rule corrected in `usadebusk-estimating` and propagated to `01-context/estimating-approach.md` on 2026-08-15. **Closed out the same day:** F6 was replayed blinded against the corrected rule, passed all twelve diff keys, and was re-promoted (config `ebb1217`) — rig-in 10→12, rig-out 10→12, raw 41→45, quoted unchanged at 48, mob/demob unchanged at $3,774.00 each. The baseline's `open_tier_selection_question` is retired as resolved at 12. |
| DQ-010 | 2026-08-10 | 2026-08-15 | [[2026-08-10-prestaged-quote-note-bid-folder-staleness]] | Should a quote-note-vs-bid-folder staleness check be built (extending POINTER-DEAD in lint, or as a health-dashboard soft signal), or deferred until the pre-send reconciliation gate exists? | **A rejected; B approved, narrowed by C.** A lint rule cannot distinguish an unsynced folder, an offline machine and a genuinely stale note, and lint is a binary 0-errors gate that cannot carry a maybe. Built instead as a `Bid folder` column on the Commercial pipeline table: existence and recency only, base-gated exactly like POINTER-DEAD. Value reconciliation deferred to the pre-send gate, which reads the workup's reconciled total instead of guessing from filenames. Found two real gaps on first run, neither the motivating case — DSP26080 records no bid-folder path, DSP26039 no `verified:` date. |
| DQ-011 | 2026-08-11 | 2026-08-15 | [[2026-08-11-prestaged-backtest-workup-fixture-mapping]] | Should `usadebusk-fixture-replay-guard.mjs` gain a mapping from `usadebusk-estimating/scripts/*` (backtest_workup.py/extract_workup.py) onto the f1/f6 replay requirement, given a back-tested 25% (1/4) miss rate above the hook's own adopted 14% threshold — and if so, is f1/f6 replay the right verification or should it run the script directly? | **A and B rejected; C approved on corrected reasoning.** The note's load-bearing unknown resolves no: f1 and f6 are *conversation* fixtures — replay prompts pasted to a model, carrying no `.xlsx` and invoking no Python — so they cannot exercise the scripts, making A a replay that proves nothing. The cited miss (`beb24ed`) is not a miss: its commit body records "Not in any frozen fixture, so no replay impact," and the diff is one T&C string in `render_proposal.py`, which `backtest_workup.py` never calls. Substantive-miss count over the full history is **zero**. B's correct form gates on the backtest's exit code, but that run leaves no dated artifact to check, so it would fire at 100%. No hook change. |
| DQ-012 | 2026-08-13 | 2026-08-15 | [[2026-08-13-prestaged-syncrude-fill-flush-question]] | Should Jesse's read on whether the Syncrude per-coilset pig-hour figures include fill/flush time be captured now, or is it moot today and better bundled with the parked per-coilset re-grain schema decision — or does the "~6 ft/hr" figure need to be reproduced first? | **B approved with edits; A and C rejected.** Deferred, but not closed as moot — closing as moot is exactly how the figure would enter a benchmark later with the question still open, so a `revisit-trigger:` was pinned to the same event that unparks the re-grain decision. C's finding reorders the question and is the important part: ~6 ft/hr **cannot be reproduced from card data at all** — the 47 tubes / 2,311 ft are unevenly split across coils, so no per-coilset footage exists to divide by the 63/48/53-hour sub-totals. Reproduce first, qualify second. Recorded on the heater card as well as the review note. |
| DQ-013 | 2026-08-14 | 2026-08-15 | [[2026-08-14-prestaged-obsidian-link-retargeting-guard]] | Should the vault add a `LINK-FACILITY` lint rule to catch wikilinks that resolve to the wrong facility, treat it as a one-time remediation with no new tooling, or fold the question into DQ-003? | **A approved with edits, B applied, C rejected on a false premise.** C assumed DQ-003 was still open; it closed 2026-07-28, and the note contradicted itself on this (its own Source Material table said "closed"). A as specified would have caught neither the cause nor the survivor — it compares facility segments, and the surviving link is a *bare* `[[_facility]]` with no segment, in `archive/`, which `SKIP_SCAN` excludes. Rule ships in two halves (MISMATCH + AMBIGUOUS); `strip_inline_code()` added after the first run produced six false positives on prose discussing the broken form. Archive link fixed by hand. |
| DQ-015 | 2026-08-15 | 2026-08-15 | [[idea-quotation-workup-reconciliation-check]] | Build the quotation-vs-workup pre-send gate — a reconciliation on the single pair being submitted, not a calendar sweep. Opened because DQ-010 and `health.md` both deferred value reconciliation to a gate that did not exist. | **Built and verified the same day** — `scripts/presend_gate.py` in `usadebusk-estimating` (config `c7d31c2`). Applies the 2026-07-29 compare-scope-before-dollars rule to one pair; exit 0 send / 1 do not send / 2 could not judge. Every verdict was produced by the tool against real documents rather than predicted: DSP26095 and DSP26085-`Bids` pass at a $0 execution-line gap, DSP26085-`Jobs` blocks on a **$6,180.00** matched-scope gap, and DSP26026 blocks on the 12/36/12 → 6/24/6 narrowing signature reproducing the rule's own worked figure of **$39,548.16** to the cent. **The narrowing re-price is deliberately not automated** — it needs equipment-profile × rate-table math no quotation states in structured form, and guessing it would produce a confident wrong pass on the one branch where the estimate and the bid legitimately disagree; the gate names what narrowed and hands the arithmetic back. **Two defects found by building it.** `backtest_workup.py` was **silently dead for the second time** — not a storage bug this time but two ordinary events (the facility tree gained a customer-group level, and DSP26071.2 was awarded so it moved `Bids/ → Jobs/`), so cases now resolve by filename rather than hard-coded path; green again at 3/3 exact. And `read_duration()` hard-coded rows 16–24 while the strip sits at row 15 on the current template and row 13 on older ones, so **older workups returned no task hours at all** — an empty hour breakdown in generated proposals and no scope surface for this gate; now anchored on its header row. |
| DQ-014 | 2026-08-15 | 2026-08-15 | [[2026-08-15-prestaged-decision-queue-duplicate-closed-tables]] | Should this table's own two differently-shaped Closed sections be merged into one format (backfilling DQ-001/DQ-002), left split with an explicit placement rule added, or only have the stale "queue is empty at launch" prose corrected? | **A approved with edits.** Merged to the single richer `id \| opened \| closed \| source \| ask \| outcome` shape. **Edit:** DQ-001's `source` and both rows' `opened` are left blank rather than backfilled — the note's own risk paragraph is right that manufacturing a citable source would make the row look better-evidenced than the record supports. DQ-002's source was recoverable, but from its own row text (`[[2026-07-22-duration-model-capture]]`), not reconstructed. Their decision text is preserved verbatim in `outcome`. B's placement rule is kept as an operating rule, applied to the merged table. The stale launch section is retitled and corrected to past tense rather than deleted, since it holds real history. |

## Launch history (2026-07-05)

*Retitled and corrected 2026-08-15. This section previously ran under "Why the queue is empty at launch" and asserted in the present tense that "there are no genuinely-open decisions to seed" — true at launch, false from DQ-001 onward. Kept because the drain it records is real history worth not losing.*

The redesign proposal (2026-07-02) counted 12 open decision checklists in `06-reviews/` — the backlog that motivated this queue. That backlog **was drained on `main` over 2026-07-03 → 07-05** through real, per-note closures (see commits `1da7bec`, and the routing-review closures), *not* by the branch's blanket "verify-on-use" sweep (which was superseded and not applied).

At that reconciliation every `06-reviews/` review note was closed by frontmatter `status` (`resolved` / `complete` / `superseded` / `decided-blocked` / `approved-blocked`). The multiple unchecked `- [ ]` boxes remaining in those notes were **rejected mutually-exclusive alternatives**, not open asks — in each, exactly one option is checked and the note's Apply Log records the action taken. There were no genuinely-open decisions to seed *at that point*; the queue has since carried DQ-001 through DQ-014.

One housekeeping mismatch was found and fixed directly (not queued, because it needed no decision): `06-reviews/2026-06-30-skill-naming-cleanup.md` had `status: inbox` while its body said "both items are closed for now" — frontmatter corrected to `complete`.
