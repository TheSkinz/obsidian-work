---
type: review
status: resolved
review_type: pre-staged
source_authority: inferred
confidence: medium
created: 2026-08-14
review_after: 2026-09-13
related:
  - [[2026-07-30-obsidian-link-expansion-mis-resolved]]
  - [[2026-07-28-prestaged-stale-editor-buffer-guard]]
  - knowledge-system-governance
tags: [review, knowledge-system, data-integrity, obsidian, lint]
---

# Review — Should the vault add a lint check for wikilinks resolved to the wrong facility?

## Trigger

Pre-staging loop run 2026-08-14, processing the oldest unprocessed inbox item carrying a `vault-loop:` marker: `00-inbox/2026-07-30-obsidian-link-expansion-mis-resolved.md`. That note documented a 2026-07-30 incident where Obsidian auto-expanded 24 bare `[[_facility]]` wikilinks across the vault; 23 resolved to the correct facility folder, and one — in `archive/2026-06-26-cad25004-candidate-canonical-updates.md` — resolved to `02-facilities/PBF/Toledo-OH/_facility` instead of the note's actual subject, Syncrude Fort McMurray. It was corrected by hand in commit `1d0e522`. The source note left two open questions: whether the already-approved DQ-003 stale-editor-buffer guard covers this failure mode, and whether other pre-existing ambiguous bare links remain unexpanded elsewhere in the vault.

## Source Material

| Source | Authority | Notes |
|---|---|---|
| `00-inbox/2026-07-30-obsidian-link-expansion-mis-resolved.md` | Observed | Full incident writeup. Explicitly frames this as "the same incident class" as the 2026-07-19 B-101 revert but "a distinct failure mode" — not a content loss, a link that stayed well-formed and resolved to a real, wrong note. Notes `vault_lint.py`'s DEAD-LINK rule only catches links that resolve to nothing, not links that resolve to the wrong target, so lint passed cleanly on the bad link. |
| `06-insights/2026-07-28-prestaged-stale-editor-buffer-guard.md` (read this run) | Observed | DQ-003, closed 2026-08-01. Jesse approved A (narrowed) + B: WORD-DELTA gained a `--worktree` mode that reports word-level *losses* against the working tree, and Obsidian's Live Preview table editor was switched off (Source mode). Neither targets this failure mode: WORD-DELTA reports losses, and a wikilink gaining a path prefix is an addition, not a loss — the note's own Apply Log independently flags this exact blind spot ("WORD-DELTA cannot catch this — a checked box is an addition, and the rule reports only losses"), in the context of a different addition (a spurious checkbox), but the same mechanical gap applies to link retargeting. |
| `tools/vault_lint.py` (grepped this run for all rule codes) | Observed | Full rule set: `CHECKBOX-DELTA`, `CONF-CONFLICT`, `DEAD-LINK`, `DURATIONS-HEADER`, `INBOX-AGE`, `OP-FRONTMATTER`, `ORPHAN`, `POINTER-DEAD`, `REVIEW-OVERDUE`, `SECRET`, `STATUS-VOCAB`, `SUPERSEDED`, `WORD-DELTA`, `YAML-COMMENT`. None checks that a wikilink's facility path segment matches the linking note's own facility. |
| `~/.claude/hooks/` (listed and grepped this run) | Observed | `usadebusk-word-delta-guard.mjs`, `usadebusk-staged-count-guard.mjs`, `usadebusk-exec-guard.mjs`, `usadebusk-git-guard.mjs`, `usadebusk-fixture-replay-guard.mjs` and their tests. No hook addresses link-target correctness; the one text match on "facility" was coincidental (inside `usadebusk-exec-guard.test.mjs`, unrelated). |
| `archive/2026-06-26-cad25004-candidate-canonical-updates.md` (read this run) | Observed | The two body-text links (lines 34–35) are already corrected to `[[02-facilities/Syncrude/Fort-McMurray-AB/_facility]]`. But the frontmatter `related:` list (line 12) still carries the bare, unexpanded `[[_facility]]` — the same ambiguous form that caused the original mis-resolution, in the same file, still live. This directly answers the source note's second open question: yes, at least one more pre-existing ambiguous link remains, in the exact file the incident already touched. |
| `INDEX.md` (grepped this run) | Observed | Contains dozens of bare `[[_facility]]` links by design — one per facility entry, disambiguated for a human reader by trailing plain-text `(Facility/Location)` annotations rather than by the link path itself. This is machine-generated by `tools/vault_index.py`, not hand-edited in Live Preview, so it carries a different risk profile: any future facility-match lint rule needs to exclude generated index files or it will misfire on all of them. |
| Recent `git log` (checked this run) | Observed | Nothing since 2026-07-30 addresses link-retargeting; the most recent related work is the Outlook/SharePoint documentation effort, unrelated. |

## The Question

Given a confirmed second instance of the same ambiguous-bare-link pattern still sitting live in the vault (this run's own finding, not yet fixed by anyone), and no existing lint rule or hook that would catch a wikilink resolving to the *wrong* target rather than resolving to nothing: should the vault add a mechanical `LINK-FACILITY`-style lint check, treat this as a one-time remediation with no new tooling, or fold the question into DQ-003 (still open) rather than deciding it separately?

## Proposed Change

### A. Add a `LINK-FACILITY` lint rule

For any wikilink whose target path runs through a facility folder (`02-facilities/<Company>/<Site>/...`), flag it when the target's facility segment doesn't match either the linking note's own `facility:` frontmatter (if present) or the linking note's own containing folder. Would have caught the 2026-07-30 mis-resolution mechanically and, per this run's finding, would also flag the still-live bad link in `archive/2026-06-26-cad25004-candidate-canonical-updates.md:12` today. Needs an explicit exclusion for generated index files (`INDEX.md`) to avoid misfiring on its by-design bare links.

- [ ] Approved
- [x] Approved with edits
- [ ] Rejected
- [ ] Needs more research

### B. One-time remediation only, no new lint rule

Fix the one remaining bare link this run found (`archive/2026-06-26-cad25004-candidate-canonical-updates.md:12`) and stop there. Rationale: both known instances trace to a single one-time Obsidian mass-expansion event on 2026-07-30, not a recurring pattern, and Source mode (already enabled via DQ-003's Option B) removes the Live Preview auto-expansion behavior that caused it — a repeat may not be possible under the current editor setting.

- [x] Approved
- [ ] Approved with edits
- [ ] Rejected
- [ ] Needs more research

### C. Fold into DQ-003, no separate decision

Treat link-retargeting as a sub-question of DQ-003 rather than a parallel ask — DQ-003 is still open, and this note becomes supporting evidence for whatever Jesse decides there rather than its own row.

- [ ] Approved
- [ ] Approved with edits
- [x] Rejected — premise is false, see Decision
- [ ] Needs more research

## Risks and Counter-Arguments

Option A adds a fourteenth lint rule to a system Jesse has previously pushed back toward propose-only, low-ceremony (2026-07-03 policing-pattern rejection) — though DQ-003's own WORD-DELTA extension was accepted on the grounds that a narrow, mechanically-scoped data-integrity check is a different shape than open-ended policing, and A here is comparably narrow (one path-segment comparison, not general content review). Option B leaves the gap open for any future bulk-link operation (a vault reorganization, a facility rename, a future Obsidian plugin) to repeat the same failure silently, since DEAD-LINK still would not catch it — the "Source mode should prevent recurrence" argument is unverified, since Source mode was already enabled *before* B-101's original 2026-07-19 incident type existed, but this link-expansion event is a different Obsidian feature (wikilink autocomplete/expansion, not the Live Preview table editor) and may not be gated by the same setting at all. Option C risks the narrower, still-live finding (the archive file's uncorrected frontmatter link) getting lost inside DQ-003's broader and differently-framed question, which is about content *loss*, not link *correctness* — the two failure modes don't share a detection mechanism.

## Decision

**Resolved 2026-08-15 (Jesse, in session). A approved with edits, B approved and applied, C rejected on a false premise.**

**C's premise is false, and the note contradicts itself about it.** Option C says "DQ-003 is still open," and The Question repeats it. This note's own Source Material table says the opposite one screen earlier: "DQ-003, closed 2026-08-01." The decision queue is the authority and it is blunter than either — DQ-003 sits under `## Closed` with `opened 2026-07-28 / closed 2026-07-28`, so the note also has the closing date wrong. There is nothing to fold into. Flagging the shape rather than just the fact: a pre-staged note that establishes a fact in its evidence table and then contradicts it in an option is the failure mode these notes are most prone to, because the options get drafted against the original inbox item's framing rather than against what the run just found. Worth reading the two sections against each other before ruling on any future pre-staged note.

**A is approved but needed two corrections, because as specified it would not have caught either the cause or the survivor.**

The note claims A "would also flag the still-live bad link in `archive/2026-06-26-cad25004-candidate-canonical-updates.md:12`." It would not, twice over. First, A compares a link's facility segment against the linking note's own — and the surviving link is a *bare* `[[_facility]]` with no facility segment at all, so there is nothing to compare. Second, `archive/` is in `SKIP_SCAN`; lint does not read that directory under any rule. (DEAD-LINK includes `archive/` in its *resolution* set, which is about link targets, not scanned sources — an easy pair to conflate.)

So the rule ships in two halves. **MISMATCH** is A as written: a note under `02-facilities/<Co>/<Site>/` linking into a different `<Co>/<Site>`. **AMBIGUOUS** flags any bare `[[_facility]]`, which is the form that actually caused the incident — 12 facility folders each hold a `_facility.md`, so the link resolves by basename to whichever one Obsidian picks. Without the second half the rule protects against a consequence while ignoring the cause.

Two further corrections to A's stated design. The proposed exclusion for generated index files is unnecessary — `INDEX.md` carries the GENERATED marker and `collect_notes()` already skips it, so its dozens of by-design bare links cost nothing. And `facility:` frontmatter is deliberately **not** used as the reference: it carries a slug like `Westlake-South-Westlake-LA` that does not correspond to the `Westlake-Chemical/Westlake-LA` path pair, so matching on it would false-positive on every quote note. The linking note's own folder is the only reliable reference.

**One thing the build found that no option anticipated.** The first run fired six times on the real vault, all false positives, all prose *discussing* the broken form inside inline backticks — in the incident writeup and in this review note. `body_lines_outside_fences()` strips fenced blocks but not inline spans. A rule whose only hits are its own documentation is wallpaper, so `strip_inline_code()` was added and the rule now reports zero on the live vault. This is a general trap for any future lint rule that pattern-matches on markup the vault also writes *about*.

**B is approved as remediation, not as an alternative.** Its "Source mode should prevent recurrence" argument is unverified — the note itself concedes Source mode governs the Live Preview table editor, while this was wikilink autocomplete, possibly a different setting entirely. That is not a basis for declining the rule. But B's fix was still needed and lint structurally cannot do it, so the archive link was corrected by hand.

## Apply Log

| Date | Action | By |
|---|---|---|
| 2026-08-15 | Ruled and applied. Added `check_link_facility()` to `tools/vault_lint.py` with both MISMATCH and AMBIGUOUS halves, registered alongside the other tree rules, warning severity. Added `strip_inline_code()` after the first run produced six false positives on prose. Shipped fixture `tools/fixtures/02-facilities/TestClient/Test-City-TX/T-400.md` tripping both halves per the no-fixture-no-rule contract; self-test now 16 rules. Real vault: 0 LINK-FACILITY findings, warnings unchanged at 58. Separately fixed the surviving bare link at `archive/2026-06-26-cad25004-candidate-canonical-updates.md:12` by hand to `[[02-facilities/Syncrude/Fort-McMurray-AB/_facility]]`, matching the two body links already corrected in `1d0e522` — lint cannot see `archive/` and never will under the current SKIP_SCAN. Verified this is the only surviving bare link in the vault outside generated files and prose. Decision-queue row DQ-013 left for the queue-hygiene pass. | Claude (review queue) |

## Apply Log

| Date | Action | By |
|---|---|---|
| 2026-08-14 | Note filed by pre-staging loop from `00-inbox/2026-07-30-obsidian-link-expansion-mis-resolved.md`; confirmed no existing lint/hook coverage and found a second live instance of the same pattern in `archive/2026-06-26-cad25004-candidate-canonical-updates.md:12`; no vault content modified beyond the source marker (the found bad link was left untouched — fixing it is an apply action, out of this loop's scope) | Claude (pre-staging loop) |
