# Fixture: WORD-DELTA "after" revision

Deliberately narrow lines, so the "after" revision can rewrap every one of them and a line diff shows the whole paragraph as changed.

Phase II runs sequentially across all three circuits. A single Trimax Pumper drives the set, so the passes are pigged one after another.

The reissue question was superseded on 2026-07-27: REV 2 is required.
