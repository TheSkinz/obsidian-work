# Fixture: WORD-DELTA "before" revision

Deliberately narrow lines, so the "after" revision can rewrap every one of them
and a line diff shows the whole paragraph as changed.

Phase II runs concurrently across all
three circuits. Mode = 3. A single Trimax
Pumper drives the set, so the passes are
pigged simultaneously rather than one
after another.

The reissue question was settled on
2026-07-27: this is not an open action
item and no REV 2 is required.
