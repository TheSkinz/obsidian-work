---
type: heater-card
status: draft
source: fixture
verified: never
---

# T-400 (fixture)

LINK-FACILITY fixture. Trips both halves of the rule.

MISMATCH half: this note lives under `02-facilities/TestClient/Test-City-TX/`, and the
link below points into a different facility folder. Lint must flag it.

See [[02-facilities/OtherClient/Other-City-OH/_facility]] for the site record.

AMBIGUOUS half: the bare link below has no facility segment at all, which is the exact
form that auto-expanded onto the wrong facility on 2026-07-30. The MISMATCH half
structurally cannot see it — there is nothing to compare. Lint must flag it separately.

Also see [[_facility]].

Valid `source`/`verified` frontmatter is present so this fixture isolates the link rule
and does not also trip OP-FRONTMATTER. The two targets do not exist, so DEAD-LINK fires
here too — that is incidental and not what this fixture is proving.
