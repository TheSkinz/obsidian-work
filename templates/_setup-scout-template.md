---
type: setup-scout-dossier
tool: <tool name>
status: draft
created: YYYY-MM-DD
adoption_trigger: <the real task that made this tool worth adopting now>
tags: [setup-scout, llm-tooling]
---

# Setup Scout — <Tool>

Dossier produced at adoption time, before first configuration attempt. Search first (curated indexes, official docs, GitHub, Reddit/forums, Hugging Face where relevant), extract proven patterns, adapt second. File to `07-llms/<tool>/overview.md` (or `08-systems/` if it's system-level tooling). Every claim below needs a source link — no unverified specifics stated as certain.

## Recommended setup

<The single best-evidenced install/setup path, stated as steps. Note OS-specific caveats (this machine: Windows 11, PowerShell primary, Git Bash available).>

## Configuration

<Non-default settings power users actually change, with the why. Include config file paths.>

## Plugins / extensions / connectors

<Only ones with real adoption evidence. Name, one-line purpose, link.>

## Key repos and assets

<GitHub repos, HF models/datasets/spaces, curated lists worth starting from. Skip anything unmaintained.>

## Power-user workflows

<2–4 workflows people report as the actual payoff of the tool — not feature-list marketing.>

## Common setup mistakes

<Documented failure modes and how to avoid them.>

## What to ignore

<Features, forks, or advice that's stale, overbuilt, or irrelevant to my use case — with a one-line reason each.>

## Verification

<Concrete checks proving the setup works end-to-end. "Run X, expect Y." The setup session isn't done until these pass.>

## Sources

<Links used, most load-bearing first.>

<!-- Re-open test: if this dossier is never re-read after the setup session, note that in the next vault review — two unused dossiers means drop the template. Skill conversion (/setup-scout) only after two pilots pass both the one-session-setup test and the re-open test. -->
