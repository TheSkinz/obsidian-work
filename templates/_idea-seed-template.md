---
type: idea-seed
status: unexplored
created: YYYY-MM-DD
tags: [idea, vault-system, future]
---

# <Short title>

Idea seed captured YYYY-MM-DD for a future exploration session. The read below is tentative — confirm intent with Jesse before designing.

**Tentative read:** <One or two sentences: what problem or opportunity this idea addresses, stated as your best guess, not a commitment.>

**To explore:** <The open questions that need answering before this could become a real design — what would have to be true, what tradeoffs exist, what's unknown.>

**Gate:** <Optional. Delete this line if the idea is researchable now. If the idea only makes sense once something else is true — a test has run, data has accumulated, another build has landed — state that condition here in one sentence. A gate means "do not spend a research cycle on this yet," and the research loop enforces it: it checks the gate before researching, and if the gate is verifiably unmet it flips this seed to `status: gated` and moves on instead of researching. Mirror the same condition into a `revisit-trigger:` frontmatter field so the health dashboard's dormant-trigger registry keeps it visible while it waits — a gated seed with no `revisit-trigger:` is an idea you will lose.>

<!-- Do not add a Research Findings section by hand — the Vault Idea Research Loop appends that itself as a linked review note in 06-reviews/ and updates this file's status to `researched` with a `related:` link. Leave status as `unexplored` when creating a new seed; the loop sets `gated` itself if it finds the gate shut. -->
