# Change Log
**Purpose:** Append-only **decision log** — human decisions and approved canonical/structural changes only. It carries the *why* that a diff cannot. The record of *what* changed and *when* lives in `git log` (author, date, diff — and it cannot drift from itself).
**Format:** Date | File(s) changed | What changed | Trigger
**Narrowing rule (effective 2026-07-05):** From this date forward, log only decisions and approved canonical/structural changes here — not routine loop runs, ingests, or ordinary content edits (those live in `git log`). Entries dated before 2026-07-05 predate this rule and are preserved as-is; history is not rewritten.

---

| Date | File(s) | Change | Trigger |
|---|---|---|---|
| 2026-08-15 | 00-inbox/ (20 notes: 14 status flips, 2 retirements, 1 promotion, 2 kept-with-reasoning, 1 new seed); 06-insights/ (3 triggers retired); 50-dashboards/decision-queue.md (DQ-015/016/017); tools/vault_health.py (`note-count` trigger token) | **[gated] Retirement sweep, surfaces 1-3 — and the largest finding is that most of what looked like undecided work was finished work nobody had flipped.** Fourteen notes were carrying `open`/`inbox`/no-status while their decision had already closed elsewhere, ten of them as DQ rows on 2026-08-15 itself; every closure was re-verified against evidence rather than the DQ summary before flipping. **Three seeds were found built-but-filed-`researched`** — the `DURATIONS-HEADER` lint lock, the business-normal register, and the job-report generator, the last built the same day its spec was written and filed as pending for 25 days. That is structural, not sloppiness: `researched` means "Jesse has not decided," the Terminal-Note Sweep is forbidden from touching it, and nothing else ever revisits it, so a landed build has no path out. Seeded for a class fix. **Two retirements.** `idea-context-packet-builder-skill` — its trigger had *fired* against USA26041's Baytown mobilization on 2026-08-11 and sat unnoticed for four days; the answer was no friction, so it retires on a fired-and-answered trigger rather than re-parking. `idea-portfolio-revival-pass` — dead by consequence, the inventory it existed to surface having been drained one DQ row at a time, which is the alternative the seed itself floated. **One promotion:** the hardened-corpus/vision-arm seed is the *beneficiary* of the thesis retirement, since DQ-004 named building harder items as the only live path. **One reversal of a same-week ruling:** DQ-012 pinned a `revisit-trigger:` on the Syncrude fill/flush question one day earlier specifically so it could not go quiet — retired here and replaced by DQ-017, because the gating event (a heater-card schema open) had not occurred since 2026-07-28 and three triggers were queued behind it, making the wait itself the risk. The reproduce-first constraint is carried verbatim into the queue row, which is the stronger carrier: capped, counted, and ageing. **Two owed builds re-tested and kept** with reasoning recorded — the baseline staleness detector, whose value went *up* while it waited (F6's stale baseline and F1's $720 crew-truck over-quote are exactly what it catches), and the coil visualization, every prerequisite verified intact on disk. **A near-miss worth keeping:** the wake condition written for `idea-llm-navigable-vault-map` was first anchored to the retrieval eval's stated "~100 notes" — counted against the commit as of that date, the real scanned population was **169**, low by about 70% as written. Same defect class as the Syncrude ~6 ft/hr figure. Re-anchored to the count the script computes, and `[machine: note-count>=N]` added to `vault_health.py` because an unrecognized token degrades silently to event-shaped and would have produced a trigger that looked checked and never was. | The sweep commissioned at the close of the thesis v2 retirement, 2026-08-15 |
| 2026-08-15 | 00-inbox/{2026-08-01-thesis-v2-rerun-owed.md, 2026-08-15-thesis-v2-rerun-handover.md, 2026-08-15-retirement-sweep-what-else-has-outlived-its-reason.md (new)}; 50-dashboards/decision-queue.md | **[gated] Thesis experiment v2 re-run RETIRED UNRUN — the pre-registered verdict was already determined by data in hand.** Owed since 2026-08-01 under DQ-004. `DESIGN.md` records v1 at A 0.795 / B 0.962 / C 0.962 with 100% of failures across all three conditions traced to the unit-scoring artifact, so v2 rescoring can only move scores upward. The frozen thresholds return **inconclusive** when B pass@1 sits within 10pp of A — and B at 0.962 is within 10pp of every value A can take, the ceiling being 3.8pp above it. No arithmetic path to a validating or falsifying result; the run would spend ~450 calls to return "items too easy," which the design already names the remedy for. Secondary: condition A specifies `claude-fable-5`, a routing choice no longer made for extraction-shaped work, and swapping it requires a new version of the frozen design rather than a flag change. **The v1 headline stays non-citable** — that was the thing needing resolution, and a ruling retires it as cleanly as a measurement. Reviving the thesis means building items a cheap model does not already score 96% on, decided on its own merits rather than inherited from July. The runbook written the same day is kept as reference, marked DO NOT RUN, because the mechanics survive the verdict. **Commissioned a retirement sweep for the next session** — the failure mode is that approval and filing are recorded while re-examination is not, and this session found four instances of it (this re-run, F6's stale baseline, F1's over-billed crew truck, the unreproducible Syncrude ~6 ft/hr figure). | Jesse asked whether the re-run was still worth doing given elapsed time and the Opus 5 release |
| 2026-08-15 | `~/.claude/skills/usadebusk-estimating/SKILL.md`; `~/.claude/regression/{frozen/f1-proposal-output.md, frozen/f6-duration-mobdemob-output.md, README.md, runs/claude-opus-5/}`; 50-dashboards/decision-queue.md; 00-inbox/ (handover + 2 idea seeds) | **[gated] Four Lane-4 estimating rulings from Jesse, and both Track-A fixtures re-promoted — one of which had been carrying a $720 defect since promotion.** **(1) Rig-in is always one of six values — 2, 4, 6, 8, 10, 12 — never odd, never fractional.** The tier ladder and the +2 fitter adder already land inside that set by construction, so a build-up producing 7, 9 or 11 has made an arithmetic error rather than a judgment call. **(2) Rig-in precision is explicitly not where estimating effort belongs** — it has never been a significant commercial concern; the drivers and hose count pick the right rung out of six, and **pigging hours are the line that deserves the complexity** because that is where the money and schedule risk move. **(3) Vault rig-in actuals are not a calibration basis** — the recorded durations mix sources, some rows being other salesmen's quoted figures produced by their own methods, which is why that column spans 2 to 34.5 hrs with eight rows above the 12-hr cap. Scoped to rig-in only; the pigging columns remain a valid cross-check on condition- and mode-matched rows. **(4) All mobilized crew trucks bill on mob/demob, but only ONE bills hourly** at project hours however many travel — the trucks taxi each shift between hotel and facility and exactly one is inside the facility at any moment, so 2 shifts × 12 hrs = 24 truck-hours per day. **F6 re-promoted** after the three-driver rig-in correction: rig-in and rig-out 10→12, raw 41→45, quoted unchanged at 48, mob/demob unchanged; its `open_tier_selection_question` retired as resolved at 12, and diff key 5 recorded as no longer independent (the skill's worked example names the fixture and its answer) with restoring independence explicitly rejected as effort the line does not warrant. **F1 re-promoted** on the first full replay→judge→patch→re-run→re-cut cycle this suite has run: the replay failed on the crew-truck line, ruling (4) followed, and the re-run came clean — **the frozen baseline was the wrong one**, having billed 2 × 48 = 96 truck-hours since promotion. Equipment $38,130→$37,410, total $61,796.40→$61,595.60. | Session close-out of the F6 stale-baseline item left open by the review-queue pass earlier the same day |
| 2026-08-15 | 06-insights/ (10 review notes); 50-dashboards/{decision-queue.md, health.md}; tools/{vault_lint.py, vault_health.py}; tools/fixtures/ (T-300, T-400, README); 04-knowledge/{concepts/business-normal-facts.md (new), knowledge-system-governance.md, _canonical-job-sheet.md, vault-capture-loop-spec.md, vault-prestaging-loop-spec.md}; 01-context/estimating-approach.md; 02-facilities/Syncrude/Fort-McMurray-AB/7-1-F-1.md; archive/2026-06-26-cnd25004-candidate-canonical-updates.md; `~/.claude/regression/f4-instruction-density-arm-test-2026-08-15.md` | **Cleared the entire review queue in one pass — ten decisions ruled, decision queue and review-note backlog both to zero, health dashboard fully green.** Rulings, briefly. **(1) Backtest-workup fixture mapping: rejected** — f1/f6 are conversation fixtures that cannot exercise the Python scripts, and the cited miss was correctly dispositioned at the time, so the evidentiary basis was zero. **(2) Business-normal facts: approved** — the three-tier convention (point-of-use note / skill guardrail / assistant memory) written down and the memory-tier register given a vault copy at `04-knowledge/concepts/business-normal-facts.md`; no new mechanism built. **(3) Over-wide tables: `_directory.md` left rich, `TUBE-GEOM-HEADER` lock built** — all 43 files already conformed, so the lock fires on zero and is pure drift protection. **(4) CHECKBOX-DELTA trigger: approved with edits** — a 43-commit back-test put WORD-DELTA at 100% and CHECKBOX-DELTA at 0%, so the diff rules now skip generated paths and both daily loops moved to `--worktree`; an ungated hook was rejected on an unmeasured fire rate. **(5) F4 density arm test: run** — replicates F5 exactly (B fails, C matches A, both stripped guardrails held), taking the instruction-density finding to n=2 across two skills and two pass-bar shapes; still does not license a trim pass. **(6) Quote-note staleness: health soft signal, not lint** — existence and recency only, value comparison deferred to the pre-send gate; found two real gaps on first run. **(7) Job sheet: six blocks ruled a floor, not a ceiling**, and USA26041's live sheet pinned as unmigrated until its job report closes it. **(8) Syncrude ~6 ft/hr: deferred but pinned and reordered** — the figure cannot be reproduced from card data at all, so reproduction now gates the fill/flush question via a revisit-trigger. **(9) `LINK-FACILITY` lint rule built in two halves** (wrong-facility mismatch + bare ambiguous `[[_facility]]`), option C rejected on a false premise the note contradicted itself about; the surviving bad link fixed by hand since lint cannot see `archive/`. **(10) Decision queue's two Closed tables merged** to the richer format, with DQ-001's source left blank rather than manufactured. Separately, the corrected rig-in tier rule was propagated from the skill into `01-context/estimating-approach.md`, which had still carried the two-driver version (Jesse approved in session), and `vault_lint.py` gained `.claude` in SKIP_SCAN after a task worktree inside the vault took it from 0 errors to 6. **Left open on purpose:** F6's frozen regression baseline is stale and still needs a replay and re-promotion (recorded on DQ-009), and DQ-004's thesis v2 re-run remains unexecuted. | Session review-queue pass, continuing 2026-08-15's health-dashboard FAIL cleanup |
| 2026-08-15 | `~/.claude/skills/usadebusk-estimating/SKILL.md`; 06-insights/2026-08-08-prestaged-f6-rig-tier-decision.md | **The rig-in tier rule was missing its dominant driver, and that — not fixture wording — was the F6 12-vs-10 divergence.** The rule set the tier from two drivers (connection elevation, run distance from the pumper); Jesse named a third: **mode**, i.e. pumps utilized, which sets circuit-line and spool count and therefore how much hose gets built. A circuit line runs from a pumper port to one launcher or receiver connection: single = 1 pump / 2 lines / 2 spools, double = 2 / 4 / 4, triple = 3 / 6 / 6. Hoses are 17 ft, hard pipe 20 ft, a 60 ft run is ~4–5 hoses **per line**. Critically the three drivers **multiply rather than offset** — elevation adds to run length (60 ft out + 60 ft up = 120 ft, doubling hose per line) and mode multiplies by line count — so the long-standing "one driver low, one high, call it the middle" reading is an error, and it is what produced four inconsistent F6 replays. Also recorded: **rig-in never exceeds 12 hrs and rarely exceeds 8**, so Very large is a ceiling rather than a rung reached by accumulation, and the pipefitter adder stacks *inside* the cap (Very large + fitter wait = 12, not 14). F6 under the corrected rule: rig-in 12, rig-out 12, raw 45, landing 48 on a +3 adjustment that no longer trips the mid-band diagnostic. Jesse's first-stated triple-mode hose count (12–15) contradicted his own primitives (6 lines × 4–5 = 24–30); flagged in session, **confirmed a slip, corrected to 24–30**, and the per-mode figures at a 60 ft run (single 8–10 / double 16–20 / triple 24–30) are now in the skill. | Pre-staged review note 2026-08-08 (F6 rig-tier ambiguity) walked in session; Jesse supplied the mode/hose mechanics and the 12-hr cap, which reframed the question from a fixture amendment to a rule gap. |
| 2026-08-11 | 08-systems/outlook-email-architecture.md; 07-llms/copilot/overview.md; memory `project-outlook-architecture`; SharePoint `CONTEXT_Outlook-Routing.md`; 00-inbox/2026-08-11-outlook-doc-three-copies.md (new) | **[default] The vault's Outlook three-tier model was fabricated on both tiers and is corrected against the live system.** Folders read from the mailbox and categories from Outlook's own category list. The real folders are a **personal triage workflow** — `00 Action - Today`, `01 Waiting On Others`, `02 Internal Review`, `03 Sent - Pending Response`, `04 Execution Active`, `05 Automation & Systems`, `06 IT / Security`, `07 Read Later`, `99 Archive` — not the nine-folder commercial pipeline recorded since 2026-06-29. The real categories are a **seven-stage pipeline** — RFQ, Proposal Draft, Submitted, Awarded, Execution, Post-Job, Closed / No-Go — with USA# assigned at Awarded; there are **no facility or customer categories**, and the remaining six Outlook entries are stock colour placeholders. Of the fourteen categories the vault claimed, exactly one (`Awarded`) exists. **The commercial lifecycle therefore lives on the category axis, not the folder axis** — which makes the corrected document more useful than the original, since the DSP→USA transition is now mapped where it is actually true. **Root cause, four times over:** folder names taken from a stale vault note, category names from Copilot's rendering, six facility categories invented by Copilot from customer names in email and published as verified fact, and finally an accurate `Phase 1 Outlook Operating Procedure - Reference.docx` that already existed on OneDrive and would have been found by one tenant search before anything was written. Every one was answerable by reading the system. **Standing rule added:** search the tenant for existing documentation before authoring into the knowledge base, and never restate folder or category names from a note. Three overlapping copies now exist and the reconciliation is filed, unresolved. | Routing eval run immediately rather than waiting a day — Jesse's call — surfaced the folder mismatch within hours of publishing |
| 2026-08-11 | 07-llms/copilot/overview.md; 00-inbox/2026-08-10-sharepoint-pilot-cleanup-owed.md; _OUTPUTS/sharepoint/_COLUMN-VALUES.md; SharePoint `Knowledge` + `Site Assets` | **[default] The Furnace Decoking build is functionally complete: 29 documents with full column metadata and Descriptions, and `Decoking Knowledge.agent` relocated out of its own knowledge source into `Site Assets`.** Both panel actions verified against the REST API rather than the panel's transcript, which is required because it commits before offering an undo card. **A recorded limitation is corrected: `_ExtendedDescription` is NOT unreachable in both directions.** Writes still fail a `MERGE` with `InvalidClientQueryException`, but the field reads cleanly through `RenderListDataAsStream` with an explicit `ViewFields`, because that endpoint renders through the view engine rather than the `SP.Data.KnowledgeItem` entity type. That turned the prescribed "verify visually in a list view" into an exact string comparison — all 8 panel-written Descriptions confirmed verbatim. **Generalised rule worth keeping: when an OData property is refused, try `RenderListDataAsStream` before concluding a field is unreachable.** Still owed, and it needs the M365 desktop app: re-ask the pig OD question with the agent tagged to confirm the relocation did not disturb it, against the recorded baseline of `MANUAL-04` §4.3 + `MANUAL-18` §18.2. | Panel reported both actions done; verification of the Description half required solving the read path the prior note had written off |
| 2026-08-11 | 07-llms/copilot/overview.md; 00-inbox/idea-sharepoint-projection-drift-check.md; _OUTPUTS/sharepoint/_COLUMN-VALUES.md; SharePoint `Knowledge` library (8 files uploaded, columns set) | **[default] Tranche B loaded — the Furnace Decoking `Knowledge` library is complete at 29 markdown documents, all columns and Owner set and read back from the API.** The durable finding is not the load, it is what the load caught. **A manual upload silently bypasses the projection and nothing on the SharePoint side notices.** `CONTEXT_Outlook-Routing.md` had been uploaded by hand as the *vault source* rather than the export's projection — YAML frontmatter intact, no provenance line, dead `[[wikilinks]]` — and every cheap signal was correct: right filename, all twelve columns set, Owner right, content reads fine. It surfaced only on comparing stored `File/Length` against the staged copy (5489 vs 5461) and was confirmed by SHA-256. Frontmatter in the body is precisely what the exporter strips so the columns stay the only copy of `status`/`review_after`; two copies drift silently. Overwritten and verified identical by hash. **This is the first live instance of the drift [[idea-sharepoint-projection-drift-check]] exists to catch, and it settles that design:** presence-only or columns-complete checks would both have passed it, and `--check` was green throughout because the *staging folder* was right and the *library* was wrong — so the check must do a REST read of live content, not a local diff. That idea's gate is now satisfied and it is unblocked. **Also reproduced rather than assumed:** Owner does not default on any upload path (n=2, API and drag-and-drop); `moveto()` is still refused by the auto-mode classifier, leaving the `.agent` relocation a panel job; `_ExtendedDescription` still fails `MERGE`, so Descriptions remain panel-only and unverifiable programmatically — retested, not taken on trust. | Ranking retest passed, unblocking the load; drift found while verifying uploads byte-for-byte rather than trusting HTTP 200 |
| 2026-08-11 | tools/sharepoint_export.py (MANIFEST + exclusion note); _OUTPUTS/sharepoint/CONTEXT_Workflow-Map.md (staged copy deleted); 07-llms/copilot/overview.md; 00-inbox/{2026-08-10-markdown-ranking-retest-owed, idea-sharepoint-projection-drift-check}.md | **[default] `01-context/workflow-map.md` is dropped from the SharePoint projection, answering the open question the retest note raised about whether it belongs on the site. It does not.** Two independent grounds, either sufficient. **It is not decoking content** — its three sections are paused initiatives, retired claude.ai Projects, and completed internal projects (Claude skills, Google Drive architecture, the Outlook security rules), none of which a furnace decoking knowledge library has any use for. **It is deliberately historical**, self-described as *"retired-system history — nothing live"*, having stopped tracking active work 2026-07-19; loaded as `active` it would surface a superseded Google Drive structure and retired Projects as current state, and `stale` would not rescue it because the content is off-topic as well as retired — that label would just be accurate about a file that should not be there. Same disqualifying profile as `rfq-intake-protocol.md`, which was dropped 2026-08-10 for being vault-internal mechanics that mean nothing to Copilot. It also carried wikilinks to `active-jobs` and `rfq-intake-protocol`, neither of which is in the library. **Net effect on the load: tranche B is still eight files, but not the same eight** — Outlook routing in, workflow map out. Column values for all eight are now worked out in `_OUTPUTS/sharepoint/_COLUMN-VALUES.md` against the build spec's choice vocabulary rather than inferred. | Preparing the tranche B column values surfaced it — the file had to be assigned a Status, and no honest value existed |
| 2026-08-11 | 08-systems/outlook-email-architecture.md (new); tools/sharepoint_export.py (MANIFEST); _OUTPUTS/sharepoint/CONTEXT_Outlook-Routing.md (staged); 07-llms/copilot/overview.md; 00-inbox/{2026-08-10-markdown-ranking-retest-owed, idea-sharepoint-projection-drift-check}.md | **[default] The Outlook three-tier routing model gets a standalone canonical home in `08-systems/` and is projected to the Furnace Decoking site, making tranche B nine files rather than eight.** It previously existed only as a summary inside `07-llms/copilot/overview.md`, which meant Copilot read the nine folders daily without knowing what any of them encode. The new document carries the folder legend, the three category groups, the orthogonality rule, the map from the commercial folders onto the DSP#/USA# transition, and an explicit valid/invalid inference section. **Two design rulings.** The six facility roster categories are **described structurally and deliberately not enumerated** (Jesse, 2026-08-11) — they are live tenant state not recorded anywhere in the vault, the roster changes as accounts do, and Copilot reads the actual names off the messages themselves; naming them would have bought nothing and guaranteed staleness. The five-rule email security architecture is **excluded** — it governs inbound deliverability, a different concern from routing semantics, and its rule bodies are not in the vault, so writing them would have been inference. Its open smtp.com Rule 02A gap is noted as a pointer only. **Consumer correction worth carrying forward:** SharePoint agents cannot reach Outlook mail, so `Decoking Knowledge` can never use this file for its stated purpose. Its actual consumer is tenant-wide M365 Copilot, which reads the mailbox and indexes the site — so it belongs on the site, but any eval of it must be run **untagged**, and this is the first concrete thing the current architecture cannot serve, which is the case for the deferred Agent Builder declarative agent. Upload not performed; no browser path to the tenant from this session. | Ranking retest passed `9e24396`, unblocking tranche B; this was the one item in the load that did not yet exist |
| 2026-08-11 | 07-llms/copilot/overview.md; 00-inbox/2026-08-10-markdown-ranking-retest-owed.md (closed); 00-inbox/2026-08-10-sharepoint-pilot-cleanup-owed.md | **[default] The markdown ranking retest passed, closing the last open risk in the SharePoint knowledge-base build. Markdown ranks as well as it retrieves in this tenant, so the `.md`-no-conversion ruling is confirmed on evidence rather than merely left unreversed, and the `.docx` converter contingency is closed rather than dormant.** Both questions ran against the 21-file `Knowledge` library with `Decoking Knowledge` tagged, and both passed all three citation criteria with no fabrication — every quoted string and all five tube IDs verified exact against source, all three section attributions correct. **Two results carry the claim, and neither is explainable by filename matching.** Q1 cited `MANUAL-11` alone despite `MANUAL-14` §14.6 being *titled* "Approval gate before rig-out" — a stronger surface match than §11.1 — and despite chapters 10 and 7 stating the same gate; it went to the one chapter carrying both gates together. Q2 cited one chapter for the rule out of the four that state it, choosing §4.3 *Pig sizing derivation* against a question asking how the figure is *determined*. **The pre-registration was corrected before the run, not after:** it predicted chapter 9 and named a single governing chapter per question, but the rule and the rig-out gate are each stated in four chapters, so "governing vs plausible neighbour" was under-specified and would have misgraded a correct answer. Scored on the merits; chapter 4 is the better answer than the predicted chapter 9. **Second finding, independent of the first:** four runs were voided because the agent was not tagged, sending the questions to tenant-wide Copilot, which answered every one from a job-specific execution SOP on OneDrive. The failure is altitude, not accuracy — asked how max pig OD is *determined*, it returned F-501's 4.635"→4.875" framed as "the governing rule." A job instance served as methodology is harder to catch than a wrong number, and a library-scoped agent cannot reach OneDrive without OneDrive files being added explicitly, which is what makes the tag load-bearing. Unblocks tranche B, the Outlook routing document, and the `Decoking Knowledge.agent` relocation; none were run, session scoped to score-and-file. | Dormant trigger on the retest note fired — tranche A indexed 2026-08-11, the day after loading |
| 2026-08-10 | tools/sharepoint_export.py (`613d872`, then `b959a45`); 07-llms/copilot/overview.md (`75967d1`, `795fe3b`); 00-inbox/2026-08-10-sharepoint-pilot-cleanup-owed.md (`c4844d7`); 00-inbox/2026-08-10-markdown-ranking-retest-owed.md (new); SharePoint `usadebusk.sharepoint.com/sites/FurnaceDecoking` | **[default] Nothing on the Furnace Decoking site needs redacting — every member is trusted with all of its data — and the eval question written against the opposite assumption is retired.** Jesse's ruling. The inherited constraint read "no cost basis, margin, markup, or rate content on this site while James Lee is a member," and a scan built to enforce it found two hits: the third-party markup floor in `field-operations.md` and, more seriously, the full 5/10/15% markup ladder at `company-context.md:27` — which was **already live on SharePoint** in the pilot's `CONTEXT_Company`. A redaction mechanism with must-match assertions was built to cut both (`613d872`), then removed the same day once Jesse ruled (`b959a45`); dead safety machinery is worse than none because it implies a boundary nobody is enforcing. It is recoverable from `613d872` in one revert if membership ever stops being fully trusted. **Consequence for the eval:** Phase 5 Q6 required the agent to "refuse and produce no number" on crew rate and markup. It refused the crew rate (absent from the corpus) and answered the markup (present, via `CONTEXT_Company`). Scored against the standing ruling rather than the stale criterion, Phase 5 is **six-for-six** — Q5's clean no-fabrication refusal being the load-bearing result, since it means the documented trigger to evaluate Copilot Studio never fired. **The platform finding survives the ruling and is the durable part:** the agent's Behavior instructions carry a hard refusal on commercial figures and it disclosed anyway. `overview.md` already recorded that Agent Builder cannot block *fabrication*; it cannot block *disclosure* either. An instruction is not an access control — the only reliable lever is what does or does not enter the library. **Tranche A loaded the same session:** 19 manual chapters, columns and Owner set via REST and audited to zero drift, descriptions written verbatim through the Copilot panel, both deliberate test artifacts (the falsified Rev-A chapter and the markdown probe) recycled and verified gone. | Q6 returned the markup ladder during the Phase 5 eval; raised as a live disclosure on a site with a member outside the cost-basis circle, Jesse ruled the premise wrong rather than the content (2026-08-10) |
| 2026-08-10 | 07-llms/copilot/overview.md (`8c076cb`); tools/sharepoint_export.py (new); 00-inbox/2026-08-10-sharepoint-pilot-cleanup-owed.md; SharePoint `usadebusk.sharepoint.com/sites/FurnaceDecoking` | **[default] The `.md`-to-`.docx` conversion requirement is reversed, and the vault→SharePoint projection is now a straight copy.** The build plan's largest maintenance item — a converter in `tools/` — is dropped. Microsoft's semantic-index supported-types table (page revision 2026-04-23) still excludes markdown, but the same page describes the tenant index as "generated from text-based SharePoint Online files," and the tenant behaves the second way. **Verified by A/B rather than inferred:** `TEST-A.md` and `TEST-B.docx`, byte-identical apart from a unique token each, indexed within ~75 minutes (docs say daily) and were both retrieved; asked a content question naming no file and no token, the M365 app cited the **`.md`**. A library-scoped SharePoint agent then returned both tokens in a linked table and cited both files on a content question. `tools/sharepoint_export.py` replaces the converter: strips YAML frontmatter (so the SharePoint columns stay the only copy of `status`/`review_after`), stamps a provenance line, and has a `--check` mode that exits 1 on drift. It reproduces all four uploaded pilot files byte-identically. **Second correction, same session:** the SharePoint Copilot side panel is not read-only — it deleted a library, created one, added twelve columns with exact choice values, and built three views, all verified against the REST API. It commits before offering an undo card, so the transcript is not evidence; the API is. **Bounded claim:** markdown is proven *reachable and citable* at n=2 with identical content, never proven to **rank** against a corpus of differing files — Phase 6 carries an explicit volume retest, and Copilot Studio is separately reported (Tech Community, May–Aug 2026) to fail on `.md` in SharePoint libraries, so that migration stays a live risk. Revert path: the converter was never built, so reversing means adding one and re-exporting. | Jesse asked what the point of the SharePoint knowledge base was, then reframed it: not coworker enablement — build the M365 ecosystem so Copilot knows his workflows. The format question gated the largest chunk of work, so it was tested first (2026-08-10) |
| 2026-07-29 | 08-systems/mobile-field-access.md (new); 07-llms/claude/{cowork.md, code.md}; 04-knowledge/vault-capture-loop-spec.md; .claude/settings.json | **[default] Adopted a surface-routing rule for working the vault from the iPhone in the field: Remote Control by default, Dispatch with an explicit "open a Claude Code session", cloud sessions only when the desktop is down.** The rule follows from two verified asymmetries rather than preference — cloud and Cowork sessions don't read `~/.claude/skills/`, and the Capture Loop harvests only `~/.claude/projects/`, so those surfaces are both skill-blind and unharvestable. Recorded the harvest coverage hole in the loop spec, rewrote `cowork.md` (it still called Cowork "not a separate product" and the capture loop unbuilt), and widened the vault permission allowlist to the three tools scripts and read-only git so a one-handed mobile session doesn't stall. Revert path: delete the new note, `git revert` the others; nothing depends on them. | Jesse asked for an appropriate field workflow from the iPhone, then flagged that he's used Dispatch without a model for what it controls (2026-07-29) |
| 2026-07-25 | ~/.claude/skills/usadebusk-estimating/SKILL.md, ~/.claude/skills/usadebusk-equipment/SKILL.md, ~/.claude/skills/usadebusk-vault-ingest/SKILL.md, ~/.claude/regression/frozen/{f1,f2,f6} (config `e396ddf`, `e245d6a`, `c973ed4`, `0e32d94`, `8fa272c`, `1f7e6a1`); 00-inbox/2026-07-25-pm-billable-hours-open.md; 00-inbox/idea-rollup-per-rig-coilset-grain.md | **[gated] Five further Jesse rulings, all surfaced by replaying the first three rules rather than by reading.** (1) **A Project Manager is always on dayshift and bills like any other dayshift employee** — day-side per-person hours, same figure as the Day Supervisor, never the project total. USA26038 H-20 is the actual (PM 44 against a day side of 44 and a project total of 80); frozen F1 had been billing 45 and over-quoting by $1,680. (2) **Max pig OD is governing ID + 0.250" rounded to the nearest 1/8" pig size that does not exceed that ceiling** — 4.026" → 4.250", not the raw 4.276", because the raw sum names a pig that cannot be bought. Also the single carve-out to the derived-values rule, with the test that decides future cases: *max pig OD is our specification, wall thickness is a claim about the customer's steel*. (3) **The pipefitter adder stacks on the rig tier rather than promoting it, and rig-out mirrors rig-in at the whole figure including adders** (~90% of the time; a customer wanting something different is the exception). (4) **The parallel-friction allowance applies *after* the coil round-up**, not before — which required amending the pre-existing "round once, do not re-round downstream" sentence that forbade the second rounding the correct order needs. (5) **Land the total on a whole shift as a final judgment step** — crew are paid the full 12 hours whether they work 8 or 12, so a 20-hour project is a 24-hour cost. Applies when the heater has *no historical pigging times*, moves **up or down** by a few hours, and must be stated as its own step rather than folded into a task line. Also required: report shift-boundary fragility (how much slack before the next shift) alongside the total. | The first rule-writing pass's replays. Each of these was enforced by a frozen baseline, or by Jesse's practice, and stated in no skill. Verified by six fixture replays; F1, F2 and F6 all re-cut from passing runs. Two independent F1 runs converged on $61,085.00. The shift rule was deliberately **not** re-cut into the baselines — it is judgment, so the diff key is whether a run names the adjustment, not whether it hits a number. Jesse's own reframe closed the session: the shift round-up partly subsumes hour-level precision (F1 at 45 vs 46 hrs is 4 shifts either way; F6 at 31 vs 39 crossed 36 and cost a real shift), so estimating effort belongs at the shift boundary, not below it. |
| 2026-07-25 | ~/.claude/skills/usadebusk-estimating/SKILL.md, ~/.claude/skills/usadebusk-vault-ingest/SKILL.md, ~/.claude/regression/{README.md, fixtures/f6-duration-mobdemob-input.md, runs/claude-opus-5/*-ruleswrite.md} (config `98ac964`, `c961ad5`, `415c08b`); 00-inbox/2026-07-25-skill-drift-lane4-holds.md; 00-inbox/2026-07-25-f6-divergences-awaiting-adjudication.md; 00-inbox/2026-07-24-fixtures-work-better-as-rule-audit.md; 00-inbox/2026-07-25-pm-billable-hours-open.md | **[gated] Jesse closed the implicit-rule backlog in usadebusk-estimating — four rulings and a trigger change.** (1) **Rig tier is set by the launcher/receiver connection points, not the heater.** Coil footage and pass count barely matter; what matters is the *elevation* of the connection points (8 ft off grade is a walk-up; 60 ft means a crane and that much more pipe and hose) and the *run distance from the TriMax pumper* to them. Settled at the job walk. A ~2 hr adder applies where USADeBusk waits on the customer's pipefitters to hang launchers — a known recurring cost, not a contingency. The Small/Moderate/Large/Very-large hours survive only as an explicit no-job-walk fallback. (2) **A heater card carries stated values only, never derived ones** — and a `(derived)` marker is not a licence to write the value, because markers strip in copy-paste and a card is the record of what the customer's document says. Canonical in vault-ingest; estimating carries the pointer and the estimate-may-compute/card-may-not distinction. (3) **An enumerated list contradicting its own stated count is a flag, not a rounding error** — count, compare, stop, name both readings, price the swing. (4) **Fixtures now replay after any substantive skill edit, not only on model change**, because the battery's most valuable output has been defects in the thing being measured against, not model drift. All four items were rules that worked in Jesse's head, were enforced by frozen baselines, and were stated in no skill — the failure shape that cost most of a session on mob/demob. | The 2026-07-25 skill-drift review held items 4 and 5 as Lane 4; the F6 adjudication held two more; the fixtures-as-rule-audit note proposed the trigger change. Verified by replaying F1, F2 and F6 against the edited skills — the first use of the new trigger. Harness confirmed live first, after `backtest_workup.py` was found silently broken on 2026-07-25. F2 closed clean; F6 corrected mob/demob to $3,774.00 each; F1 fired the new intake rule and caught a $1,680 defect in its own baseline (PM hours). No frozen baseline re-cut. |
| 2026-07-25 | ~/.claude/skills/usadebusk-estimating/{SKILL.md, scripts/extract_workup.py, scripts/backtest_workup.py} (config `677d447`, `fb0018e`, `a4ed96f`); OneDrive Formosa bid folders (moved); Marathon Detroit DSP26026 workup (patched); 00-inbox/2026-07-25-quotation-workup-reconciliation-scan.md; 00-inbox/2026-07-24-dsp26085-submitted-wrong-quote-number.md | **[gated] Jesse ruled that a workup's mob/demob figure is not the quotable one, and three storage/tooling decisions followed.** Mob and demob bill as a **lump sum — a flat fee regardless of what happens during the mobilization** — and where workup and quotation diverge it is because *the contract caps what can be billed*. The workup is a cross-facility template whose mobilization build-up exists for **internal cost/profit visibility only**. This closed DSP26058's $9,602.00 gap (exactly 2× its $4,801 unit) as expected rather than defective, and corrected the audit method: mob/demob is excluded from reconciliation, execution lines only. Written into the skill in three places so it is reachable from where pricing actually happens, and enforced in code — mob/demob now emit CONFIRM (J field) flags, and a gap that is a whole multiple of a mob/demob unit reports as expected instead of tripping the "do not send" block. That blocking flag had been firing on healthy jobs, which trains a reviewer to ignore it. Verified against six real workups; DSP25142 correctly **still** blocks ($90,090 is not a multiple of its $14,274 unit), so the fix does not over-suppress. **Second decision: Formosa bids moved to the canonical store.** DSP26068.1 and DSP26075 existed only under the old Desktop-rooted tree, making `rfq-intake-protocol` step 1 false in practice; both folders moved whole (16 files) into `Formosa Point Comfort TX\Bids\`. This also revealed that `backtest_workup.py` was not merely fragile but **already broken** — its Exxon paths had gone dead when those files moved, so the generator's "proven" back-test had been silently unrunnable. Now single-rooted on canonical; all three pairs reproduce exactly. **Third: DSP26026's `26027` was a mistake** (no DSP26027 exists anywhere) — patched in the workbook XML rather than via openpyxl, which would strip its conditional-formatting extensions. That fix exposed a latent crash: the newly-reachable tab has a header row in the duration strip whose labels hit a numeric format; non-numeric duration rows are now skipped. Still open, unverified: that workup reads $153,715.40 against a submitted $114,167.24, a gap that is not mob/demob-shaped — the quotation covers the 70H1 Coker alone (6/24/6) while the workup row carries 12/36/12, probably ordinary scope narrowing. Revert paths: config `677d447`, `fb0018e`, `a4ed96f`; pre-fix backups for DSP26085 and DSP26026 in the session scratchpad. | Session: inbox triage → Exxon quote-number scan → totals reconciliation across Formosa, Marathon, HF Sinclair |
| 2026-07-25 | ~/.claude/skills/usadebusk-sop/SKILL.md (config `f9af97c`); ~/.claude/regression/runs/claude-opus-5/f6-duration-mobdemob-2026-07-25.md (config `2eebec6`); 00-inbox/2026-07-25-f6-divergences-awaiting-adjudication.md (new); 00-inbox/idea-pig-actuals-maturation.md (new) | **[gated] Closed the handover's reverse-direction item and replayed the second estimating fixture.** SOP builds load core + equipment, not estimating, yet three references in `usadebusk-sop` told the author to consult it. Fixed by keeping what each was protecting rather than deleting: the Rig-In/Rig-Out parentheticals now say directly that hours belong to the estimate and not the SOP, and the plug-header clause keeps the physical fact while marking the duration consequence as priced at bid time. Every cross-skill pointer left in that skill now targets a skill the SOP path actually loads. **F6 replayed** because it is the other estimating fixture and its 2× TriMax against 1 filter press is the exact configuration the rewritten `:111` governs — and frozen F6 still closed by recommending "a second press or a larger unit," the instruction retired that morning. The correction was adopted near-verbatim and nine of ten diff keys held. Frozen not re-cut: two divergences remain, neither caused by the change — a rig tier of Large 8 hrs against Moderate 6 (a judgment call the skill leaves open, but it moves 4 hrs at each end) and non-driver travel at $64 against $58, which is a **fixture defect** where the replay prompt's "use baseline rates" contradicts the job data and the skill's precedence rule. Both filed for adjudication. Revert paths: config `f9af97c`, `2eebec6`. | Session close-out: orphaned equipment rules |
| 2026-07-25 | ~/.claude/skills/usadebusk-vault-ingest/SKILL.md; ~/.claude/skills/usadebusk-estimating/SKILL.md (config `950f7a0`) | **[gated] Closed the pig-actuals gap so the estimating hierarchy's step 1 can fire for the first time.** The pig method resolves decoke history first, but nothing had ever written pig actuals to a heater card — three of the four CHS cards read `(not recorded)`. The chain was complete up to the last link: `/extract` logs pig runs, the ticket breakdown carries pigs as types and quantities, and `/report` pulls them via `extract_ticket_breakdown.py`. The break was in vault-ingest, whose per-heater write list named Job History, Task Durations and Field Notes only; `## Pig Specifications` was never written and pigs survived solely as Field Notes prose. Ingest now fills it from the report's PIGS USED table, which is **job-level, not per-heater** — unlike PIG hours, which allocate from the pumper→heater map — so a single-heater job writes its total as that heater's actual and a multi-heater job writes the total tagged `combined-heaters` on every card, reusing the convention Task Durations already carries. A multi-heater pig total is never split by inference. Estimating step 1 now reads the actuals wall: a `USA#####`-sourced row is an actual and governs, a `DSP#####` row is a quote and is not consumption evidence, `combined-heaters` is a ceiling. **`Source` is carrying the actuals wall because the schema change is held** — adding a `Condition` column to Pig Specifications is Lane 4 and awaits Jesse's explicit yes; until then pigs have no condition-match rule where durations do. Deliberately not built: the ft-per-pig rollup — one usable data point (HF-0012, ~180 pigs over 12,036 ft) is not a basis for a formula; revisit at three or four heaters. Revert path: config `950f7a0`. | Session: orphaned equipment rules — pig actuals follow-on |
| 2026-07-25 | ~/.claude/regression/frozen/f1-proposal-output.md; ~/.claude/regression/README.md; ~/.claude/regression/runs/claude-opus-5/** (config `232bd31`, `e774869`) | **[gated] Verified the estimating change against F1 and promoted a new baseline.** Two arms, both fresh-context subagents with the regression framing, the comparison output and the expected numbers withheld. Arm 1 replayed the fixture verbatim: all nine prior diff keys held, including the item-15 six-against-five piece-count contradiction, still caught and reconciled with the $1,200 swing priced rather than absorbed. Arm 2 was a **probe, not a fixture** — item 15 cut to "1× TriMax" with the piece count and driver split withheld — and the new derive-vs-ask block fired as designed: it named intake item 15 as half-supplied, stated the profile it assumed, priced the 4×3 swing at $2,200, and asked for confirmation. It also judged the filter-press capacity rule inapplicable at one TriMax against one press with reasoning, instead of boilerplating the warning. **The unplanned result is the more valuable one:** the 2026-07-24 firewater fix (`902fae8`) had shipped on reasoning alone and had never been run. The superseded baseline still carried the defect verbatim — "Firewater supply at the required volume and pressure," no size. Both new arms specify a 2" minimum and reject 1½". First behavioral confirmation that the fix works. Pig quantity moved off the forbidden per-pass series in both arms, but they landed 60 and 48 hard pigs from the identical stated method — recorded as inherent to a deliberately rough method, not corrected, and the baseline now tells the next replay to judge the method rather than the count. F1 re-cut from arm 1, byte-identical below the rule; supersedes the 2026-07-24 baseline, recoverable at `44e9d73`. Separately, the suite README claimed a mixed-model baseline with F1 and F6 still on Fable 5 awaiting review — all six frozen files record Opus 5, promoted 2026-07-24; the transition finished and the README was never updated. Corrected. Revert path: config `e774869`. | Session: orphaned equipment rules — verification pass |
| 2026-07-25 | ~/.claude/skills/usadebusk-estimating/SKILL.md (config `bb78eb8`); 00-inbox/idea-orphaned-equipment-rules-proposal-path.md (closed) | **[gated] Closed the orphaned-equipment-rules audit — the diagnosis was wrong, and the fix is fewer derivations, not more cross-references.** The deferred half of the 2026-07-24 firewater call. Sweeping all 144 lines / 9 sections of `usadebusk-equipment` against the proposal path (core + estimating; both load pairings verified from the skill descriptions rather than assumed) surfaced four candidates, and Jesse's reading dissolved three of them: they were never reachability problems but **situational facts only he can supply** that had leaked into the skill's derive column. Support-equipment count is set by customer election, room at the unit, and what is uncommitted that month across 4–6 concurrent jobs — no amount of heater data determines it, so estimating's mob math had been multiplying a piece count it cannot legitimately derive. `:111` was instructing a second **or larger** press when no larger press exists in the equipment library and a second may not be free. Pig quantities had no recorded method at all; Jesse's actual one (history → footage per tube size → projected fouling by unit type, hard pigs only) is now written down as deliberately rough, with an explicit bar on replacing it with a per-pass derivation. The 1/4"-vs-1/8" split proved to be two correct numbers for two consumers — estimate cost vs. the field pig load list — not a defect, now documented so it is not "reconciled" later. **Recommendation reversed mid-session:** loading `usadebusk-equipment` as a third skill on the proposal path was proposed, then withdrawn once the findings resolved to inputs — the +144 lines buys nothing that an ask doesn't. Jesse's decisions: fleet availability is **not** tracked in the vault (always ask — a hand-maintained calendar goes stale the moment it stops being updated); **no** site-laydown/real-estate field is added to the card schema, despite Suncor Montreal having invented one ad-hoc; the flange-rating adapter check is **dropped** (facility reps build adapting spools as a matter of course, the role boundary already sits in always-loaded `usadebusk-core`, and the flange record Jesse wants is already captured at intake item 7 / Section 5 / Connection info). Firewater cross-ref at `:313` deliberately left as-is. Revert path: config `bb78eb8`. | Session: orphaned equipment rules (handover-driven) |
| 2026-07-24 | ~/.claude skills/adversarial-review/SKILL.md; ~/.claude skills/usadebusk-estimating/SKILL.md; ~/.claude regression/adversarial-review-arm-test-2026-07-24.md (new); 06-insights/2026-07-24-idea-research-lint-lock-heater-schema.md; 00-inbox/idea-orphaned-equipment-rules-proposal-path.md (new) | **[gated] `adversarial-review` validated for the first time and demoted; one cross-cutting estimating gap closed.** The skill was adopted unvalidated and had never been tested against the alternative it implicitly beats. Three arms run blind on two regression deliverables with recorded defect lists: the incumbent Finder/Adversary/Referee chain found **3 of 6** recorded defects at **3.3x** the tokens and 3x the wall clock; a single agent with a grounding requirement found **5 of 6**; a plain single agent **5.5 of 6**. Two architectural causes, both measured not inferred: the chain is **subtractive** (Adversary and Referee only filter, so recall is capped by the Finder and two further Opus calls are spent lowering it), and **bundling destroys true positives** (a finding packaging a true claim with a false one gets disproved whole). Jesse's call: demote, don't delete — single grounded verifier is now the default arm, the chain is the opt-in precision pass, and a claim-level adjudication rule was added. Follow-up run settled the one variable the test had held constant: swapping the Adversary to **Fable 5** took the chain from 1/4 to 3/4 on F1 — the Finder's ceiling — at identical cost, so cross-model is now the default whenever the chain runs, and the skill's prior claim that same-model "gets most of the benefit on its own" is retired as measurably false. A **Phase 4.4 empirical gate** was added: agreement among same-family agents is one reading, not three. `Refute-or-Promote` (arXiv 2604.19049, Agarwal) verified real and its abstract read — its ten-reviewer unanimous false positive killed by one empirical test is the cited basis for that gate; results tables still unopened and the record says so. **Separately and higher-value commercially:** all three arms flagged that the F1 proposal never asked for a 2" fire hose, and the cause was structural — the rule lived only in `usadebusk-equipment`, which a proposal build (core + estimating) never loads, so Jesse's 2026-07-23 ruling was unreachable from the workflow needing it and every firewater bid would omit the ask. Section 8 now carries the trigger. Wider orphaned-rule audit deferred to `00-inbox/`. Lint-lock review closed as already-built (shipped `b7f5df3` 2026-07-20). Revert paths: config `151cd34`, `902fae8`, `43831e2`. | Session: adversarial-review architecture test (handover-driven) |
| 2026-07-23 | 04-knowledge/workup-to-proposal-generator-build-spec.md (new); 00-inbox/idea-workup-to-proposal-generator.md; 00-inbox/2026-07-23-deferred-workup-proposal-build-spec.md | **Workup-to-proposal generator: mapping build-spec written and validated (no generator built — Jesse's scope call).** The `workup xlsx → proposal docx` cell-region → line mapping was reverse-engineered from real files and **back-tested exact on three structurally different pairs**: DSP26071.2 ($60,287.42, T&M), DSP26085 ($40,477.08, T&M), DSP26068.1 ($112,642.23, Fixed Price) — every Section-7 line item and grand total reproduced 1:1 from the workup Project Financials block (col I revenue). Key rules the back-test forced/proved: locate the work tab by role not name (multi-scenario workups disambiguate by quote # / submitted total); **read the financials block by H-column label, not fixed row** (DSP26068.1 inserts a `Training:` line, moving `Total:` to row 9, and Training maps to its own quotation line); revenue is col I, cost/margin never reach the customer; round to cents. One element does **not** generalize and is flagged non-blocking: the Pricing Summary box (Equipment/Manpower/Materials) re-buckets per job and needs its own workup source region — carried as an open decision for the generator build. Also surfaced: submitted proposals embed pricing as **pasted Excel images** (DSP26085's docx has no text-extractable totals), which is *why* the generator adds value and why the back-test read the PDFs. Source question settled: build against the old-style workup (proven, multi-heater); `Workup-Template-v1.xlsx` is a parallel input-cleanup track. Mapping authored on Opus (hard reasoning already done on Fable); mapping generalized cleanly so no Fable escalation was needed. Revert path: git revert this commit. | Build-spec session (approved from [[2026-07-23-deferred-workup-proposal-build-spec]]) | Nightly idea-research loop found the seed's load-bearing question (does the work-up Excel have a stable structure across bids?) still unresolved from the vault's view — `usadebusk-estimating` skill still marks the template "a work in progress." Jesse answered directly in-session: **"The work ups are stable, but the data may be considered unorganized... we can back test them and even improve the structure of the current template if it warrants it."** Follow-up check of the local OneDrive working-copy tree confirmed it with real data: 4 live workups across 3 clients (HF Sinclair, Exxon Baytown ×2, Formosa Point Comfort) share the same sheet structure, and one full back-test (DSP#26071.2) reproduced its submitted quotation's total ($60,287.42) and every line item exactly from the workup's Project Financials block — proof-of-concept level evidence, same standard as the job-report generator's USA26038 back-test. Also surfaced: a `Workup-Template-v1.xlsx` redesign (dated 2026-07-19, addressing the exact "unorganized" complaint) already exists in the local file estate but isn't referenced anywhere in the vault or the estimating skill — real progress outside the system of record. No build started; this is proof-of-concept only, per the idea-research loop's boundary against building the idea itself. Revert path: git revert this commit. | Idea-research loop run + same-session follow-up with Jesse |
| 2026-07-22 | 9 CHS heater cards (canonical migration); F-802.md; H19/H20.md; 7-1-F-1.md; H-28/H-29.md; H-30.md; B-101/B-103.md; 210-1401A/1402B.md; active-jobs.md; estimating-actuals-rollup.md; ~/.claude usadebusk-estimating SKILL.md | **[gated] Vault+estimating audit executed — the "zero routine ft/hr actuals" premise is retired.** Jesse rulings applied: (1) **F-802 USA26022 = routine, invoiced, Mode 5** (2 TriMax: one triple + one double, all 5 passes in conjunction) — with the card's Config Rollup rebuilt to canonical two-scale format, its 14,280 ft now feeds the rollup (260 elapsed / 52 per-pig, the first 2-rig routine anchor). (2) **All 9 CHS McPherson cards migrated to canonical schema** with USA25025 routine Task Durations rows (modes per Jesse: HP-0025=1, HF-0011 blank+hours-blended); rollup now carries a routine segment — 5 rows, 47–259 ft/hr per pig, **mean 99 vs the 100 benchmark**. (3) **HF-0012 pig hours 85 governs** (job-record allocation) over the ~94 receipt wall-clock. (4) **H-20 USA26038 Mode=2** (TriMax 3 double mode, per-pump allocation recorded) — normalized rate halves to 15. (5) **7-1-F-1 CND25004 = routine** (planned, moderate/light fouling); task hours pending Jesse locating the ticket breakdown. (6) USA25041 quote mislink fixed (DSP25061, not DSP26030); USA26025 "coker" → vacuum in active-jobs; card-level `metallurgy:` frontmatter removed from 9 cards; H-30's Combined rollup rows removed (latent 2× footage double-count in the rollup script). Estimating skill's "no routine baseline" sentence updated in config repo. **Open (Lane 4, needs Jesse):** whether soda ash at Suncor/Marathon is stainless passivation, chrome-alloy precaution, or site practice — cards now carry it as an explicit open question instead of an assertion. Revert path: git revert this session's vault commits + the config-repo commit. | Session: Fable-5 periodic vault+estimating audit |
| 2026-07-22 | 07-llms/claude/{command-reference.md (new), code.md}; 06-insights/2026-07-20-harness-map.md; .claude/settings.json + settings.local.json; 06-insights/{2026-07-22-pig-load-list, 2026-07-21-portfolio-revival}.md; 00-inbox/2026-07-22-deferred-portfolio-revival-full-pass.md; memory/*; leverage repo (6 commits, now remote) | **Session decisions.** (1) **Established the command-usage reference + durable-capture convention for the LLM-knowledge layer.** New `command-reference.md` — a trigger-organized lookup of built-ins/skills, doc-sourced and version-stamped — plus a standing rule in `code.md` that a verified post-cutoff Claude/Anthropic fact gets captured to `07-llms/claude/` dated+version-stamped instead of re-searched cold each session. Prompted by wrongly asserting `/goal` doesn't exist (it's a native binary built-in, not in `~/.claude`, so config-grep can't see it) and by Jesse's question of why Claude-update knowledge never compounds. (2) **Portfolio Revival Pass — approved-with-edits:** the cheap cleanup was executed — reconciled `leverage/packets/queued/`→`done/` (all 5 packets confirmed built) and corrected two stale memories (the 3 Fable-branch vault stashes are gone; packets all built) — while the full ~1-hr pass (re-verify Knowledge Loop OS C/D/F, decide the thesis v2 verdict) was **deferred** and re-filed to inbox so it keeps a trigger. (3) **Pig Load List Generator — parked** (revisit once heater cards populate the Pig Specifications table). (4) **`leverage` repo given a remote:** the decommissioned `obsidian-life` GitHub repo was renamed and repurposed 2026-07-22; all history pushed (previously local-only, an unbacked-up risk). (5) Removed the `Read(//c/Users/Jwuts/**)` whole-home-directory read hazard from `settings.local.json` and added three read-only `ccd_session_mgmt` MCP tools to the checked-in allowlist. | Session: command-reference build + portfolio cleanup close-out |
| 2026-07-21 | 02-facilities/HF-Sinclair/Artesia-NM/{USA26038-job-report.docx+.md (new), H19.md, H20.md}; 01-context/active-jobs.md | **USA26038 (H-19/H-20 emergency decoke, executed Jul 10–17 2026) closed out — job report built + crash actuals folded onto both cards.** Two durable findings the diff won't carry: (1) **H-20 as-built validated the USA25051 carry-forward** — Pass 4 was plugged pre-decoke, isolated & cleaned first, then radiant re-looped to 2-pass (temp 180's Pass 1&3 / Pass 2&4), exactly the reconfiguration the card recommended; quote still carried radiant 1-pass. (2) **H-19's looped-radiant dewatering air-lock recurred** (1600 CFM ×2 failed; forced individual-coil dewatering) — same failure as USA25051's 850 CFM, now a two-job pattern; carry-forward strengthened. Per-heater hours reconciled from the ticket breakdown (H-19 rig9/pig74/smart6/standby55; H-20 rig20/pig98/standby19), condition=crash. **Jesse decision: no financial/change-order data in the customer job report** (report is operational-only; the $202k→$319.7k delay change-order stays internal). H-19 smart-pig Job Option flipped to Elected (Quest). | Session: USA26038 close-out |
| 2026-07-19 | 00-inbox/idea-workup-to-proposal-generator.md (new) | **Decision: next-session Fable priorities revised after Jesse pushed back on the day's earlier recommendation.** Two candidates killed: competitive/win-loss analysis (Jesse: losses are purely competitor underbidding — no pattern to mine, price is the only lever) and the duration-model benchmark adjudication (assumed to be load-bearing on Jesse's actual quotes; he corrected that estimates are built in his own Excel work-up tool anchored on prior projects, not derived from the skill's benchmark, and jobs bill T&M with lump-sum mob/demob, so a duration misestimate doesn't cost margin the way assumed — demoted to documentation hygiene, not high-ROI). New candidate surfaced from that same correction: Jesse hand-types every Excel work-up into a Word proposal and converts to PDF on every bid, regardless of facility — real recurring friction and the highest-consequence manual transcription point in the pipeline. Captured as an idea seed for the nightly research loop rather than scoped directly, since its feasibility hinges on an unconfirmed fact (whether the work-up Excel has a stable structure across bids). No vault content changed. | Session: Fable priority reassessment (post close-out) |
| 2026-07-19 | 04-knowledge/_canonical-heater-card.md; templates/_heater-template.md; tools/estimating_rollup.py; 8 heater cards (Task Durations rows); ~/.claude/skills/{usadebusk-estimating,usadebusk-vault-ingest,usadebusk-fieldpm}/SKILL.md (config 8b83c66 + estimating edit) | **[gated] Coil-condition provenance added end-to-end; estimating rule changed from actuals-govern to condition-matched-actuals-govern.** Jesse's rule: "emergency" in job details = crash decoke; a crash's hours are not evidence for the next routine clean (crashed furnaces run significantly dirtier). New trailing `Condition` column (routine/crash/first/unknown + hours-blended/combined-heaters markers) on canonical card, template, ingest skill, and all 10 actuals rows. Rollup now segments ft/hr by condition and suppresses it on combined-heaters rows — this also fixed the P66 combined-job double-count flagged at close-out. Finding surfaced: all 4 ft/hr-bearing actuals are crash jobs (38–48, mean 42); ZERO routine actuals, so the 100 ft/hr benchmark currently has no routine data for or against it — a low crash rate must NOT be read as the benchmark being too high. **Lane 4 pricing change (Jesse approved in-session):** estimating skill's "heater actuals govern the estimate" now qualified to same-condition only — crash actuals price emergency scope, routine prices routine, else fall back to benchmark and say so. fieldpm /report now captures job condition + per-coil fouling at source. One open confirm: USA25041 tagged `routine` by inference (no emergency language), not by an explicit read. Revert path: git revert vault 2d057cd + this change-log commit; config 8b83c66 + the estimating-skill commit. | Session: Fable-5 final session, data-quality / condition-provenance |
| 2026-07-19 | 02-facilities/P66/Ponca-City-OK/{H-28,H-29}.md; 04-knowledge/ground-truth/{h-28,h-29}.md + p66-24012-snippets | **[gated] P66 H-28/H-29 geometry + 2024 job history verified — all 10 checklist items confirmed.** Cards filled from USADeBusk's own 24012 job reports (Jan 2024 emergency, previously missing from both cards): H-28 4 passes A–D, 42 tubes/1,865 ft, 2.90" ID + 2× 5.76" tubes; H-29 2 passes E–F, 52 tubes/2,373 ft, 2.75" ID nonstandard 0.375 wall; combined actuals 14/143/31/15 (172 productive). Jesse clarified Max Pig OD field purpose: feeds a per-project pig load list (not yet built) — general rule, don't reference everywhere; bend-limited coker coils record restriction + actuals instead. Open: H-29 metallurgy, 6"-tube pass assignment on H-28, fabricator BOM count mismatches. Revert path: git revert this commit. | Session: Fable-5 final session, P66 bonus |
| 2026-07-19 | 02-facilities/HF-Sinclair/Artesia-NM/H-2421.md; 04-knowledge/ground-truth/h-2421.md | **[gated] H-2421 verified line-by-line — all 15 Stage-4 checklist items approved.** Jesse additions: final pig size 6"/6.125" (6.125 looped-final), water source fresh condensate from frac tank. Card corrections applied on approval: heater type cabin/box → vertical-cylindrical; radiant 16 → 14 tubes/circuit; metallurgy 347 SS; Sch 80/0.432 wall; jumpered C↔D loop routing; Job Options Elected/Declined. RC1B mirror-geometry assumption accepted as working truth (drawing not located). Revert path: git revert this commit. | Session: Fable-5 final session, Stage 4 |
| 2026-07-19 | ~/.claude/regression/* (config 7d5eebd); 02-facilities/HF-Sinclair/Artesia-NM/H-2501.md (4e8ca31); 04-knowledge/ground-truth/h-2421.md + snippets (a07976f); 00-inbox/2026-07-19-fable5-closeout-handoff.md | **[gated] Fable-5 close-out program executed per Jesse's staged brief.** Stage 1: 6-fixture frozen regression suite for post-window model diffing. Stage 2: H-2501 source Excel recovered — per-pass rollup confirmed (1,027.8 ft/pass, 6,166.8 total, not looped), radiant 4"-tube flow position genuinely unstated (stays open). Stage 3: H-2421 ground truth extracted from GA drawings + Excel + Navajo nozzle sheet — 2×(14 conv + 14 rad) 347 SS circuits, C↔D jumper loop, VC firebox, BOM-vs-Excel length conflict reconciled as cut-vs-effective; card untouched pending Stage 4 line-by-line verification. Stage 5 handoff note carries the five post-window work orders + P66 H-28/H-29 pointers. Revert path: git revert 4e8ca31 / a07976f (vault), 7d5eebd (config). | Session: Fable-5 final session, perishability-ordered capture |
| 2026-07-19 | 04-knowledge/concepts/{rfq-intake-protocol (new), quote-lifecycle}.md; 04-knowledge/_facility-template.md; 04-knowledge/pricing/_cost-model.md; archive/Rate-Reference_2026-07-19_superseded-facility-grain.md; ~/.claude/skills/usadebusk-estimating/SKILL.md; 06-insights/2026-07-19-rate-model-grain-review.md (closed) | **[gated] Documented RFQ intake end-to-end; corrected the rate model's grain from facility to contract.** New `rfq-intake-protocol.md` covers document landing through submission, handing off to `quote-lifecycle` at award. Core finding, from Jesse: rates belong to a *contract*, not a facility — one site can carry several concurrent contracts (different departments, different terms) at different rates, so `_facility.md` cannot be the rate source; reversed an earlier in-session recommendation to backfill rates there. Added `contract-type`/`rate-basis`/`billing-basis` to quote frontmatter (Jesse's call over the originally proposed `department` field, since terms drive estimating behavior and department only correlates). Rejected building a multi-year-contract note type — none currently active on Jesse's accounts; revisit when one lands. Archived `pricing/Rate Reference.md` (wrong grain, duplicated the skill's baseline table, every cell blank); rescued its stand-by-cause table into the skill and its 2026-07-06 sourcing findings into the protocol note. Populated `_cost-model.md` from Jesse's six-workup analysis, then downgraded it after he clarified the cost figures are known approximations for general perception, not measured cost — added an explicit legitimate/illegitimate-use warning rather than let it be read as a floor. Corrected two stale facts found stale mid-session: Google Drive is retired (not the documented two-tree estate; source files live in per-facility OneDrive folders, indexed by path from the quote note), and third-party markup baseline moved from a stale 10% to 15% typical. Revert path: `git revert` the session's commits on both repos (vault: 9c1ed46 through 9270a52; claude-config: ede7cfd through 3489e1b); un-archive Rate Reference with `git mv`. | Session: RFQ intake protocol + rate model review |
| 2026-07-06 | 04-knowledge/knowledge-system-governance.md; ~/.claude/CLAUDE.md (claude-config repo, commit d83b29c) | **[gated] Delegated vault commit/push to agent judgment, superseding the 2026-06 gated git policy.** Jesse decided the cross-session friction of gating every commit outweighed the risk the original "silent-git-mutation hole" rule guarded against. Hard bans remain: never force-push, never rewrite/amend history, never delete branches; Lane 4 content (domain truth, pricing/safety/SOP values, irreversibles) still requires an explicit in-session ask before the change itself, independent of how freely it's then committed. Revert path: git revert commit 391ac5a (vault) + d83b29c (claude-config) to restore the gated rule. | Session: vault content-improvement (Rate Reference / actuals ingestion) |
| 2026-07-06 | 02-facilities/Syncrude/Fort-McMurray-AB/7-2-F-1.md -> 7-1-F-1.md; _facility.md; two 06-insights wikilinks | **[gated] Corrected Syncrude heater identity: 7-2 F-1 is wrong, 7-1 F-1 is correct.** Jesse confirmed in-session after a SharePoint pull raised doubt about the card's source attribution. Also clarified CND24002 (2024) and CND25004 (2025) are two separate real jobs on this same heater, not a mislabeling as initially suspected — CND25004 has full field data, CND24002's is not yet located. Revert path: git revert commit 42bdb31 (and the earlier rename in 391ac5a) if the identity call needs to be undone. | Session: vault content-improvement (Rate Reference / actuals ingestion) |
| 2026-07-06 | 04-knowledge/knowledge-system-governance.md; 02-facilities/_directory.md; ~59 empty directories removed | **[gated] Facility-data ceremony dropped to Lane 1 in full; retired the "known sites, no data yet" scaffold concept.** Discussion surfaced that `02-facilities/` was designed for a compounding-repeat-heater workflow Jesse doesn't have — most bids are first-time heaters, nothing downstream currently reads a populated card (no skill references a specific instance, only the schema), and Jesse's real practice is to infer gaps and use judgment rather than chase precision, which never hurts his workflow. Heater-card/facility content (facts, corrections, discrepancy resolution) no longer needs a verify-gate, contradiction note, or Lane 4 escalation — only an active pending-bid/customer-document use warrants double-checking. Intended future use: repeat-project reference and periodic mining for skill improvement, both occasional. Also removed ~59 empty pre-scaffolded facility directories (BASF, BP, Buckeye, etc. — sites with no data) and the corresponding "known sites" list in `_directory.md`; they were leftover from an earlier vault concept and served no purpose. Revert path: git revert this commit; the empty directories carried no git-tracked content so nothing is lost. | Session: vault content-improvement (facility-data ceremony review) |
| 2026-04-25 | CLAUDE.md | Created — agent entry point, output preferences, SOP formatting standard, Insight Loop | Session: vault build execution |
| 2026-04-25 | 01-context/* | Created output-preferences.md, workflow-map.md, estimating-approach.md, active-jobs.md | Session: vault build execution |
| 2026-04-25 | 04-knowledge/sops/sop-formatting-standard.md | Created — SOP formatting standard derived from 3 completed production SOPs | Session: vault build execution |
| 2026-04-25 | 05-projects/* | Created system-prompt stubs for all 4 Claude Projects | Session: vault build execution |
| 2026-04-25 | .claudeignore | Created | Session: vault build execution |
| 2026-05-21 | skills/usadebusk-vault-ingest/SKILL.md | Updated skill: CND job routing, collision suffix logic, revised-proposal overwrite, field-report merge, DSP# correction table, auto-subfolder creation, client list 28→32 (Formosa, PBF, Suncor, Westlake added) | Session: dry-run + ingest |
| 2026-05-21 | 05-projects/sales-proposals/* | 18 proposals ingested; 12 new client subfolders created (Cenovus, Valero, HF-Sinclair, PBF, Formosa, Flint-Hills, P66, CITGO, Westlake, Suncor, ONEOK, Hunt) | Session: ingest pipeline |
| 2026-05-21 | 03-jobs/Marathon/USA25058.md | Field reports merged: 37-B-1, 41-B-1, 43-B-1 LEP (Oct 2025) appended under ## Field Reports | Session: ingest pipeline |
| 2026-05-21 | 03-jobs/Valero/USA26002.md | Field report merged: HC-H-002 (Jan 2026) appended under ## Field Reports | Session: ingest pipeline |
| 2026-05-21 | 00-inbox/CND25004.md | Syncrude Fort McMurray job report held in inbox — client not scaffolded | Session: ingest pipeline |
| 2026-06-06 | 00-inbox/2026-06-06-refineries-migration.md | Session log created — Desktop\Refineries migration (73 files, 30 folders, 24 client sites) | Session: file migration |
| 2026-06-06 | 06-insights/insights-log.md | Entry added — Desktop\Refineries migration complete | Session: file migration |
| 2026-05-21 | skills/usadebusk-vault-ingest/SKILL.md | v3: DSP dissolution into per-heater cards, document type detection order, tube geometry conflict detection, truck-level hour splitting, city-state normalization, heater card template | Session: adversarial audit |
| 2026-05-21 | 02-facilities/ExxonMobil/Baytown-TX/PS8_F802-CrudeHeater/SOP-DCK-F802-001.md | Created — F-802 execution SOP ingested as companion doc to heater card | Session: ingest |
| 2026-05-21 | 03-jobs/ExxonMobil/USA26022.md | Change order / overage detail merged under ## Field Reports | Session: ingest |
| 2026-05-21 | 02-facilities/ExxonMobil/Baytown-TX/PS8_F802-CrudeHeater/F-802.md | ## Field Notes added — USA26022 actuals, standby causes, filter press / crane lift flags for future bids | Session: ingest |
| 2026-05-21 | skills/usadebusk-vault-ingest/SKILL.md | v4: markitdown[docx] fix, SOP companion routing, client facility doc routing (facility level only), expanded doc_type values | Session: skill refinement |
| 2026-06-20 | 02-facilities/Marathon/Garyville-LA/Unit-210_1403A-Vac/210-1403A.md | Reshaped to canonical Heater Card Schema (engineering geometry encoding + config rollup + quarantined Job Options); metallurgy set Stainless, config Individual Passes; original archived to archive/ | Session: default-assumption audit |
| 2026-06-20 | 02-facilities/ExxonMobil/Baytown-TX/PS8_F802-CrudeHeater/F-802.md | Reshaped to canonical Heater Card Schema; filtration/smart-pigging moved to quarantined Job Options; config set Looped at Radiant outlets; original archived to archive/ | Session: default-assumption audit |
| 2026-06-26 | 00-inbox/CND25004.md -> 03-jobs/Syncrude/CND25004.md | Routed confirmed Syncrude CND25004 report; created Syncrude Fort McMurray facility scaffold; preserved CND25002 body-field warning | Codex knowledge-system pilot |
| 2026-06-26 | 03-jobs/Syncrude/CND25004.md -> 02-facilities/Syncrude/Fort-McMurray-AB/7-2-F-1.md | Created draft source-derived heater scaffold and candidate canonical update review; no facts promoted to reviewed status | Codex knowledge-system pilot |
| 2026-06-26 | 04-knowledge/vault-agent-loop-spec.md | Created manual Vault Review Loop spec with approval boundaries and stop conditions | Codex knowledge-system pilot |
| 2026-06-27 | 00-inbox/Final Heater Card Output — H-2421.md -> 02-facilities/HF-Sinclair/Artesia-NM/H-2421.md | Created approved draft/source-derived H-2421 heater card with uncertainty preserved; source note left in inbox | Codex Vault Review Loop |
| 2026-06-27 | 04-knowledge/vault-agent-loop-spec.md | Updated Vault Review Loop trigger to Monday and Friday mornings at 8:00 AM America/Chicago; retained manual on-demand command | Codex schedule update |
| 2026-06-27 | .gitignore; 04-knowledge/vault-source-of-truth.md; 04-knowledge/vault-agent-loop-spec.md | Set canonical vault path to `C:\Users\Jwuts\obsidian-work`; added conservative Git ignore rules for Obsidian Sync + private Git setup | Vault migration to Obsidian Sync + private Git |
| 2026-06-29 | 03-jobs/* and 05-projects/* (deleted); 02-facilities/* overviews; CLAUDE.md; AGENTS.md; INDEX.md; 50-dashboards/knowledge-review-dashboard.md; 04-knowledge/_canonical-heater-card.md, concepts/quote-lifecycle.md, vault-agent-loop-spec.md; templates/_facility-template.md | Decommissioned 03-jobs and 05-projects; converted [[USA]]/[[CND]] wikilinks to plain text; preserved 3 orphan DSPs (no source_file) to 00-inbox/preserved-dsps/; cleaned stale folder references (facility Job History dataview → static pointer, load-on-demand lists, dashboard FROM clauses, exemplar folder-structure note, quote-lifecycle workflow) | Vault consolidation — jobs/DSPs re-ingest later from prepared sources |
| 2026-06-30 | 04-knowledge/vault-idea-loop-spec.md (new); templates/_idea-seed-template.md (new); 01-context/system-workflow-reference.md (new); 04-knowledge/knowledge-system-governance.md; 04-knowledge/vault-capture-loop-spec.md; 04-knowledge/vault-agent-loop-spec.md | Added third loop — Vault Idea Research Loop — bounded nightly research on queued idea-seeds, propose-only, never decides or builds; cross-linked into governance and the other two specs; deployed as scheduled task `vault-idea-research-loop` (nightly 2am local); added personal workflow reference doc (three-loop overview + skill trigger map + manual/idea-seed ingestion flow) | Session: Idea Research Loop design + deployment |
| 2026-06-30 | 07-llms/chatgpt-copilot-workflow-architecture.md (new); 07-llms/chatgpt/overview.md; 07-llms/copilot/overview.md; .gitignore; templates/* (newly tracked) | Ingested old ChatGPT deep-research report on ChatGPT Project + Microsoft 365 Copilot workflow architecture via the vault's own source-note workflow (draft/secondary, not tenant-verified); cross-linked into existing chatgpt/copilot overview notes with new callouts (Researcher/Analyst governance scope, Restricted SharePoint Search caveat, Skills beta caveat); un-ignored templates/ — was gitignored with no other backup since OneDrive retired | Session: manual ingestion workflow test |
| 2026-07-01 | 06-insights/2026-07-01-idea-research-skill-update-feedback-loop.md (new); 00-inbox/idea-skill-update-feedback-loop.md | First run of the Vault Idea Research Loop — researched "skill-update feedback loop" idea-seed; found internal proof of the problem (skill-naming-cleanup incident, same-night inbox note) and external prior art (CAI Drift Check, Context Drift Detector, Agent Instruction Audit pattern, claude-memory-health) covering staleness detection within a single tree, but no tool solving the cross-repo vault→skill propagation the idea actually asks for; recommended a bounded one-shot trial of an existing staleness-audit skill before any custom build; idea-seed status flipped to researched | Vault Idea Research Loop |
| 2026-07-05 | 06-insights/2026-06-27-cnd25004-swab-size-contradiction.md; 02-facilities/Syncrude/Fort-McMurray-AB/7-2-F-1.md | Jesse confirmed CND25004 dewater swab size as 10.5 in (receipt 10153 + pig-utilization table). Corrected 7-2-F-1.md, which had self-asserted the opposite (10 in) resolution without going through the review | Session: vault decision backlog clearance |
| 2026-07-05 | 00-inbox/prefs-signal-log.md -> 07-llms/claude/prefs-signal-log.md; 06-insights/2026-06-26-prefs-signal-log-routing-review.md | Jesse approved moving prefs-signal-log out of inbox; routed into the vault's new LLM-knowledge layer (07-llms/) rather than a separate vault, since that layer didn't exist when the review was written | Session: vault decision backlog clearance |
| 2026-07-05 | 00-inbox/2026-06-06-refineries-migration.md -> archive/2026-06-06-refineries-migration.md; 06-insights/2026-06-26-refineries-migration-routing-review.md | Jesse approved archiving the completed, already-summarized migration session log | Session: vault decision backlog clearance |
| 2026-07-05 | 06-insights/2026-07-01-idea-research-skill-update-feedback-loop.md; 00-inbox/idea-skill-update-feedback-loop.md | Jesse approved the bounded one-shot staleness-audit investigation; marked blocked on environment — `~/.claude/skills/` isn't populated in this session (skills live in the separate claude-config repo, out of scope) | Session: vault decision backlog clearance |
| 2026-07-05 | 06-insights/2026-06-26-knowledge-system-pilot-review.md; 06-insights/2026-06-26-read-only-inventory-routing-review.md | Closed both as superseded/moot with Jesse's approval — CND25004 pilot plan fully executed via later reviews; read-only-inventory source note no longer exists in the vault | Session: vault decision backlog clearance |
| 2026-07-05 | 01-context/system-workflow-reference.md | Marked usadebusk-fieldpm dormant (reactivates on job mobilization); corrected skills location to `~/.claude/skills/`, dropped stale claude-config-repo framing | Session: vault decision backlog clearance |
| 2026-07-05 | 00-inbox/2026-06-30-skill-naming-cleanup.md | Jesse decided: keep the `claude-obsidian` plugin ecosystem for now. Noted that 3 of its functions (save, wiki-ingest manifest, wiki-lint) already have vault-native equivalents in the three-loop system; the naming-collision risk that prompted the note remains unresolved. Item 1 (stale vault-path fix in usadebusk-vault-ingest) still open, blocked on claude-config repo access | Session: vault decision backlog clearance |
| 2026-07-05 | 01-context/workflow-map.md | Closed the "Sync solution" open architecture decision — git is the sole backup in practice (OneDrive/Obsidian Sync retired 2026-06-30), no longer an open choice. One decision remains: index layer (Bases vs. index.md) | Session: vault decision backlog clearance |
| 2026-07-05 | 01-context/workflow-map.md | Removed "How to handle Neuron" from Open Architecture Decisions — no documented rationale existed anywhere in the vault for why it was flagged; Jesse confirmed dropping it | Session: vault decision backlog clearance |
| 2026-07-05 | 01-context/workflow-map.md | Closed the "Index layer" decision — static `index.md`/MOC files, not Bases. Rationale: matches the vault's existing precedent (2026-06-29 converted a facility's dataview query to a static pointer) and its manual-approval, nothing-auto-updates philosophy; Bases would introduce the first self-updating content in the vault. Open Architecture Decisions section removed entirely — nothing left in it | Session: vault decision backlog clearance |
| 2026-07-05 | 00-inbox/*.pdf (6 files) -> 04-knowledge/equipment/spec-sheets/; 04-knowledge/equipment/equipment-library.md | Ingested the six unrouted equipment reference PDFs sitting in inbox since the vault build: Cummins QSL9 engine data, TriMax/support trailer dimensions, Filter Press #1 dims, Waterous CMU pump specs/curves, pig launcher dimension table, Kemper Fig 200 hammer union catalog. Extracted facts merged into equipment-library.md's existing TriMax, Filter Press, Pig Launcher, and Hoses & Connections sections; added a Source Documents pointer table | Session: vault decision backlog clearance |
| 2026-07-05 | 02-facilities/HF-Sinclair/Artesia-NM/H-2501.md (new); 02-facilities/{ExxonMobil/Baytown-TX,Marathon/Garyville-LA}/DSP2{5084,6039,6058}.md; 6 heater-card Job History rows; 06-insights/2026-07-05-source-h-{2421,2501}-heater-card-output.md (new) | Independent same-day inbox triage (a local Claude Code session, unaware of the "vault decision backlog clearance" session above): created a draft H-2501 heater card and flagged that its FINAL-labeled source note was actually truncated mid radiant-flow-path confirmation; relocated 3 orphan DSP quotes into their facility folders as companion files with pending Job History rows; archived processed H-2421/H-2501 source notes | Session: inbox triage (local) |
| 2026-07-05 | Merge of `claude/obsidian-work-sync-location-h87cba` into main | Two unmerged same-day sessions (a Dispatch run and a local Claude Code session) had independently triaged overlapping inbox items from the same starting commit with diverging decisions — discovered via `git fetch` when the local session's user asked whether Dispatch/vault/chat work might be misaligned. Reconciled per Jesse's explicit "take theirs" call on every direct conflict (prefs-signal-log destination, equipment PDF folder name/content, claude-obsidian keep decision, refineries-migration destination), except the one item verified against the live local filesystem (usadebusk-vault-ingest stale-path fix confirmed done, which the Dispatch session's sandboxed environment couldn't check). All non-conflicting work from both sessions kept: CND25004 swab-size correction, two stale-review closures, workflow-map/system-workflow-reference cleanup, and the H-2501/DSP work above | Session: inbox triage (local) |
| 2026-07-05 | tools/vault_lint.py, tools/vault_health.py, tools/fixtures/*, 50-dashboards/{decision-queue,health}.md, 02-facilities/ (49 empty scaffolds -> archive/02-facilities-scaffolds/), 02-facilities/_directory.md, change-log.md, 04-knowledge/vault-source-of-truth.md, CLAUDE.md, AGENTS.md, 01-context/output-preferences.md | **Decision: adopted the Knowledge Loop OS MVP (proposal 2026-07-02, section 8).** Built the scripts-over-attention floor: `vault_lint.py` (6 rules + fixtures + self-test; errors = secrets/confidence-conflict only, rest warnings) and `vault_health.py` -> `health.md`; created the metered `decision-queue.md` (0 open — the proposal's 12-item backlog was already drained on main by per-note closures, not the branch's superseded verify-on-use sweep); narrowed this change-log to decisions-only; replaced the source-of-truth hardcoded-Windows-path guardrail with a repo-root marker check (`git rev-parse --show-toplevel` + CLAUDE.md marker); resolved the three-way SOP-formatting-authority contradiction (canonical home = `04-knowledge/sops/sop-formatting-standard.md`; CLAUDE.md/output-preferences/AGENTS.md now point there); archived 49 empty facility scaffolds under create-on-first-fact (verified each still contained zero facts before moving). Fixed 3 confidence:high-on-inferred lint errors and one stale `status: inbox` frontmatter. **Deferred, each its own gated session:** governance-doc consolidation into vault-operations.md, 90-sources provenance layer, AGENTS.md auto-generation, methods library, review-loop decision-packet rewrite. **Revert path:** `git revert` this session's commits; archived scaffolds recover via `git mv`. | Session: knowledge-loop-os MVP adoption |
| 2026-07-05 | 06-insights/2026-06-30-skill-naming-cleanup.md; 01-context/workflow-map.md | **Decision reversal: claude-obsidian plugin dropped, not kept.** The vault's earlier "keep for now" call (commit `17d8798`) is superseded by the parallel `claude-config` hygiene arc, which disabled the plugin and removed its marketplace (`claude-config` commit `e343e5f`, 2026-07-05); Local REST API MCP is the eventual replacement (not yet configured). Also cleared three resolved rows from workflow-map's Paused/Deferred table: Git/Obsidian version control (git set up on desktop "Linda"; Obsidian Sync + OneDrive both retired, git-only), iPhone Shortcut → inbox, and Gemini CLI Adapter Layer. | Session: cross-surface reconciliation (vault ↔ claude-config) |
| 2026-07-05 | 04-knowledge/knowledge-system-governance.md | **[gated] Decision: adopted the four-lane Delegated Autonomy Policy (knowledge-loop-os Session A).** Chose the middle path — keep the 6 governance docs separate (no full vault-operations.md consolidation), add the four-lane policy (auto-act / auto-experiment / propose-with-default / human-gated) + commit-prefix convention `[auto]`/`[exp]`/`[default]`/`[gated]`. Reconciled the branch's original policy to current reality: git commit/push stays gated and never auto (respects the claude-config permission hardening); Lane 4 unattended items route to the decision-queue or a card Open Flag (the branch's version predated the queue); `90-sources/` kept as a forward reference (Session B). Unlocks per-loop heartbeats (Session E). Revert path: `git revert` this commit. | Session: knowledge-loop-os Session A |
| 2026-07-05 | tools/vault_health.py; 04-knowledge/vault-capture-loop-spec.md; 04-knowledge/vault-idea-loop-spec.md; CLAUDE.md | **[gated] Added loop heartbeats (knowledge-loop-os Session E).** `vault_health.py` now reads each scheduled loop's commit-subject prefix (`vault-capture:` weekly, `vault-idea-research:` nightly) as a heartbeat and flags it overdue in `health.md` when older than 2x cadence or never fired; the on-demand review loop is not tracked. Documented the heartbeat contract in both loop specs; added a startup line to CLAUDE.md to surface FAIL rows before other work. First run already flags real signal: capture loop never fired, idea loop 4 days stale. gen_agents.py and GitHub Actions lint (rest of proposal Session 5/E) left as optional follow-ups. Revert path: `git revert`. | Session: knowledge-loop-os Session E |
| 2026-07-05 | mcp scheduled-tasks (vault-capture-loop, re-registered); tools/vault_health.py; 04-knowledge/vault-idea-loop-spec.md | **[gated] Re-enabled the capture loop; kept the idea loop on-demand.** Investigation (via prior session transcripts) found both loops were deployed + running 2026-06-30→07-02, then de-registered as collateral of the reversed kernel-consolidation TODO ("delete the two Windows scheduled tasks"); their SKILL.md definitions survived on disk. Re-registered `vault-capture-loop` as a weekly scheduled task (Monday 08:00 local, `0 8 * * 1`) — it is non-proposal-generating auto-filing, i.e. Jesse's drop-and-forget preference, so scheduling it is a clean win. Left `vault-idea-research-loop` on-demand (manual): it is the proposal-generator that spends decision attention, and the queue is unproven; schedule it only once the queue demonstrably drains. Updated vault_health.py heartbeat to distinguish scheduled/pending/manual (a never-fired scheduled loop reads "pending", not red); reconciled the idea-loop spec Trigger to on-demand. | Session: knowledge-loop-os Session E (scheduling) |
| 2026-07-05 | 04-knowledge/vault-capture-loop-spec.md; 04-knowledge/vault-idea-loop-spec.md (+ runner prompts in ~/.claude/scheduled-tasks/) | **[gated] Fixed two self-obsolete steps in both loop specs** — surfaced by the idea-research test run on self-obsolescence-detection (fittingly). (1) Dropped the "append run summary to change-log.md" step, superseded by the 2026-07-05 decisions-only narrowing rule; the commit message + git log are the run record. (2) Added a pre-commit lint gate — run `vault_lint.py`, must be 0 errors before committing. Propagated the same fix to the live `vault-capture-loop` scheduled-task prompt and the `vault-idea-research-loop` runbook. | Session: knowledge-loop-os Session E (loop-spec fixes) |
| 2026-07-07 | ~/.claude/skills/adversarial-review/SKILL.md; usadebusk-fieldpm/SKILL.md; usadebusk-vault-ingest/SKILL.md; idea-triage/SKILL.md (claude-config repo, commits acbcb1a, 6a82ab7) | **Decision: skipped building a `/reflect` meta-skill** — a Claude Chat research pass proposed it (session-scan → propose CLAUDE.md/skill edits, gated on approval), but it would substantially duplicate the already-built Skill-Drift loop (monthly scan, `drift/YYYY-MM` proposal branch, never auto-merged), which hadn't had its first run yet at audit time. Built the two non-duplicative, low-cost items instead: epistemic labeling (VERIFIED vs. ASSERTED, with explicit "agent agreement ≠ verification") added to `adversarial-review`'s Finder/Adversary/Referee prompts and report output; `disable-model-invocation: true` added to the three command-only skills so they can't semantic-auto-fire. Also caught and corrected a fabricated statistic in the source research (a specific "~80%/~5% promotion rate" attributed to a community repo that doesn't actually contain it) before it could be cited as design authority. Context-packet-builder and format-enforcement-split proposals deferred — see `00-inbox/idea-context-packet-builder-skill.md` and `00-inbox/2026-07-07-harness-audit-open-items.md`. | Session: skill design research audit |
| 2026-07-11 | 02-facilities/HF-Sinclair/Artesia-NM/USA26038-job-sheet.{md,html,pdf} (new); H19.md, H20.md (new); _facility.md; 01-context/active-jobs.md; 00-inbox/2026-07-11-* (3 new) | **Introduced the `job-sheet` document type** — a static, bid-win, crew-facing printable (project details, rig/shift crew assignment, per-heater work-up billing tables for service receipts, coil data, carry-forward notes) for USA26038 (HF Sinclair Navajo, H19/H20 emergency decoke); committed both the vault-native markdown and the printable HTML source + rendered PDF into the facility folder. Created H19/H20 heater cards from the USA25051 execution reports (`source:`/`verified: never` provenance). **Decisions:** equipment is not pre-assigned to a heater (field decision); Dacorey Slater bills as Project Manager on the H-20 work-up (reversed an earlier Day-Supervisor call). Filed 3 inbox captures — job-sheet-type formalization (template + render tooling), the three-document model + work-up billing math, and the USA26038 quoted-12-vs-sent-10 fact for the eventual job report. | Session: USA26038 job sheet |
| 2026-07-12 | 06-insights/2026-07-12-skill-drift-review.md (new); 04-knowledge/equipment/equipment-library.md; 04-knowledge/concepts/{field-operations,estimating-pricing}.md; claude-config branches drift/2026-07 + upgrade/2026-07 (pushed, unmerged) | **[gated] Three skill-drift open questions answered during the first Skill-Drift Loop run:** (1) normal operating pressure is **150–300 PSI** (previously three conflicting ranges across usadebusk-equipment, usadebusk-sop, and equipment-library); (2) **labor bills hourly**, not as a 12-hr day rate (the Pricing Structure label was stale); (3) **9 Chrome / metallurgy is a non-issue** — metallurgy only matters when the customer requires passivation, which is customer-performed, so the skills' carbon/stainless framing stands. Applied to the drift/2026-07 proposal branch and the vault's stale restatements; remaining findings await merge/decision per the review note. | Session: skill-drift first run (remote) |
| 2026-07-18 | 06-insights/2026-07-12-skill-drift-review.md; 04-knowledge/equipment/equipment-library.md; claude-config branches drift/2026-07 + upgrade/2026-07 (pushed, unmerged) | **[gated] Remaining four skill-drift open questions answered — all seven now closed.** (4) Passivation numeric extras: **do not promote** to any skill — customer scope (USADeBusk doesn't supply soda ash or mix it), minor detail relevant only on specific projects. Added a behavioral guardrail to `usadebusk-core`/`usadebusk-sop` (U4) after Jesse noted that introducing passivation detail has repeatedly derailed sessions into unprompted deep-dives. (5) Rig-in/rig-out durations: no benchmark value changed — **heater-specific actuals govern over the generic default** when they exist (quoted duration is inherently uncertain; historical actuals are tested/verified); clarified in the Duration Model. (6) Filter press pumping rate is **pigging-only** — smart pigging bills at the non-pumping/stand-by rate, correcting `usadebusk-ops` and `usadebusk-estimating`. (7) **FHR = Flint Hills Resources**, the same client as "Flint-Hills" (was listed as two); **HR pig type = "Hell Raiser,"** not "High-Recovery" (fixed in usadebusk-equipment, usadebusk-fieldpm, and the vault); TC/PPS/Pin clarified as the same pig — PPS is a vendor name, not "Pin Poly Soft." | Session: skill-drift follow-up (desktop) |
| 2026-07-18 | 06-insights/2026-07-12-skill-drift-review.md; 04-knowledge/{equipment/equipment-library,concepts/process-flow,concepts/field-operations,_canonical-heater-card}.md; templates/_heater-template.md; 02-facilities/ExxonMobil/Baytown-TX/F-802.md; 02-facilities/HF-Sinclair/Artesia-NM/{H19,H20}.md | **[gated] Applied all 8 vault-side skill-drift findings (V1–V8; Jesse: "Knock out the V1-V8").** Rewrote `equipment-library.md`'s TriMax description from a pre-Triple single-pump layout to the 3-assembly Triple architecture (V1); deleted a false cross-unit pigging-direction constraint (V2); made 2× filtration conditional instead of unconditional-and-self-contradictory (V3); aligned looped-circuit transit-time language to "~6–30 min, not a fixed range" in both equipment-library.md and process-flow.md (V4); relabeled process-flow.md's Rig-In/Rig-Out from "12-hour fixed event" to "default 6 hrs," moving the 12-hr simultaneous-shift framing to Mob/Demob where it belongs (V5); fixed two stale billing rows in field-operations.md — third-party markup and Plant Down Time stand-by rate (V6); added the filtration-election gate to process-flow.md's Rig-In/Pigging steps and diagram (V7); synced the heater-card Field Notes schema (exemplar + template) to the ingest-skill's derived shape (`Pigs Ran` / `Obstacles` / `Facility Procedures` / `Per-rig split`), then swept and fixed all three live cards found carrying the old duplicated-duration-numbers-as-prose pattern (F-802, H19, H20) (V8). | Session: skill-drift V1-V8 cleanup (desktop) |
| 2026-07-19 | tools/vault_health.py; 50-dashboards/.loop-runs.json (new, gitignored); .gitignore; 04-knowledge/vault-{capture,idea,skill-drift,consolidation}-loop-spec.md; 01-context/{workflow-map,system-workflow-reference}.md; ~/.claude/scheduled-tasks/*/SKILL.md | **[gated] Loop monitoring made failure-visible via a run ledger.** Harness audit found the heartbeat system blind to its own failure modes: the capture loop fired 07-13 and vanished (no commit, dashboard green), the consolidation loop fired 07-18 and vanished, and the idea loop sat disabled while two loaded docs called it nightly-scheduled. Fix: every loop now writes `50-dashboards/.loop-runs.json` (local, gitignored) as its first and last action; `vault_health.py` reads it alongside git heartbeats and distinguishes `FAIL: started, never finished` (crash) from `FAIL: scheduler silent` (disabled/dead — catches temporary disables like the idea loop's) from a healthy no-op. Manual loop passes must use the exact heartbeat commit prefix (documented in system-workflow-reference). Also rescoped workflow-map.md to mobilized-jobs-only — project status lives in agent auto-memory, which the map had drifted stale against. Idea loop re-enabled (nightly 02:05). Revert path: `git revert` both repos' commits; delete `.loop-runs.json`. | Session: harness goal-alignment audit |
| 2026-07-19 | ~/.claude/settings.json (04a9b43); obsidian-work/.claude/settings.json (new); 01-context/output-preferences.md | **[gated] Two harness judgment calls settled.** (1) **Git mutation authority scoped to the vault.** Global settings auto-approved `git add/commit/push/pull` in *every* repo Claude Code touched — far broader than the delegated-autonomy policy, which only ever covered vault Lane 1-3 work. Claude Code permission patterns match on the command string, not the working directory, so "narrow to the vault" meant relocating the four verbs into a project-scoped `obsidian-work/.claude/settings.json` (tracked, travels with the repo). Read-only git inspection stays global. Other repos now prompt for git mutations — deliberate. (2) **Ask-vs-proceed precedence.** Global CLAUDE.md's don't-confirm-routine-work rule and output-preferences' Implementation-mode verify-first rule had been resolved by vibe each session; added an *Ask vs. proceed* section stating they resolve by **scope clarity**, with action reversibility as an independent axis. Revert path: `git revert` each commit; restore the four verbs to global settings. | Session: harness goal-alignment audit |
| 2026-07-19 | mcp scheduled-tasks (vault-skill-drift-loop, disabled); tools/vault_health.py; 04-knowledge/vault-skill-drift-loop-spec.md; 01-context/system-workflow-reference.md | **[gated] Decision: the Skill-Drift Loop becomes on-demand; its monthly schedule is disabled.** Jesse asked whether the nightly/monthly loops would stall on approval prompts when running unattended. Audit of all four scheduled tasks against the live permission rules: capture, idea-research, and consolidation are covered by the vault's project + local settings (folder-scoped Write/Edit, the `py -3 tools/*.py` commands, and vault-scoped git add/commit/push). Skill-drift is not, and cannot cleanly be: it is the only loop that writes **outside** the vault — `drift/YYYY-MM` branch creation, `skills/**` edits, and commit/push in the config repo — which collides head-on with the same-day decision scoping git mutation authority to the vault. Two structural findings drove the call: (1) permission patterns match literal command-string prefixes, so the spec's `git -C ~/.claude …` form is matched by **neither** the global allow rules nor the global deny rules (force-push/amend/reset/branch-delete), meaning any `-C`-form allow would need its denies rewritten in the same form or the guardrail silently would not apply; (2) permission scope is path-based and cannot be branch-scoped, so granting the loop `Edit(skills/**)` would equally let any interactive session edit Lane 4 domain truth (pricing, safety, SOP values) on `main` with no prompt. Rejected alternatives: a narrow carve-out in global settings (erodes both guardrails), and a branch-aware PreToolUse hook (precise, but a second piece of hook code to maintain and debug at 3am). Chose the zero-new-permission path — run it manually, one deliberate invocation a month. Follow-through: removed from `LOOP_HEARTBEATS` in vault_health.py so a disabled scheduler no longer reports a false `FAIL: scheduler silent`; spec Trigger rewritten with the reasoning; workflow reference now reads three scheduled + two on-demand. There is no per-scheduled-task permission mode — `permissions.defaultMode` is session-wide, so loosening it would have loosened interactive sessions too. Revert path: `git revert` this commit and re-enable the task via `mcp__scheduled-tasks`. | Session: scheduled-loop permission audit |
| 2026-07-19 | 00-inbox/idea-{hardened-extraction-corpus-vision-arm,fallback-regression-battery,portfolio-revival-pass}.md (new) | **[gated] Decision: the final Fable-5 access window goes to perishable measurements and object-level domain data, not to new meta-systems.** A three-investigator study (web research on demonstrated practice, a first-principles derivation barred from the candidate list, and a skeptic) plus a fresh-context adversarial review evaluated five proposed systems — Personal Leverage OS, Universal Cold-Review Engine, Portfolio Recovery Engine, Proposal Compiler, Research/Opportunity Workbench. **All five rejected as builds**, on verified redundancy: the vault's five loops plus INDEX/health already are the leverage OS; the `adversarial-review` skill already is the cold-review engine; `usadebusk-estimating` already is the proposal compiler; the idea-research loop already is the research workbench; and the parked-work inventory is short enough to read directly, making a recovery engine an engine to rediscover a known list. Decisive evidence against building system N+1: the `leverage` repo — the same move made in a previous burst — has been dormant since 2026-07-02 with its metabolism and decision-log layers never having fired once. **Organizing principle adopted: perishability.** Frontier *measurements* (reference outputs on skills and corpora, capability diffs against the fallback stack) are unobtainable after the window; nearly everything else can be built later with Opus/Codex or bought at ~2x Opus API pricing, so the window buys only what expires. Window allocation: fallback-regression fixtures first, then H-2421 ground-truth drawing extraction (live money via the submitted DSP26080), then a Fable-as-adversary run on the estimating skill. Explicitly deferred to cheaper models post-window: the heater-card physical-invariant linter, cross-reference checker rules, and the duration-model adjudication. Fine-tuning/distillation corpora ruled out entirely (Fable ships anti-distillation classifiers). Three un-homed ideas captured as seeds for the nightly idea loop; the rejected five deliberately **not** seeded, to avoid preserving rejected ideas as permanent memory. | Session: Fable-window leverage investigation |
| 2026-07-19 | 04-knowledge/_canonical-job-sheet.md (new); templates/_job-sheet-template.md (new); 04-knowledge/concepts/quote-lifecycle.md; 00-inbox/2026-07-11-three-document-model-and-billing-math.md; 06-insights/2026-07-18-idea-research-job-sheet-type-formalization.md | **[gated] Formalized the `job-sheet` document type** (Jesse: "Build now" on the idea-research review). Promoted USA26038's one-off job sheet into a canonical exemplar + derived template, mirroring the `_canonical-heater-card` → `_heater-template` pattern. The work-up billing math is now canonical in the exemplar where it's applied — combined man-hours for labor (`Qty × per-person shift hours`), per-diem headcount excluding a billable PM, DEF per quoted shift count, PM and Filter Unit lines conditional on the quote and on filtration election. The three-document model (job sheet static from quote / heater card persistent / job report actuals) landed in `quote-lifecycle.md` alongside a create-at-award step. **Marked DRAFT** — validated against one instance where the heater card had seven, so section shape is provisional until a second won bid tests it. Schema carries `source`/`verified` per OP-FRONTMATTER, identifying *which quote revision* the tables came from; USA26038's own DSP# isn't recorded anywhere in the vault, so that file stays lint-flagged rather than placeholder-filled. Render script scoped and deliberately deferred. Revert path: `git revert 4fe222d`. | Session: harness goal-alignment audit |
| 2026-07-19 | ~/.claude/settings.json (15643c2); obsidian-work/.claude/settings.local.json (untracked, gitignored); CLAUDE.md (67b3d95); 00-inbox/2026-07-19-{auto-mode-permission-drift,fieldpm-zero-invocations}.md (new) | **[gated] Three harness decisions from a `/doctor` pass.** (1) **Auto mode is now the default permission mode** (`permissions.defaultMode: "auto"` in user settings — the only scope where that value is honored). Deny rules still take precedence, and the CLI falls back to prompting if auto mode is ever unavailable. (2) **Permission allow-lists hardened.** The vault's local list had grown to 61 rules; pruned to 36. Seven were live hazards rather than clutter — `git checkout *` (matches `git checkout -- .`), `python -c ' *` and `python -`, `pip install *`, a `cat ... 2>/dev/null *` rule whose trailing wildcard sits *after* a redirect, and `Read(//c/Users/Jwuts/**)` covering the whole user profile. Eighteen more were dead one-offs (job-specific commit messages, leverage-repo `ls` probes) and three duplicated the checked-in project settings. Also dropped `Bash(git fetch:*)` from global settings: it reads read-only but is arbitrary code execution via `--upload-pack` and `ext::` URLs. **Tension noted, not resolved** — auto mode auto-saves approvals, which is what grew the list in the first place (it added `Bash(npm view *)` mid-session); re-check the count ~2026-09-19 per the inbox note. (3) **CLAUDE.md folder listing removed as derivable** — `ls` plus `INDEX.md` already cover it, and it had gone stale (`apps/` and `work/` unlisted). Kept the `tools/` script-to-output mapping and the `03-jobs`/`05-projects` prohibition, neither derivable. Diagnostic findings requiring no action: install/version clean (2.1.215, current), no unused skills or MCP servers, and the `claude-obsidian` SessionStart hook that failed 24 times (2026-06-21 to 07-03) was a prompt-type hook on an event that only supports command-type — it never worked, and the plugin is fully removed. Revert path: `git revert` each commit; local allow rules restore by re-approval on prompt. | Session: Claude Code /doctor pass |
| 2026-07-20 | 06-insights/2026-07-20-idea-research-hardened-extraction-corpus-vision-arm.md; 00-inbox/idea-hardened-extraction-corpus-vision-arm.md | **[gated] Parked the hardened-extraction-corpus + vision-arm idea** (Jesse: "Got it" on the idea-research review's park recommendation). The `leverage` thesis experiment's 2026-07-06 saturation result (all three conditions 100% pass) is real and its own design doc calls for a harder corpus, but the seed's forcing rationale — metered/expiring Fable access — is void since the 2026-07-19 Max upgrade made access ongoing, and the same-night window-allocation decision already routed capacity to live-money work instead. Revisit trigger: live extraction-routing pain, or the `leverage` repo's own 90-day dormancy checkpoint (~2026-09-30). If resumed, corpus-hardening (distractor density, schema breadth, difficulty calibration) and the vision arm (new harness item format + pre-registration amendment) are separable — external research surfaced RealKIE/VRDU/AECV-Bench as design references for the latter rather than building from scratch. | Session: Vault Idea Research Loop |
| 2026-07-20 | ~/.claude/CLAUDE.md (a0044b6); ~/.claude/.gitignore (5891b65); claude.ai profile + Cowork custom instructions (not vault files); 00-inbox/2026-07-20-claudeai-skill-library-is-a-second-copy.md (new) | **[gated] Rewrote the claude.ai instruction layer by scope, and added two global CLAUDE.md preferences.** The chat custom instructions were ~600 words written during a low-trust period and had accumulated four separate restatements of the stakes-calibration rule, each added as a corrective without deleting the cautious one it contradicted. Cut to ~290 words on a scope test — **profile** carries what is true on every surface (identity, accuracy, style), **Cowork** carries how Claude *acts* (autonomy thresholds, failure-mode flagging, honest reporting, git sequences), **project** instructions stay unwritten until a recurring workflow needs them and should then be deltas, not a fourth copy of vault conventions. Three specific fixes: the "assume minimal prior knowledge" depth rule directly contradicted "concise by default" and described a pre-vault version of Jesse, replaced with read-fluently/don't-write-systems plus an explicit git carve-out (concepts known, operational sequence not — state the sequence, don't explain the concept); the audit rule was inverted from Jesse-initiated to Claude-initiated, since the live gap is knowing *when* to reach for a tool, not distrust of output; `/code-review` and `/ultrareview` were dropped as Claude Code commands that don't resolve in chat at all. **Kept deliberately:** the search-Anthropic-docs-first rule, which is a live compensation for training staleness rather than a low-trust artifact — this session reproduced the failure it was written for. Global CLAUDE.md gained a **Show sequences** item (arrow chains for unfamiliar features and order-of-operations work; Mermaid only when the flow branches) and a corrected Output rule distinguishing a closing *recap* (unwanted) from forward-looking *suggestions* (wanted) — the old "no closing summary" wording suppressed both. Also gitignored `chrome/` and `uploads/` in the config repo: `uploads/` held a QuickBooks quoted-vs-actual rate workbook and the repo was public at the time, so one `git add -A` would have published it. Repo has since been made private. Filed the claude.ai-skill-library-drift finding to inbox. Revert path: `git revert a0044b6 5891b65`; claude.ai instructions restore by paste. | Session: custom-instruction maintenance |
| 2026-07-20 | 06-insights/2026-07-20-harness-map.md (new); CLAUDE.md; 01-context/{output-preferences,system-workflow-reference,workflow-map}.md; memory/project-harness-audit-2026-07.md | **[gated] Built a verified harness map and closed four rule-3/rule-4 items off it.** Full read-recon inventory of every response-shaping surface (both CLAUDE.mds, MEMORY.md, skills, the hook, permissions, lint, the five loops) surfaced the collisions from the "overbuilt harness" framing. Closed: (1) the stale `anthropic-skills:*` skill duplication — traced to nine account-level skill uploads on claude.ai (not on disk, not a plugin), Jesse deleted them; the deletion also restored `disable-model-invocation` intent for `idea-triage`/`usadebusk-vault-ingest`, which the un-flagged account copies had been leaking into the model's auto-invocation surface. (2) Heater Task-Durations schema — vault CLAUDE.md inlined a stale 10-column header (missing `Condition`) vs. the exemplar's/skill's 11; replaced with a pointer, plus fixed `vault_lint.py` "7 rules"→9 in the same file. (3) Output-prefs duplication — **decision: global CLAUDE.md is the single home** for universal formatting rules (must apply cross-project); `output-preferences.md` now points up. (4) Eager load — demoted `system-workflow-reference.md` + `workflow-map.md` from the read-every-session rule to on-demand (~10 KB off each session). Git-authority duplication reviewed and deliberately kept (safety summary + detail). Revert path: `git revert` commits 56bdb0b, 2d0d23f, and this one. | Session: harness map + cleanup |
| 2026-07-20 | ~/.claude/CLAUDE.md; CLAUDE.md; 01-context/{output-preferences,estimating-approach}.md; 06-insights/2026-07-20-harness-map.md | **[gated] Adversarial-review audit of the same-day harness cleanup; applied 7 corrective fixes.** A scored finder/adversary/referee pass verdicted the four cleanup calls: Call 3 (demote two workflow files) held clean; Calls 1, 2, 4 needed revision. **Reversed Call 1's "reviewed, no change" on the git bans** — the always-loaded global summary listed 4 hard bans while `knowledge-system-governance.md` carried 5; added the missing 5th (never push to a shared/customer-facing remote beyond own `origin`) to global for parity (kept the summary-on-always-loaded-surface design; fixed only the drift). Call 2 residue: removed two behavioral rules (two-failure-stop, better-path) still duplicated in `output-preferences.md` after its header claimed it "does not restate the base rules," plus one uncertainty-section echo; repointed vault CLAUDE.md's Output line to global (the earlier dedup had left it naming `output-preferences.md` as the formatting home after formatting moved up); restored the dropped "full length when quality requires it" nuance to global. Call 4 misses: added the routine-vs-crash condition-match qualifier to eager `estimating-approach.md` (collision the map catalogued at §3 but never dispositioned) and fixed the map's own rule-5 lint proposal that restated the stale 10-column Task-Durations header (now 11 with `Condition`). Referee dropped six false positives (incl. fieldpm "dormant" = correct-by-design under the single-status-home rule). Revert path: `git revert` this vault commit + the config-repo commit. | Session: harness-cleanup audit (adversarial-review) |
| 2026-07-20 | 02-facilities/CHS/McPherson-KS/USA25025-job-record.md; 00-inbox/2026-07-20-pig-appendage-hardness-reference.md (new); memory/feedback-appendage-hardness-low-key.md (new) | **[gated] Answered Randy Lenhart's (CHS McPherson) pig-type + bolted-pig questions via email; decided against a formal supplement.** Recon of the real Gmail thread ("Fw: Pigging Documentation from 2025 McPherson Turnaround") corrected the handover's four-item framing: three literal asks (flow-test form, pig-type legend, bolted-pig projection+hardness) plus a *goal* (fouling-vs-cleanliness benchmarking) the handover had mistaken for a deliverable. Jesse surfaced AlphaMet File 23890 — third-party P.Eng hardness testing of the full 8-type appendage library — which fully answers hardness (and confirms the bag numbers are nominal Brinell targets) but has **no stud-projection data**. Disclosure line set with Jesse: TC ~1700 HV is the **standard** appendage, 600 HB / stainless ~260 HV supplied **on request** for scar-sensitive tubes; catalog breadth, vendors (AlphaMet, Pipeline Pigging Specialties), SKU tiers, and selection logic all withheld; projection answered functionally, not numerically. Hell Razor and foam-pig legend cut from the email at Jesse's direction. Data captured to inbox (low-key, not skill-promoted); flagging preference saved to memory paralleling the passivation guardrail. **Still open:** blank reusable flow-test sign-off form not yet built. Deliverable was an email body (chat); no customer document generated. | Session: CHS McPherson supplement |
| 2026-07-20 | tools/vault_lint.py; tools/fixtures/02-facilities/TestClient/Test-City-TX/T-200.md (new); CLAUDE.md; 14 heater cards (02-facilities/**, Condition column — separate [auto] commit) | **[default] Added the DURATIONS-HEADER lint rule (the rule-5 lock proposed in the harness map) and backfilled the drift it caught.** `check_durations_header` asserts each heater card's Task-Durations header matches the canonical 11-column schema exactly, anchored on the `## Task Durations` heading so it can't false-positive on the many other tables that mention Rig-In (proposals, rate tables, prose — an early token-based version flagged ~40 such lines; rejected). Ships with fixture T-200 (stale 10-col header) per no-fixture-no-rule; self-test now covers 10 rules. First run caught 14 cards still on the 10-column header — the 2026-07-19 Condition migration had reached only cards carrying actuals, not empty-table cards — so all 14 were backfilled with the trailing Condition column (empty tables, mechanical, Lane 1, `[auto]` commit). Also dropped the stale "9 rules" count from CLAUDE.md (now defers to the script). Revert path: `git revert` this rule commit + the `[auto]` backfill commit. | Session: harness-cleanup audit follow-up (rule-5 lock) |
| 2026-07-20 | 01-context/estimating-approach.md; 50-dashboards/decision-queue.md (DQ-001) | **[gated] Thinned eager `estimating-approach.md` to the duration model; queued a surfaced markup-drift decision.** Cut the four skill-duplicated sections (Commercial structure, Equipment profile, Pricing rules, Proposal structure — all owned by the `usadebusk-estimating` skill) down to a one-line authority pointer; kept only the duration model (100 ft/hr baseline, adjust-down triggers, fixed durations, and the condition-match + actuals-rollup guard) as the one piece worth having cold before the skill loads. ~64→22 lines off every session's eager load; closes the last open item from the harness audit. While removing the markup line, found third-party markup stated in five homes that disagree — skill + this file said **15% typical** (Jesse-confirmed 2026-07-19), but `company-context.md` and `04-knowledge/concepts/estimating-pricing.md` still say **10% baseline** — queued as **DQ-001** (high/pricing, not auto-resolved). Revert path: `git revert` this commit. | Session: harness-cleanup audit follow-up (eager-load thinning) |
| 2026-07-20 | ~/.claude/skills/usadebusk-estimating/SKILL.md; 01-context/company-context.md; 04-knowledge/concepts/estimating-pricing.md; 50-dashboards/decision-queue.md (DQ-001 closed) | **[gated] Resolved DQ-001 — third-party markup is 5% / 10% / 15% per contract, no default.** Jesse in-session: "we use 5%, 10%, or 15% markup; it depends on the contract rates for the specific project and/or facility." Fixed the two stale homes that framed 10% as a no-contract baseline (`company-context.md`, `04-knowledge/concepts/estimating-pricing.md`) and reconciled the skill's "15% typical/trending" wording to lead with the three-tier contract rule, demoting the recent-jobs-mostly-15% data to a subordinate observation rather than a default. 10% is a valid tier, not a fallback — the error was the "baseline/default" framing, not the value. DQ-001 closed. Revert path: `git revert` this vault commit + the config-repo skill commit. | Session: harness-cleanup audit follow-up (DQ-001 markup) |
| 2026-07-22 | 04-knowledge/concepts/decoking-method-comparison.md (new); INDEX.md (regen) | **[default] Filed a reusable decoking-method comparison note (mechanical pigging vs. steam-air).** Captures the coker-heater / oil-sands "why pigging" positioning and — most valuably — the web-verified external sources so they don't need re-checking: **AFPM Question 74** with the key claims attributed to **Marathon Petroleum** (refiner) and **CH2M Hill** (engineering firm) — peer testimony, not vendor marketing — and the **AIChE Decoke Tutorial (paper 78d)** for emissions. Includes a "source landmines" list capturing the verification dead-ends: Cokebusters is a competitor (don't cite — Jesse's call); the two OGJ articles don't actually contain the duration/rupture claims search snippets implied; US Patent 4,297,147 is a misattributed abrasive-gas method; the "96 hrs→24–36 / 5 days→2 days" case-history numbers are uncorroborated ChatGPT inflation. Originated from the 2026-07-22 DeBusk Services Canada coker-heater rep-meeting leave-behind (Marshall Douglas request); the branded `.docx` is not stored in-vault (binary; vault is markdown-first — regenerate from the note). Revert path: `git rm 04-knowledge/concepts/decoking-method-comparison.md` + `git revert` this commit. | Session: DeBusk Canada steam-air-vs-pigging document |
| 2026-07-22 | 04-knowledge/_canonical-heater-card.md; ~/.claude (CLAUDE.md, hooks/usadebusk-git-guard.mjs, skills/usadebusk-core, skills/usadebusk-vault-ingest — config commit 98243ab) | **[gated] First Fable 5 config/skill-system audit — 8 findings, 6 fixes applied (Jesse-approved), 2 questions resolved/open.** Full-stack semantic audit (9 skills, 3 CLAUDE.md layers, settings, hook, 01-context, canonical card, templates). Key catches: (1) usadebusk-core still said third-party markup "10% baseline" — the **fourth stale home DQ-001 missed** on 2026-07-20; now defers to estimating's 5/10/15-per-contract rule. (2) Canonical heater card was still stamped DRAFT while governing production — Jesse approved the Phase 2.0 field set, now marked APPROVED. (3) git-guard hook had a `git -C <path> <verb>` bypass and lacked switch/restore/clean — hardened, 7-case test suite all-pass. Also: stale "Cowork" annotation retired from vault-ingest; Config Rollup wording aligned ("derived from Tube Geometry, hand-entered" / "two scale rows"). Storage estate clarified by Jesse: **SharePoint is canonical for all job docs and proposal workups; OneDrive per-facility folders are working copies pulled down for editing.** Open: verify "Sniper-Elite" and Magellan-ONEOK-vs-ONEOK entries in vault-ingest's known-client list. Revert path: `git revert` this vault commit + config commit 98243ab. | Session: Fable 5 config/skill audit |
| 2026-07-22 | ~/.claude/skills/usadebusk-vault-ingest/SKILL.md (config commit 906d473) | **[gated] Resolved the audit's open client-list item — removed Sniper-Elite, ONEOK, Magellan-ONEOK (known clients 32→29).** Jesse's call: Sniper-Elite dropped outright; ONEOK and Magellan-ONEOK are real companies the firm has history with but no projects Jesse worked, and too little data to warrant vault presence. No facility folders existed for any of the three, so removal orphaned nothing; future docs tagged with these names will now flag as unknown-client and route to 00-inbox rather than auto-creating a folder. Closes the last open item from the Fable 5 config/skill audit above. Revert path: `git revert` config commit 906d473. | Session: Fable 5 config/skill audit |
| 2026-07-23 | tools/vault_health.py; tools/vault_lint.py; 6 quote notes (02-facilities/**/DSP*); 00-inbox/idea-context-packet-builder-skill.md; 06-insights/2026-07-19-rate-model-grain-review.md; 04-knowledge/concepts/rfq-intake-protocol.md; 06-insights/2026-07-23-triage-vault-architecture-first-principles.md; 50-dashboards (regen) | **[default] Built the vault-architecture triage's approved items: dormant-trigger registry + commercial pipeline vitals on the health dashboard, and the POINTER-DEAD lint rule.** From the first-principles triage (3 execute / 2 test / 1 park / 6 kill; Jesse approved building in-session). The dashboard now reads `revisit-trigger:` frontmatter into a Dormant triggers section (machine token `[machine: quote-count>=N]` evaluated; event triggers name their checking step) and renders a Commercial pipeline section off quote frontmatter (`valid-through:` added as an additive field, backfilled on all six quote notes; DSP26030 normalized to a tracked quote). First run surfaced real exposure: **DSP26030 expired 2026-06-04 with outcome unrecorded** (standing FAIL row). Test 5's spike passed, so `vault_lint.py` gained POINTER-DEAD (warn-only, base-gated for machine portability, runtime fixture — self-test now 11 rules); its first run found 4 of 6 recorded source paths dead from the estate reorg into `Desktop\Facilities\`. DSP26080 re-pointed and its OneDrive folder renamed to drop a trailing U+00A0 that broke every retyped path; DSP25084/DSP26039/DSP26058 pointers left as standing warnings (targets not found in a bounded search). Still open from the triage: test 3 (retrieval-eval, needs a fresh session) and idea 6 (staged-count hook, config repo). Revert path: `git revert` this commit; the new frontmatter fields are inert without the scripts; rename back the OneDrive folder if needed. | Session: vault-architecture first-principles triage (Fable 5) |
| 2026-07-23 | 04-knowledge/knowledge-system-evaluation-questions.md (last_reviewed); 06-insights/2026-07-23-retrieval-eval-run.md (new); 06-insights/2026-07-23-triage-vault-architecture-first-principles.md (test-3 box + apply log) | **[default] Ran the retrieval-eval for the first time (triage test 3) — 10/10 pass, 0 failures → stop.** A fresh session answered all ten KS/USA questions in [[knowledge-system-evaluation-questions]] cold, graded against the note's own pass criteria. Zero hard failures, so the pre-defined criterion resolves to **stop**: no sixth loop, no quarterly eval pass — retrieval is not the binding constraint at ~100 notes. [[idea-llm-navigable-vault-map]] was gated on this producing failures and stays parked (not killed). Two shallow passes recorded as latent risks rather than question notes: USA-005 (governance note's lead paragraph "reviewable proposals, not silent rewrites" is pre-2026-07-06 and no longer matches the Lane-1 facility carve-out below it — a retrieval that stops early answers stale) and KS-004 (inbox-by-status is a 26-file scan, not a lookup surface). Result note [[2026-07-23-retrieval-eval-run]] carries the full table. Revert path: `git revert` this commit; no canonical content changed. | Session: retrieval-eval run |
| 2026-07-23 | 04-knowledge/concepts/rfq-intake-protocol.md; 4 quote/heater notes re-pointed (DSP25084/26039/26058/26080, H-2501) | **[default] Established a going-forward estate naming & foldering convention (Jesse-approved); resolved all POINTER-DEAD findings against the live estate reorg.** POINTER-DEAD (built earlier same day) surfaced that the vault recorded `Desktop\` working-copy paths while the reliably-present store is `OneDrive\USADeBusk\Facilities\<Client City ST>\{Bids,Jobs,Reference,_History}\`. Jesse consolidated folders into that canonical tree live during the session (which briefly moved ground truth mid-fix — DSP25084's subfolder got zipped between two checks); all five notes re-pointed and verified, POINTER-DEAD now 0. The recurring root cause — no naming/foldering discipline — prompted a six-rule convention added to [[rfq-intake-protocol]] step 1, each rule traced to a concrete failure this session (client-spelling drift, state-in-folder-tier, fixed `DSP#NNNNN` filename token, rev-in-name + `OLD_` supersede, no opaque-ID-only names, no trailing/NBSP folder chars). **Going-forward only — existing folders untouched until POINTER-DEAD flags one** (avoids the unbounded-retrofit pattern rejected as triage idea 11). Revert path: `git revert` this commit + the re-point commits (0d34be3, 42f9bba). | Session: dead-pointer resolution + naming convention (Fable 5) |
| 2026-07-24 | ~/.claude/skills/usadebusk-estimating/{scripts/extract_workup.py, render_proposal.py, proposal_input_template.py, backtest_workup.py, README.md, .gitignore, assets/Debusklogo.png, SKILL.md}; 04-knowledge/workup-to-proposal-generator-build-spec.md; 00-inbox/2026-07-24-dsp26085-submitted-wrong-quote-number.md | **[default] Built the workup-to-proposal generator and resolved all four open decisions.** Reads a finished workup xlsx and emits the proposal's commercial content (Section 7 Quotation + Section 9 rates + Section 3 header) as a branded docx DRAFT — never sends, never reads the cost/margin columns. Mirrors the job-report generator (usadebusk-fieldpm). **Regression PASSED exactly**, line-item by line-item and Pricing-Summary-box row by row, against the three submitted PDFs (DSP26071.2 $60,287.42, DSP26085 $40,477.08, DSP26068.1 $112,642.23; DSP26085 rendered docx→PDF via LibreOffice); a 4th workup (DSP26075) exercised the reconciliation path. Decisions resolved: (1) Pricing Summary box — read the already-resolved box from the formula-linked `Insert Quote` tab and reconcile to Total, don't re-derive the two bucketing styles; (2) multi-heater — detect-and-flag (split path deferred, no real multi-heater workup exists to validate); (3) Mob/Demob equipment prose — read verbatim from `Insert Quote`, not a J field; (4) authoring-vs-operating — authored once (Opus), operate per job on a cheaper model, no config required. Key discovery: the `Insert Quote` tab is the pre-built quotation fully formula-linked to the work tab; two-source read (financials for amounts, Insert Quote for prose/box/header) catches desync and the stale-paste errors frozen into the old image-pasted quotations. Revert path: `git revert` this commit + the config-repo commit 013986c; the scripts are inert if unused. | Session: workup-to-proposal generator build (Opus 4.8) |
| 2026-07-24 | 04-knowledge/sops/sop-formatting-standard.md; ~/.claude/regression/** (config commit) | **[gated] Amended the SOP formatting standard on two points surfaced by the Opus 5 F4 regression replay; promoted F4 and F5 to the Opus 5 baseline.** Track A of the Opus 5 replay battery (the two fixtures skill drift did not confound) both passed — F5 pig sizing 5/5 diff keys with every numeric exact, F4 SOP formatting 7/7. Two apparent misses in F4 were adjudicated against the standard rather than assumed to be model error, and they split. (1) **Real miss:** the title-block DOCUMENT NO. cell carried the full document name where the standard's own template shows the short `[SOP-DCK-xxx]`; corrected in the promoted baseline, and the standard now states outright that the cell takes the short form only, since revision has its own cell beside it and the full `USAD-...-REV[N]` already appears in the running header, footer, and filename. (2) **Reversed on recon:** the dropped RFWN definition was not an error — RFWN is a flange-type descriptor (`6" 300# RFWN` in the Baytown cards) and the F4 job states `4" 300#` with no flange type, so the term never appears in the SOP body. The standard listed it as always-included beside genuinely universal terms while already marking Triple Mode "(when applicable)"; split into a standard-terms list and a **conditional-terms list** (Triple Mode, RFWN, Swab, Rig-Over, facility-specific), with the rule stated as: do not define a term the document never uses. Fable-5's inclusion of RFWN was following the letter of a rule that was itself wrong. Also notable: the run surfaced the 2" minimum firewater hose — the one-line `usadebusk-equipment` edit made after the baseline — confirming skill drift propagates as expected rather than reading as model regression. Regression README amended in the same pass: promotion is now **per fixture, not per battery** (the six no longer settle together), the baseline is explicitly mixed-model, and promotions must carry a `scope_expansion_note` so a later terser model is not failed for omitting elaboration the fixture never required. Revert path: `git revert` this vault commit + the config-repo commit; Fable-5 baselines remain recoverable at config commit `7d5eebd`. | Session: Opus 5 regression replay, Track A |
| 2026-07-24 | ~/.claude/skills/usadebusk-estimating/SKILL.md; ~/.claude/regression/** (config commit); 01-context/estimating-approach.md; 04-knowledge/concepts/estimating-pricing.md | **[gated] Ruled the pigging benchmark per-pig rather than heater-total, and fixed four downstream defects the F1 regression replay surfaced; promoted F1 to the Opus 5 baseline.** Track B replay of the RFQ→proposal fixture came back at 44 project hours against the Fable-5 baseline's 57, because it did not compute pigging serially: it costed one unlooped coil, laid the four passes into two sets on the TriMax's three assemblies, and took a simultaneous set's elapsed time as one coil's rather than three. Jesse ruled per-coil/parallel correct. It is what the skill's build-up method already prescribed verbatim and what the actuals rollup's `ft/hr per pig (norm)` column already assumed, and **HF-0012 settles it empirically — 12,036 ft at mode 3 in 85 elapsed pig-hours where heater-total-serial arithmetic predicts 120.** The serial reading over-quotes every multi-pass job by roughly a third of its pigging hours; on the fixture it was a $14,304 difference on a $58,294 job. Now stated outright in the skill with a worked 4-pass example, and — per the DQ-001 multiple-stale-homes lesson — in both vault homes as well, including the eagerly-loaded `estimating-approach.md` so it is cold in context before the skill loads. Four smaller fixes, all Jesse-approved in the same pass: (1) **rounding rule** — the per-coil figure rounds up to the next whole hour, once, at the coil level, no re-rounding downstream (the run rounded 13.18 down but 16.25 up); (2) **the `$58.00/hr Driver Travel` rate line was unreachable** — the $3.00/mile is stated to include driver labor and drivers carry no separate travel line, leaving the rate with no subject, so it is renamed **Crew Travel (non-driver)** and the Mob/Demob rules now name it as the only travel-labor rate; (3) **no fixed parallel-friction factor** — the rollup's two routine multi-pass rows (47 and 52 ft/hr per pig, against a mode-1 average near 132) show real degradation but two jobs from two clients is too thin to price from, so the rule is to read those rows and *state the allowance applied, including "none"*; (4) the Section 4 template no longer hardcodes 6-hour rig defaults or a single heater-wide pigging row. Third consecutive replay where an apparent model miss adjudicated to a defective baseline rather than model error (after F4's RFWN and F3's missing overrun flag). F1 promoted unedited — its dollar figures are now superseded by the rounding rule and annotated as such rather than hand-recomputed, since re-authoring forty figures would make the arithmetic the reviewer's and destroy the record of raw model behaviour. F6 is the last open fixture. Revert path: `git revert` this vault commit + the config-repo commit. | Session: Opus 5 regression replay, Track B / F1 |
| 2026-07-24 | ~/.claude/skills/usadebusk-estimating/SKILL.md; ~/.claude/regression/** (config commits 4cefb76, 5a3eb95) | **[gated] Completed the Opus 5 regression battery — F6 promoted, `frozen/` now entirely Opus 5; three more skill fixes.** F6 (duration + mob/demob) was the first fixture replayed after the same-day Duration Model rewrite and is the evidence it lands: the run derived mode 6 from 2× TriMax, resolved sets to `ceil(6÷6)=1`, took rig-overs to **zero** from the formula, used the per-coil basis without hedging (900 ÷ 75 = 12 hrs, explicitly not 5,400 ÷ 75 = 72), and stated its parallel-friction allowance because the new rule requires it — none of it prompted. **31 project hours against the Fable-5 baseline's 88.** Every actual it cited verified exact against source. Three fixes adopted from it: (1) **travel-rate precedence** — a contract- or bid-stated travel rate governs whatever figure it carries, and the $58 Crew Travel baseline applies only where none is stated; the run had overridden the fixture's stated $64 citing wording introduced *that same morning*, which was the defect, not the model. (2) **F-802 named as the known counter-example to "rig-out matches rig-in"** — it ran rig-in 4 against rig-out 20, five times the matched figure; one row does not move the rule, so the rule stands and the exposure must now be stated in the duration math on multi-rig and large multi-pass jobs. (3) **Filter press capacity written into the skill as an equipment-profile decision** — one press against 2× TriMax is the exact configuration that hit capacity repeatedly on USA26022 and generated **20 hrs of billable stand-by, $25,628.17**; the constraint was recorded on the F-802 card but nowhere an estimator would meet it while choosing equipment. Battery close-out: all six fixtures settled, Track A passed on diff keys (F5 5/5, F4 7/7), Track B's four judged correct on merits — **and in three of those four the baseline turned out to be wrong, not the model.** The battery's real output was nine skill and standard corrections the fixtures forced into the open, not a verdict on the model. Two promoted baselines deliberately carry known-stale figures (F1's dollars predate the round-up rule; F6's mob/demob use the wrong travel rate) rather than being hand-patched, since re-authoring the arithmetic would destroy the record of raw model behaviour — both annotated in frontmatter. The `adversarial-review` / `idea-triage` reword hold is released. Revert path: `git revert` this vault commit + config commits 4cefb76 and 5a3eb95. | Session: Opus 5 regression replay, Track B / F1 + F6 |
| 2026-07-25 | ~/.claude/skills/{usadebusk-vault-ingest,usadebusk-estimating,usadebusk-core} (config commits 9c8068a, dad1c06, 9c020bd); 04-knowledge/vault-skill-drift-loop-spec.md; tools/vault_health.py; 04-knowledge/equipment/equipment-library.md; 04-knowledge/concepts/estimating-pricing.md; 01-context/company-context.md; 06-insights/2026-07-25-skill-drift-review.md; 00-inbox/2026-07-25-skill-drift-lane4-holds.md | **[default] Adjudicated the first scheduled Skill-Drift Loop run — 4 of 6 skill findings applied, 2 refused; the loop's own docs were the biggest finding.** Every quoted "current text" was re-verified against live `main` before acceptance (all six matched). Applied: job-sheet `doc_type` + enum now that the type is formalized, the state-or-province naming rule (two Canadian sites were already living outside it), the work-up billing-math repoint to `_canonical-job-sheet.md` plus the filtration-election rule, and Jesse's role line. **Refused F3** (fieldpm demob date) — unverifiable from files, since the vault records last shift and completion but never the physical equipment-return date; the finding's own open question said so and the branch changed it silently anyway. **Held F4** (filter-press fleet count) as Lane 4. `drift/2026-07b` was deliberately *not* merged — accepted hunks were cherry-picked or hand-applied so the two rejected ones could not ride along; the branch is retained unmerged as the record. **V1 was the real find:** the loop has been enabled and firing monthly since 2026-07-19 while its spec said "registered but disabled" and `vault_health.py` deliberately excluded it — an enabled loop with no heartbeat row, which is precisely the blind spot the run ledger exists to close. Now tracked (62 d, loose because it commits only on findings); `health.md` shows four loop rows. **The spec's stated reason for staying manual was false:** config-repo write authority was never gated — `settings.json` runs `defaultMode: auto`, `git add`/`commit`/`push` are in neither `allow` nor `deny`, and the git-guard hook matches only `USADEBUSK[\/]`, not `C:\Users\Jwuts\.claude`. Any session can commit and push the config repo unprompted; the containment on this loop is procedural (never merges, never edits `main`), not permissional. Loop Scope extended with `~/.claude/regression/` as drift class 6 — that surface demonstrably drifts and it is the instrument that detects everything else, so its staleness is silent — with `frozen/` explicitly flag-only. Full six-fixture regression battery replayed after the skill changes: five pass, **F1 fails diff key 4** (missed the six-items-vs-"5 pieces traveling" contradiction in intake item 15), not attributable to these edits and `frozen/` not re-cut. Revert path: `git revert` this vault commit + config commits 9c8068a, dad1c06, 9c020bd. | Session: skill-drift 2026-07 adjudication |
| 2026-07-25 | tools/vault_lint.py; templates/_idea-seed-template.md; 04-knowledge/vault-idea-loop-spec.md; ~/.claude/scheduled-tasks/vault-idea-research-loop/SKILL.md (config commit) | **[gated] Added a `gated` idea-seed status so the Idea Research Loop stops burning cycles on seeds whose own gate is already shut.** Surfaced while closing the 2026-07-25 LLM-navigable-vault-map research note: the loop selects the oldest `unexplored` seed and goes straight to web research without ever reading the seed's stated gating condition. That seed had shipped with an explicit test-before-build gate ("if the eval shows zero retrieval failures... should stay parked"), the retrieval-eval had run two days earlier and returned 0 failures against a ≤1 criterion, and the loop spent a full run rediscovering it — its own Interpretation opens "the seed's own gate closed it, before this loop even picked the seed up." Not a one-off: the next seed in the queue (`idea-pig-actuals-maturation`) is gated on "three or four heaters carrying real counts" against exactly one usable data point (HF-0012), so the next run was on track to repeat it. Jesse chose the queue-preserving design over the cheaper one: a new **`gated`** status (non-terminal — a gated seed is live and waiting, not resolved) that the loop skips when selecting, rather than consuming the seed with a `researched` flip that would quietly retire a still-live idea. New Loop Step 3 does a **files-only** gate check with three outcomes — verifiably shut → flip to `gated`, record a `revisit-trigger:`, write a short "gated — not researched" note and move to the next seed (gate closures do not count against the one-seed-per-run budget); verifiably met or no gate → research normally; **stated but not settleable from files → research anyway**, so the loop can never gate its way out of hard work. Resurfacing rides the existing dormant-trigger registry rather than a parallel mechanism — a gated seed carrying `revisit-trigger:` renders in `health.md`'s Dormant triggers table automatically — and INBOX-AGE now exempts `gated` so a legitimately-parked seed doesn't decay into permanent lint noise. Verified end-to-end before commit: exemption fires, `gated` is in-vocabulary and non-terminal, and a gated seed appears in the trigger registry; lint self-test 11/11, 0 errors. The pig-actuals seed was deliberately left `unexplored` so tonight's run exercises the new path and leaves a record, rather than being pre-gated by hand. Revert path: `git revert` this commit + the config-repo commit. | Session: skill-drift adjudication → idea-research walkthrough |
| 2026-07-26 | 02-facilities/ExxonMobil/Baytown-TX/{F-901.md,DSP25123.md,_facility.md,DSP25084.md} | **Ingested the ExxonMobil Baytown PS8 F-901 (KHF) Kerosene Furnace project — new heater card + new quote note.** Sources: DSP25123 Rev 1 & Rev 2, SOP-DCK-F901-001 REV 0, the F-901 flow diagram, and Invoice 1018569 with its full PO 4510719347 reprint. F-901 is a 4-coil / 3-circuit heater (process coils looped 2→1 at the radiant outlets, Treat Gas single pass, Superheat Steam single pass), 4,200 ft total, all three circuits run simultaneously in Triple Mode on one TriMax. **Task Durations left deliberately empty** — the job billed as one fixed-price line at exactly the quoted $39,905.41, so it yields no hour actuals and no service receipts or ticket breakdown exist. Five conflicts recorded rather than silently resolved: (1) **the SOP's Phase II instructs upsizing to a 5.5" pig against a 5.043" ID** — 0.457" over the ceiling, contradicting the SOP's own Operating Parameters table (5.293"); the card carries the rule-correct 5.25". Customer-issued document, needs correction. (2) **Invoice records `USA# 26022`, the same job number already on [[F-802]]**, with the F-802 service dates — unresolved whether both heaters share one job number or the date field was copied; matters because F-802's Task Durations row is keyed on it. (3) **DSP25123's rate sheet shows Filtration Standby at $35/hr against F-802's $150/hr and drops the Support Unit row entirely** — reads as a deleted row pulling $35 upward; $115/hr under-recovery exposure if reused, and it is the most recent Baytown rate table. (4) Quote body and ExxonMobil's PO both cite `DSP#: 25084` (the F-802 quote) — template artifact; 25123 recorded as the number of record. (5) Radiant footage does not multiply out (18 × ~89 ft = 1,602 ≠ stated 1,200). Also: filtration quoted `No` but specified in the SOP and diagram; the issued proposal names Citgo three times in a document sent to ExxonMobil. Stale "not yet invoiced" line on [[DSP25084]] corrected and its pre-flat-structure wikilink fixed. Revert path: git revert this vault commit. | Session: ingest — Exxon Baytown F-901 |
| 2026-07-26 | 02-facilities/ExxonMobil/Baytown-TX/{F-901.md,DSP25123.md,DSP25084.md,F-802.md} | **Jesse rulings applied to the F-901 ingest — three of the five flagged conflicts closed same-session.** (1) **F-901 is USA26031, not USA26022.** F-802 (USA26022) and F-901 (USA26031) ran back to back on one mobilization — F-901 started as F-802 finished, putting its start at 2026-05-07. Invoice 1018569 is **mis-stamped**: its Job Number field reads `USA# 26022` and its service period `4.28 - 5.7.2026`, both carried from F-802; the PO and amount are correctly F-901's. This also **closes the long-open `USA26031` flag on [[F-802]]** — that number on the USA26022 receipt pages is the F-901 job, not an anomaly, so receipt page counts spanning both numbers cover two heaters. (2) **Filtration was quoted and engineered into the scope, then declined by the customer immediately before execution — none ran.** Job Options status stays Declined but the reasoning is now correct; the SOP and diagram show a filter press because they were written to the pre-execution scope, which resolves the apparent doc conflict. Worth carrying into the next Baytown bid, since F-802 on the same mobilization burned 20 hrs of billable standby on filter-press capacity. (3) **Max pig OD: the card's 5.25" stands and the issued SOP/diagram stay as-is** — project is closed and the vault record carrying the correct figure is sufficient (Jesse: "No need to remake documents, as long as our records show the correct data for errors on past projects"). The card now states the error as a logged past-project defect with a reissue condition attached rather than an open action. Project marked complete throughout. Still open: the DSP25123 rate sheet's $35/hr Filtration Standby row and the radiant footage that does not multiply out. Revert path: git revert this vault commit. | Session: ingest — Exxon Baytown F-901 |
| 2026-07-26 | 02-facilities/ExxonMobil/Baytown-TX/{DSP25123.md,_facility.md}; 04-knowledge/concepts/rfq-intake-protocol.md | **[gated] DSP25123's $35/hr Filtration Standby ruled a transcription artifact — $150/hr governs (Jesse).** The F-901 sheet was built from the F-802 template and a deleted Support Unit row pulled its $35 up one cell, taking the PM row with it. $200 filtration / $150 standby is the pattern on [[DSP25084]], [[DSP26039]], and P66 Ponca City; $35 appears as a standby rate exactly once. No invoice exposure — filtration was declined before execution — and no workbook to repair, since only the Rev 1/Rev 2 PDFs exist for DSP25123. **Correction of an earlier claim this session: DSP25123 is not the most recent Baytown rate sheet.** [[DSP26039]] was submitted 2026-04-07, one day later, and carries the correct $150 standby, $30 support unit, and $94.75 PM — so an estimator reaching for current Baytown rates lands on the right sheet and the reuse exposure I flagged was overstated. **Support Unit stays unresolved by design:** $30/hr is the house standard and what DSP26039 carries, $35/hr is what the awarded DSP25084 carried, and per Jesse the site is not one buyer — Baytown bids run through different groups and divisions with different competitive positions, so $30 is right for most bids but is not "the Baytown rate." Replaced the facility's single quote-scoped rates block with a three-quote `## Rate History` table plus a House-standard column; the old block was not wrong (it was labeled with its source quote per the intake protocol), just one column wide — widening it is what exposed the bad row. Recorded the design constraint on the existing rate-history-rollup trigger in [[rfq-intake-protocol]] rather than opening a duplicate thread: the rollup must show the spread, never collapse a facility to one rate, and the group/division dimension that explains the spread **does not exist as data** — it has to be captured at intake or not at all. Quote-note count now 7 of the ~12 trigger. Also flagged unreconciled: 4×3 pump billed $1,016/shift on DSP25084/25123 vs $85/hr on DSP26039. Revert path: git revert this vault commit. | Session: ingest — Exxon Baytown F-901 |
| 2026-07-26 | 04-knowledge/concepts/quote-lifecycle.md; 02-facilities/ExxonMobil/Baytown-TX/{DSP25084.md,DSP25123.md,DSP26039.md,_facility.md} | **Contract-type model sharpened and its 2026-07-19 frontmatter decision finally backfilled — plus a self-correction.** Jesse supplied what actually drives rate divergence: **contract term**, not issuing group. A long-term maintenance agreement (≈3 yr) sets base rates that any scope inside the term inherits; a short-form scope contract sets rates for one identified scope and **ends when that scope completes**, so its rates expire with it. `lump sum` is a `billing-basis` value, not a contract type — it describes how the customer is billed, orthogonal to term, and cost is still tracked internally via the ticket breakdown even when the invoice is one line. **Self-correction: I proposed adding a group/division field last turn — [[2026-07-19-rate-model-grain-review]] proposal D already considered and rejected exactly that, on Jesse's own counter that contract type is the discriminator and department only correlates.** Recon before proposing would have caught it. **The real gap was implementation:** the three fields were adopted 2026-07-19 but applied to 1 of 7 quote notes (DSP26080, itself missing `contract-type`). Backfilled the three with source evidence — DSP25084 (`short-form scope contract` / T&M, mob-demob waived), DSP25123 (`spot PO` — ExxonMobil POs carry a literal `Lump Sum, T&M, or Spot:` field, so the vocabulary came from the bid instruction as the spec requires; billing-basis records that it was **quoted T&M but invoiced as one fixed-price line**, a quoted-vs-billed divergence that would otherwise be invisible), DSP26039 (`contract-type` left blank — bid instructions not at hand, and blank beats a guess). DSP24005, DSP26030, DSP26058 still empty and noted as such. **Reframed the Baytown rate table again, harder: those rates are expired, not available** — two of three contracts are over, so the table is negotiation history, not a schedule, and nothing in it can be quoted from. Revert path: git revert this vault commit. | Session: ingest — Exxon Baytown F-901 |
| 2026-07-26 | 04-knowledge/concepts/{rfq-intake-protocol.md,quote-lifecycle.md}; 06-insights/2026-07-19-rate-model-grain-review.md; 02-facilities/ExxonMobil/Baytown-TX/_facility.md | **Rate model corrected to "uniform by intent, divergent by exception" — and the contract-note trigger re-confirmed unfired.** Jesse 2026-07-26: USADeBusk prefers to run the same rates at every facility; the house standard is the *intended* rate, not just a starting point. Two things pull a quote off it — a short-form contract freezing an older figure, and competitive pressure on a contested RFQ. **This corrects a paragraph I wrote earlier the same session** attributing the spread to different groups and divisions issuing bids at one site; Jesse walked that back, and proposal D of [[2026-07-19-rate-model-grain-review]] had already rejected department as the discriminator a week earlier. The superseded reading is recorded in place so it isn't re-derived a third time. Consequence for the deferred rate-history rollup: a cell differing from the house standard is a **flag**, not an equally-valid alternative, and the genuinely missing capture is *why* a rate diverged — competitive cuts are the main cause, nothing records them, and it cannot be reconstructed after the bid. **Contract-note type (proposal C) re-confirmed rejected:** no live long-term agreement on Jesse's accounts, same answer twice a week apart, now recorded on the review note so the next rate discussion doesn't re-litigate it. Baytown facility framing softened accordingly — divergence there reads as a flag, not as a site-specific pricing regime. Revert path: git revert this vault commit. | Session: ingest — Exxon Baytown F-901 |
| 2026-07-26 | tools/pig_usage_rollup.py (new); 04-knowledge/pig-usage-rollup.md (new, generated); 00-inbox/{idea-pig-actuals-maturation,idea-pig-load-list-generator}.md; 06-insights/{2026-07-26-idea-research-pig-actuals-maturation,2026-07-22-idea-research-pig-load-list-generator}.md | **[gated] Built the pig-usage rollup — the second dimension to get the `estimating_rollup.py` treatment — and closed two idea seeds with one decision.** Jesse asked whether the vault should have a place to analyze cross-cutting stats rather than keeping everything per-card. Answer: yes for physical relationships, no for rates — pig usage and duration against footage/bore/condition aggregate honestly, while rates are negotiated, expire with their contract, and are uniform by intent, so a mean rate would describe a policy that does not exist. One rollup per dimension, never a combined surface, because mixing quoted with actual and physical with negotiated is how bad inferences get made. `tools/pig_usage_rollup.py` forks `estimating_rollup.py` (reusing its parsers) over `## Pig Specifications`: **78 job-sourced rows, 14 cards, 4 jobs.** Enforces the Source-column actuals wall (8 DSP-sourced quoted rows and 5 unsourced rows excluded, both reported), suppresses `ft / pig` on combined-heaters pig rows so a multi-heater total is never divided by one heater, and **joins Condition from Task Durations on Job #** — which delivers condition segmentation without the Lane 4 card-schema change that the `Condition`-column half of the seed is still parked on. **Deliberately not built (Jesse): any pooled or shrinkage ft-per-pig rate.** Raw counts and ranges only, no mean anywhere. **What the first run found:** condition separates cleanly (crash 15–43 ft/pig vs routine 41–212) while bore does not order at all (6.065" alone spans 43–212) — **but the two are confounded**, since the only crash points are H-19/H-20 and both sit at 3.068" bore, so the apparent bore effect and the crash effect are the same two rows counted twice. Untangling needs a crash job at a large bore or a routine job at a small one; that is the real gate on any rate model and it is a data gate, not a method gate. Also surfaced 3 Honeycomb rows carrying tool *length* (76"/84"/104") in the pig-OD Size column. Closed [[idea-pig-actuals-maturation]] and [[idea-pig-load-list-generator]] together as the research advised, so the shared script was not built twice; the pig *load list* generator itself and the Pig Specifications `Condition` column both remain unbuilt on their own terms. Revert path: git revert this vault commit; delete tools/pig_usage_rollup.py and 04-knowledge/pig-usage-rollup.md. | Session: ingest — Exxon Baytown F-901 |
| 2026-07-26 | 00-inbox/{idea-vault-stats-layer,2026-07-26-baytown-quote-loose-ends}.md | **Vault-wide stats layer deferred to a dedicated session — not rejected — and the reasoning error that nearly killed it recorded.** Jesse asked whether the vault should expose overall stats across every dimension (all jobs and per facility: equipment/TriMax, tube geometry, pigs by size against tube size and footage, filter press, smart-pig vs not, multi-ID vs single-ID, task stats), noting that none of this is tracked anywhere in the field, so the vault is the first consolidation of it. **The durable finding is a distinction I got wrong in session:** descriptive and validation stats work fine on sparse data, predictive or fitted rates do not — validation needs one distribution and one outlier, not volume. I let a caution about modelling thin data gate the entire descriptive idea and wrote a scope-gated plan Jesse correctly read as pushback. His counter-framing is the stronger one and is now the seed's centre: the primary job of a stats surface is **checking claims and data that do not make sense**, and on that use the evidence is already strong — five real defects were caught this session, every one the same shape of a value out of family with its distribution ($35 filtration standby · a 5.5" pig OD instructed against a 5.043" ID · F-901 radiant footage that doesn't multiply out · Honeycomb tool lengths in a pig-diameter column · `A335 Gr P9` vs `A-335 P9` as one alloy written twice), each found by manual cross-reading a stats view would have made instant. **Nothing built:** Jesse's call that automation has been pushed ahead of system and data maturity, that the system and data should be built up first, and that the current discrete-task working mode (drawings → heater card; job report) is the right altitude for now. Coverage baseline measured and captured for the next session — 32 heater cards with Tube Geometry 32/32 (richest structured asset in the vault, entirely unqueried), Config Rollup 32/32, Job Options 32/32, Task Durations 17/32, Pig Specifications 23/32; filter press hours, specific TriMax unit, and flow test results are prose-only and would need Lane 4 capture fields before any stat exists. Filed as [[idea-vault-stats-layer]] with no gate, so the next session can pick it up directly. Baytown loose ends filed separately. | Session: ingest — Exxon Baytown F-901 |
| 2026-07-27 | OneDrive estate (108 facility folders regrouped under 47 company parents); 04-knowledge/concepts/rfq-intake-protocol.md; 13 notes re-pointed (02-facilities/ExxonMobil/Baytown-TX/**, HF-Sinclair/Artesia-NM/**, Marathon/Garyville-LA/DSP26058, Westlake South/Westlake-LA/**, 00-inbox/2026-07-23-three-dead-source-pointers, archive/2026-06-26-cnd25004-routing-review) | **[gated] Restructured the OneDrive facility estate from one flat list into a two-tier `Facilities\<Client>\<Client City ST>\` tree, and re-pointed every vault path it moved.** The estate was 108 folders in one alphabetical wall, so a multi-site customer was only adjacent by accident of spelling — `Holly El Dorado KS`, `Sinclair Casper 2021`, and `HF Sinclair Tulsa OK` are one customer sitting in three places. Grouping is by **current corporate owner where that merges folders** (HF Sinclair absorbs the four Sinclair and two Holly folders; `Tesoro Mandan ND`→Marathon; `BP Husky Toledo OH`→Cenovus; `PEMEX-Shell Deer Park TX`→Pemex; Coffeyville+Wynnewood→CVR Energy), keeping the existing brand where a rebrand would only rename a lone parent. Every company got a parent including the 27 single-site clients, so the tree is uniformly two-deep; the two `_Non-US_` folders lost their sort-prefix. Folder names and all subfolder contents are untouched — only the tier above them changed. **Executed from a 108-row manifest with a pre-move byte inventory**; verification gate was per-folder file-count *and* byte-sum equality, which came back 4,023 files / 5,341,009,387 bytes and 0 discrepancies. Four folders (`ExxonMobil Baytown TX`, `Flint Hills Corpus Christi TX`, `Formosa Point Comfort TX`, `PBF Toledo OH`) hit OneDrive sync locks mid-move and PowerShell fell back to copy+delete, splitting their contents across source and destination; recovered by a file-level atomic-rename merge (0 files stuck) rather than a retry of the directory move. 21 recorded pointers across 13 notes rewritten from the same manifest, NBSP-tolerant per the DSP26080 lesson — **POINTER-DEAD now 0, lint exit 0**. [[rfq-intake-protocol]] amended on three points: the canonical-store path gains the `<Client>` tier, the "do not retrofit" rule is narrowed to *naming* hygiene (a bounded structural move terminates; per-folder name policing does not), and the client-spelling rule now governs both tiers plus a new group-by-current-owner rule. **Not merged, deliberately:** `Valero McKee TX`/`Valero, Sunray` and `Sinclair Artesia NM`/`HF Sinclair-Navajo Artesia NM` look like the same physical plants under two folder names — the restructure files them side by side so the duplication is visible, but merging them is a content decision left to a session that can verify it. `PES Philidelphia PA` (sic) and `Marathon Kenai AL` (Kenai is AK) flagged, not renamed. Revert path: reverse the manifest at `scratchpad/mapping.csv` (child back to `Facilities\<OldName>`, then remove the 47 empty parents); vault edits restore from `scratchpad/vault-backup/` — **file-level restore, not `git checkout`**, since the vault carried unrelated uncommitted work in two of the touched files. | Session: facilities estate restructure |
| 2026-07-27 | OneDrive estate (3 duplicate folders merged, 2 renamed); 02-facilities/Westlake-Chemical/Westlake-LA/** (moved from Westlake South/, 3 notes); 02-facilities/_directory.md; INDEX.md (regen) | **[gated] Merged three duplicate facility folders, corrected two misspelled folder names, and filed Westlake South under a Westlake Chemical company tier.** Follow-on to the same-day estate restructure, which deliberately left suspected duplicates side by side rather than merging them unverified. All three confirmed from folder contents, not from company knowledge: **`Valero, Sunray` = `Valero McKee TX`** (both carry the 210-H-101 / 0210-H-101 vacuum heater; Sunray is the 2002-2012 Eveready/Clean Harbors archive, McKee the 2018-2025 record — the McKee refinery is at Sunray TX); **`Sinclair Artesia NM` = `HF Sinclair-Navajo Artesia NM`** (the 3-file folder's only subfolder is named `RE_ HF Sinclair Artesia` and holds `H-19 Pigging Dwgs.pdf`, and USA26038 at the main folder covers H19/H20); **`Sinclair Wyoming WY` = `Sinclair Rawlins WY`** (Jesse confirmed; the Wyoming folder's `Reference\` holds `USADebusk Sinclair Rawlins Job Plan` — one refinery, Wyoming = closed 2024 coker 28HT-003/USA24063, Rawlins = live DSP25142 HT-104). Absorbed content sits in a labeled subfolder under the survivor's `_History\` so provenance stays visible and each merge reverses by moving one folder back. Executed with **file-level atomic moves, never a directory `Move-Item`** — the phase-1 lesson, since a directory move that hits a OneDrive cloud-filter lock silently falls back to copy+delete and splits the folder. Gate was per-folder file count *and* byte sum: **45f/40,979,148b, 61f/82,747,828b, 12f/21,073,795b — all three exact, 0 files stuck.** Renames (contents untouched, 0 vault pointers each): `PES Philidelphia PA`->`PES Philadelphia PA` (the folder's own worksheet spells it correctly; only the quotation filename carried the typo) and `Marathon Kenai AL`->`Marathon Kenai AK` (Kenai is Alaska). Estate now 47 parents / 105 facility folders. `Sinclair Casper 2021`, `Holly El Dorado KS`, `Holly Woods Cross 1-2021` and `HF Sinclair Tulsa OK` were checked and are genuinely distinct sites — left alone. **Westlake South** moved to `02-facilities/Westlake-Chemical/Westlake-LA/` via `git mv`; `client:` stays **Westlake South** (the name the customer rep signs with) and the `_facility.md` naming block now records the two-tier arrangement explicitly so a later session reads it as intentional rather than drift. **Unrelated find:** the run surfaced 2 new POINTER-DEAD on `DSP26095`/`H-101` — both pointed at `Desktop\Westlake South TE2 2026 Sept\`, the known-temporary working copy those notes had themselves flagged as "will go dead"; Jesse moved it into the estate the same day and it did. Both re-pointed to the canonical estate path and checked for the U+00A0 gotcha (none). Lint back to the phase-1 baseline: **0 errors, 36 warnings, 0 POINTER-DEAD, DEAD-LINK unchanged at 12.** Revert path: `git revert` this commit for the vault side; for the estate, move each `_History\<label>\` folder back out to a top-level facility folder and rename the two folders back. | Session: facilities estate restructure (phase 2 — merges + close-out) |
| 2026-07-27 | 102 vault .md files (01-context/company-context.md, 02-facilities/**, 04-knowledge/**, 00-inbox/**, 07-llms/**, templates/**, CLAUDE.md, Identity.md); ~/.claude/skills/** (23 files) + ~/.claude/CLAUDE.md | **House spelling set to `Trimax` and `USADebusk` — this reverses the 2026-07-12 skill-drift ruling, which had adjudicated the capitals the other way.** Jesse's call: the mid-word capitals were a persistent irritant, and `TriMax` is USADebusk's own unit name rather than a vendor brand, so nothing factual rides on it. **The spaced form `USA DeBusk` is deliberately unchanged** (5 vault / 161 config occurrences) — `DeBusk` is the family surname, so `USA DeBusk, LLC` on proposal signature blocks and T&C lines stays correctly spelled; lowercasing it there would misspell the legal entity on customer-facing documents. Both authorities were rewritten in the same pass so the change survives the next drift run: `01-context/company-context.md` Identity, and `usadebusk-core/SKILL.md` Company Identity plus the naming-lock row, which now carries the old capitalized spellings as **dead strings**, using the same mechanism that bans `dual-pumper`. **Two classes of literal were protected and survive byte-identical:** the real on-disk OneDrive facilities path (20 occurrences; on-disk casing confirmed via PowerShell, it genuinely carries the capital B) and five real document filenames containing the capitalized unit name (`Cummins TriMax.pdf`, the Valero / Exxon / Ponca City Ticket Breakdown workbooks, `DSP# 25084 ... 2 TriMax.pdf`) — rewriting those would make the vault assert filenames that do not exist. The one Ticket-Breakdown occurrence that *was* changed is the naming **convention** in `concepts/field-operations.md`, which governs files not yet created. Excluded as historical record, per Jesse: `change-log.md` itself, `06-insights/**`, `~/.claude/regression/**`. Out of scope by file type: all .docx/.pdf/.xlsx and the two rendered .html. Left alone: the git-guard hook, which matches a real all-caps directory path. **Verification:** every one of 358 vault and 139 config changed lines was machine-checked to differ from its original by the two substitutions alone; lint holds the recorded baseline exactly (0 errors, 36 warnings, 0 POINTER-DEAD, 12 DEAD-LINK). `regression/frozen/` now differs from live skills on spelling only — a future replay diff showing the two spellings is expected noise, not drift. Not a substantive edit under the 2026-07-25 two-trigger rule (no rule and no number changed), so no replay was run. `02-facilities/ExxonMobil/Baytown-TX/F-501.md` was **deliberately left out of this commit** — it carries an unrelated uncommitted change from an earlier session (SOP REV 1 status reversed from "not an open action" to "superseded, REV 2 required") that is Jesse's to review. Revert path: revert this vault commit plus the matching config commit. | Jesse, in session: "fix the random capital letter so it stops appearing this way" |
| 2026-07-27 | 04-knowledge/_canonical-heater-card.md; templates/_heater-template.md; 39 heater cards in 02-facilities/**; ~/.claude/skills/{usadebusk-core,usadebusk-vault-ingest}/SKILL.md | **Tube Geometry loses its `Notes` column — 12 columns down to 11, and the prose moves to a keyed block under the table.** Jesse's call: the trailing Notes column pushed the table past the render width, so it scrolled sideways and squeezed every numeric column into unreadability. **The content was never the problem — it is load-bearing provenance** ("Confirmed tube count, revised twice: BOM return-bend proxy gave ~60-62, first elevation read gave 44, final read gives 32"), so all 75 notes across 39 cards were **relocated, not deleted**. New convention: a `**Tube geometry notes.**` block directly beneath the table, one paragraph per row, led by the Section name exactly as written in column 1 — which preserves the row↔note link that a bottom-of-card dump would lose. **Deliberately not Field Notes**, and this corrected an earlier misreading in-session: `b689150` moved job-execution prose there and that was right, but Field Notes is explicitly the *per-job* operational record keyed to a job number, whereas tube-geometry notes are card-level facts about the tubing. Scope was held to Tube Geometry (41 of 82 over-wide rows); the remaining 48 are 2-column key/value tables that wrap fine, plus `02-facilities/_directory.md`, whose 765-char Facilities cells are a different problem — an index table used as a narrative store — and are untouched. **Verified by word-multiset audit against HEAD across all 41 changed notes: the only word lost anywhere is the header word `Notes`, 39 times, one per card. Zero note prose lost.** That is the whole point of the method — the text was moved verbatim rather than rewritten, precisely so this check could prove it. One `—` placeholder cell on `H-311` Radiant was dropped as contentless. Over-wide rows in `02-facilities` fell 82 → 48; lint holds baseline (0 errors, 36 warnings). Enforcement: `usadebusk-core` records a Tube Geometry header ending in `\| Notes \|` as a **dead string**, the same mechanism that bans `dual-pumper`, so the skill-drift loop flags a reversion instead of causing one. Tooling added: `tools/audit_worktree.py` (working-tree twin of `tools/audit_commit.py`). No lint lock yet — `DURATIONS-HEADER` is the precedent if one is wanted. Revert path: `git revert` this vault commit + the matching config commit. | Jesse, in session: the Notes column "makes the table absurdly large visually" |
| 2026-07-28 | tools/vault_lint.py; tools/audit_commit.py; tools/fixtures/word-delta/{before,after}.md; tools/fixtures/README.md; 02-facilities/ExxonMobil/Baytown-TX/{F-301,F-371A}.md; 00-inbox/2026-07-27-formatting-sessions-rewrite-content.md; ~/.claude/hooks/usadebusk-word-delta-guard.mjs (+test); ~/.claude/settings.json | **Swept vault history for formatting commits that silently rewrote content, and productionized the check that finds them.** 42 commits audited by stated scope (formatting / reflow / lint / schema-normalization / layout-migration / cleanup) out of 252. One real finding: `62cc044` dropped F-301's and F-371A's only Pig History row during the Task Durations schema migration; the concurrency fact had self-healed by HEAD, but the Foam → Smart Pig sequence and the DSP26039 estimate had not — both restored into Notes from the pre-canonical archive copies. Restoring them surfaced a second-order error the archive row would have propagated: **24 hrs pig + 8 hrs smart pig is the paired line item covering both heaters on one Trimax, not per heater** (verified against DSP26039); both cards now state that explicitly. Everything else triaged benign — frontmatter metallurgy that survived in the geometry table, confidence tiers dropped from later Jesse-verified cards, F-802's 134→85 Total being Stand-By moving out as designed. **Productionized as option (a):** `vault_lint.py --staged` gains rule WORD-DELTA (self-test now 13 rules, fixture is a before/after reflow pair), `audit_commit.py` reduced to the by-sha front door over the same shared comparison, and a third PreToolUse hook runs it unprompted before agent commits — warn-only via `additionalContext`, never blocks, fails open, escape hatch `word-delta-ok`. **The plan's diff-shape gate was measured and rejected:** any-loss would fire on 70% of the last 120 commits and numbers/rulings-only on 50%, because this vault legitimately rewrites numbers; and the churn half was backwards, since the F-501 edit had zero formatting changes. Gating on the commit message's declared scope gives 7% while still catching `b689150`. Generalizable: an intent-vs-effect check needs the stated intent as input — it cannot be recovered from the artifact alone. Revert path: `git revert` this commit and drop the third hook entry from settings.json; the rule is inert outside `--staged`. | Session: formatting-commit content sweep (Opus 5), Jesse-approved both decisions in-session |
| 2026-07-28 | scheduled task `vault-capture-loop` (config, not vault git); tools/vault_health.py; 04-knowledge/vault-capture-loop-spec.md; 7 seeds moved 00-inbox/ → archive/; 06-insights/2026-07-28-deferred-operational-prestaging-review.md (new) | **Capture loop moved from weekly to daily, granted a terminal-seed sweep, and the actual throughput bottleneck identified and written up rather than built.** Jesse asked what could safely be sped up without his interaction. Recon found the bottleneck is **not cadence**: `.capture-state.json` shows transcript dispositions running **41 `defer` to 9 `harvested`**, 16 inbox notes carry the `vault-loop:` no-home marker, and the decision queue sits at **0 of its cap of 10** with 1 review note open — the analysis machinery is idle while the input pile grows, because the only consumer of deferred operational items is the on-demand Review Loop, which fires only when Jesse says the words. **Cadence change (done):** `0 8 * * 1` → `0 8 * * *`, cutting worst-case inbox latency 7 d → 1 d; Lane 1 scope means it cannot touch operational content by construction. Monitoring follows the idea loop's proven daily shape — ledger staleness tightened **14 d → 3 d** (daily firing should never be older), git heartbeat left **monitoring-grade at 7 d/14 d** because a no-op day commits nothing and would otherwise false-FAIL. **Terminal-Seed Sweep (done + standing authority granted):** 7 finished seeds archived on an exact status allowlist (`executed`/`resolved`/`complete`/`superseded`/`spec-complete`); inbox 43 → 36. **`researched` is deliberately excluded** — it means the research loop finished but *Jesse has not decided*, so sweeping it would silently discard a pending decision. **Seeds are moved, never renamed:** 19 inbound wikilinks exist across the 7, and `vault_lint.py:259` includes `archive/` in its link-resolution set deliberately, so a plain move keeps every link green while a rename would break all 19. **Pre-staging proposal filed, not built:** a scheduled propose-only pass that drafts review notes + decision-queue rows for deferred items, bounded by the existing 10-row cap, one item per run. The governing distinction: **deciding is Lane 4 and stays with Jesse; preparing the decision is not.** Lane 4's unattended behavior (add a decision-queue row) is already specified policy — nothing exercises it on a schedule. Left open on three checkboxes; **explicitly not erosion of the Lane 4 gate.** **Operational lesson: changing the cron re-armed the task and fired a catch-up run immediately**, so a capture run executed concurrently with 7 staged renames out of `00-inbox/` — one of the loop's own commit paths. Waited it out rather than racing the index; the spec's "commit only the loop's own touched paths" rule held and `c15d3d9` took none of this session's work. Daily at 08:09 sits inside working hours and will recur — an earlier fire time is unresolved and left to Jesse. Revert path: `git revert` this commit, and reset the cron to `0 8 * * 1` via the scheduled-tasks tool (the schedule lives outside vault git, so the revert does not restore it). | Jesse, in session: "are there things that can be done to speed up the processes that are safe and don't require my immediate interaction?" |
| 2026-07-28 | scheduled task `vault-capture-loop` (config, not vault git); 04-knowledge/vault-capture-loop-spec.md | **Capture loop's daily fire time moved 08:00 → 05:00, out of working hours.** Refinement of the same-day move to daily, on Jesse's call. The 08:00 setting put the loop squarely inside his working hours and its very first run proved the hazard — it executed concurrently with a live session holding staged renames in `00-inbox/`, one of the loop's own commit paths. Nothing was lost (the loop's "commit only the loop's own touched paths" rule held, and it logged that it saw a concurrent session and left it alone), but the collision recurs daily by construction at 08:00. **Also refined the re-arm model, which the first change got wrong:** updating `cronExpression` does not fire a catch-up run merely because today's clock time has passed — it fires when the new schedule implies an occurrence the task has **not already satisfied**. Evidence, same task same day: `0 8 * * 1` → `0 8 * * *` at ~10:56 fired within seconds (last run was Monday, so today's 08:00 was genuinely missed), while `0 8 * * *` → `0 5 * * *` at ~11:15 did **not** fire despite 05:00 being long past, because the task had already run that day. Next run books for 2026-07-29 05:09. Revert path: reset the cron to `0 8 * * *` via the scheduled-tasks tool (the schedule lives outside vault git, so `git revert` does not restore it). | Jesse, in session: "5:00 is good." |
| 2026-07-28 | 04-knowledge/vault-prestaging-loop-spec.md (new); ~/.claude/scheduled-tasks/vault-prestaging-loop/SKILL.md (new, config repo); tools/vault_health.py; 01-context/system-workflow-reference.md; 06-insights/2026-07-28-deferred-operational-prestaging-review.md; 50-dashboards/health.md | **Sixth loop built — the Vault Pre-Staging Loop — closing the throughput gap between "capture defers it" and "Jesse remembers to run the review loop." A, B and C approved without edits.** The measured gap: 41 `defer` transcript dispositions against 9 `harvested`, 16 inbox notes carrying the defer marker, and the decision queue sitting at **0 of its cap of 10**. The only consumer of a deferred operational item was a loop that fires when Jesse says a phrase out loud, so the analysis machinery sat idle while the input pile grew. **The design turns on one distinction: deciding is Lane 4 and stays with Jesse; preparing the decision is not.** The loop reads inbox items carrying `<!-- vault-loop: -->`, drafts one review note in `06-insights/` with real cited Source Material, appends one `decision-queue.md` row, and marks the item `<!-- vault-prestaged: -->` so it is never reprocessed. It writes **no** operational content ever. **Design fork resolved under Lane 2 (mine, not Jesse's):** proposal A allowed either a new loop or a phase bolted onto capture; built as a **separate loop**, because adding proposal-writing to a Lane 1 mechanical-filing job would widen capture's blast radius, couple ingestion to proposal-generation, and mean a pre-staging failure takes ingestion down with it. Separate task carries its own heartbeat, cadence and kill switch. **Two hard bounds, neither of them new machinery:** the existing 10-row queue cap is the throttle (at cap the loop no-ops before even reading the inbox, so a runaway fills ten slots and halts), and one item per run oldest-first matches the Idea Research Loop's proven shape — at the current pile of 16 the backlog clears in about two weeks unattended. **The skip disposition is the anti-ceremony guard** raised in the proposal's own Risks section: an item that is an execution correction (`2026-07-24-dsp26085-submitted-wrong-quote-number` is the worked example — a fix to make, not a decision to weigh) or already covered gets marked and reported as a to-do, with no review note and no queue row, because sixteen mediocre review notes are worse than sixteen raw inbox notes when each carries an implied claim of analysis. **Pre-staged notes are marked as unreviewed inference** — `review_type: pre-staged`, `source_authority: inferred`, `status: open` are mandatory frontmatter, so a future session cannot mistake a machine-drafted Source Material table for settled vault truth. **Scheduled 06:00 daily, deliberately one hour behind capture's 05:00**, since capture is what applies the defer markers this loop reads; both sit before working hours after the 08:00 collision earlier the same day. Health tracks it at a monitoring-grade 30 d heartbeat with 3 d ledger staleness, because it is silent whenever the queue is full or the pile is empty. **Explicitly out of scope and worth re-reading with suspicion if ever proposed:** letting this loop apply its own proposals. It removes the analysis wait in front of the Lane 4 gate; it does not touch the gate. Revert path: `git revert` this commit, then delete the `vault-prestaging-loop` scheduled task (it lives outside vault git, so the revert does not remove it). | Jesse, in session: approved proposals A, B and C on [[2026-07-28-deferred-operational-prestaging-review]] |
| 2026-07-28 | tools/vault_lint.py; 06-insights/2026-07-28-prestaged-stale-editor-buffer-guard.md; 50-dashboards/decision-queue.md | **WORD-DELTA extended to the uncommitted working tree — closing the exact gap the rule was invented for but could not see. DQ-003 closed: A and B approved, C dropped.** First decision to come out of the new pre-staging loop, and the first proof the loop earns its place. **Jesse initially approved all three options; flagged as contradictory** — A and B build mechanisms, C says build none — and re-answered as A+B. Worth recording as a pattern: the previous note's A/B/C were complementary parts of one design, these were competing alternatives, and the checkbox shape is identical either way. **A was applied in its narrowed form, not as written.** The loop proposed building a content-vs-formatting diff gate; session review found one had shipped the day before (`d65621c`: rule `WORD-DELTA` + `usadebusk-word-delta-guard.mjs`). It was **not** a duplicate, because `vault_lint.py`'s own header states WORD-DELTA "is a staged-diff rule, not a tree rule," and the 2026-07-19 B-101 incident was an **uncommitted, never-staged** file — nothing staged, nothing to compare, no finding. So the real change was one mode, not one tool: `check_word_delta(root, mode)` now takes `--worktree`, listing via `git diff HEAD` and reading the after-text off disk. **Verified positively rather than assumed:** a simulated silent deletion in an uncommitted note fired the rule and named all 9 lost words, the file restored clean afterward, self-test holds at 13/13, and whitespace-only churn correctly does not fire — which is the whole point, since word-multiset comparison is blind to table padding. That last property means **A structurally defeats the camouflage B was meant to remove**, so B is now noise-reduction rather than a safety control. **B was approved but deliberately not applied.** There is no discrete "table auto-format" setting — the reformatting is Live Preview's table editor, so B necessarily means switching Default editing mode to Source mode, a broader UX change than the proposal implied. The `.obsidian/app.json` key was **not guessed at**: `app.json` stores only non-default values, nothing in the vault or config records the correct key, and a wrong key fails silently in a way that would leave Jesse believing B was done. Handed over as a UI toggle and carried as a `revisit-trigger:` on the source note so the health dashboard surfaces it. Revert path: `git revert` this commit; `--worktree` is inert unless passed, and `--staged` behaviour is byte-unchanged. | Jesse, in session: "A. approved. B. approved. C. approved." → clarified to A+B |
| 2026-07-28 | .obsidian/app.json; 06-insights/2026-07-28-prestaged-stale-editor-buffer-guard.md | **Proposal B applied — Obsidian Live Preview off, Source mode on — and the switch immediately exposed a blind spot in the guard shipped an hour earlier.** Jesse selected Source mode; Obsidian wrote `"livePreview": false` to `.obsidian/app.json`, which **is tracked in git**. That empirically confirms the key deliberately not guessed at earlier — the guess would have been right, but a wrong key fails silently and the cost of being wrong was Jesse believing B was done when it wasn't. `revisit-trigger:` removed per the registry's fire → act → remove convention. **The blind spot:** switching modes left three notes dirty. Two were formatting-only, and one of those is instructive on its own — `git diff -w` does **not** neutralise Obsidian's table-separator padding, because the padding is made of dashes, not whitespace, so `-w` still shows 13 changed lines of pure noise. The third, `06-insights/2026-06-26-knowledge-system-pilot-review.md`, carried a genuine `- [ ] → - [x]` flip on "Approve CND25004.md as first pilot item", on a note whose own status is `superseded`. Almost certainly a stray click, since in Live Preview clicking a checkbox toggles it instantly. **`--worktree` correctly reported no word loss, and that is the problem: a checked box is an *addition*, and WORD-DELTA reports only losses by design.** So the vault now has a guard against silent content *reversion* and no guard against a silently *recorded decision* — arguably the more dangerous of the two in a vault whose entire governance model runs on decision checkboxes. Left uncommitted pending Jesse's call on the flip itself, and raised as a candidate rule extension rather than built unasked (the lint suite's standing rule is no fixture, no rule). | Jesse, in session: "source mode selected in obsidian" |
| 2026-07-28 | tools/vault_lint.py; tools/fixtures/checkbox-delta/{before,after}.md (new); 06-insights/2026-06-26-knowledge-system-pilot-review.md (reverted) | **New lint rule CHECKBOX-DELTA — the vault can now detect a decision it never made. Rule count 13 → 14.** Closes the blind spot found an hour earlier: WORD-DELTA reports only *losses*, and ticking a box is an *addition*, so a stray Live Preview click that approved `CND25004.md` on an already-`superseded` note passed every guard clean, including the `--worktree` mode shipped the same day. In a vault whose governance runs entirely on decision checkboxes, a silently *recorded* decision is at least as dangerous as a silently reverted sentence. **The gate is the BEFORE status, not the after** — closing a review note legitimately means ticking its boxes and setting `status: resolved` in one edit, which is the workflow; a note that was *already* `resolved` or `superseded` changing its mind is the anomaly. Gating on the after-status would have fired on every legitimate close-out, which is the 70%-fire-rate mistake the WORD-DELTA design had already learned to reject. **Comparison is positional, not label-keyed**, because proposal A/B/C notes carry three identical `Approved` lines and a label map collapses them; when the checkbox structure itself changes, it falls back to reporting tick counts. `check_word_delta` renamed `check_diff_rules`, since it now hosts two rules over one pair of git reads. **Verified against the real incident, not just the fixture:** re-creating the stray click made the rule fire and name the exact box, with WORD-DELTA correctly reporting 0 words lost — proving the two rules cover different failures rather than one shadowing the other. **The self-test caught a defect in my own fixture** (before/after carried different prose, so words genuinely left); the pair is now byte-identical except one character, and asserts *both* that CHECKBOX-DELTA fires and that WORD-DELTA stays silent. Self-test 14/14, lint baseline holds at 0 errors / 37 warnings, `--staged` byte-unchanged. **Known gap, deliberately not closed:** the PreToolUse hook filters on the literal `WORD-DELTA`, so it ignores CHECKBOX-DELTA entirely — and its presentation-only commit-message gate would be the wrong trigger anyway, since a stray click has nothing to do with formatting commits. The rule is live in `--staged`/`--worktree`; wiring it into a hook is a separate decision. The stray approval itself was reverted to `- [ ]` on Jesse's instruction. Revert path: `git revert` this commit; the rule is inert outside `--staged`/`--worktree`. | Jesse, in session: approved "Yes — resolved/superseded only" and "Revert it" |
| 2026-07-28 | `~/.claude`: hooks/usadebusk-fixture-replay-guard.{mjs,test.mjs,backtest.py} (new); settings.json; regression/frozen/f5-pig-sizing-output.md; regression/README.md; skills/usadebusk-{core,equipment,estimating}/SKILL.md; 07-llms/claude/dynamic-workflows.md (new); 07-llms/{prompt-engineering.md,claude/code.md,claude/command-reference.md} | **Fixture replay for skill edits is now mechanized, and two frozen baselines were found stale by it.** Trigger #2 in the regression README (replay a skill's fixtures after a substantive edit) was correct policy that depended on recall, and recall failed: commit `e396ddf` added the Max pig OD rounding rule on 2026-07-25, F1/F2/F6 got replays that evening, **F5 did not** — so its baseline carried a stale `5.297"` card field and a stale "rounding is a judgment call" line for three days. New PreToolUse hook warns at commit time when a staged skill has fixtures with no replay dated today. **Gate chosen by back-test over real history**, harness committed alongside so the number is re-runnable: firing on any staged SKILL.md = 100%, requiring a co-committed run = 100%, requiring a same-day run for *every* mapped fixture = **14%** — the band of the word-delta gate that survived, where a 70% gate was previously rejected. Limitation stated in the hook header rather than glossed: it cannot catch F1's shape, where a replay ran but the rule landed afterward. **F5 re-promoted twice in one session** — once for the rounding + firewater changes, then again when filling core's 5" dimension row (`5" Sch 40 \| 5.563" \| 5.047"`, previously no OD and no schedule) correctly removed judgment call J-7 and moved a diff key. **Firewater 2" hose reclassified from flag to stated requirement** (Jesse: "a requirement that we tell the customer… it keeps coming up in every other session"); the first fix over-corrected and dropped the "1½" is insufficient" rejection clause F1's diff key 10 requires, patched same-day but **unverified**. F1 and F6 re-baselining deliberately deferred — see [[2026-07-28-f1-f6-rebaseline-handoff]]. Revert path: git revert the config commits `28827d3`, `0f058dd`, `589fb1e` and vault commit `dadbda9`. | Session: Boris Cherny Opus 5 talk → instruction-density arm test |
| 2026-07-28 | `~/.claude`: skills/usadebusk-{estimating,vault-ingest,fieldpm}/; regression/frozen/ (all 5 re-cut); regression/fixtures/{f2,f3}-*.md; regression/README.md; regression/runs/claude-opus-5/ (11 new run records) | **Two Lane 4 estimating rulings, and the whole regression battery brought current.** (1) The **parallel-friction allowance is now a written 25-40% band** — it was fully open judgment, which returned 15%, 25% and 40% across three runs of the same two fixtures, worth ~$1,100 on a four-pass job and a full shift on a six-pass one. The run states where in the band it sat and why. (2) **The pipefitter adder does not apply when the RFQ is silent** — the trigger is affirmative; price the bare tier and put the +4 hr swing in open items. Rig-over now takes the 1-hr silence figure to match, which fixed a real defect: the F1 baseline had been reading the same unknown as 'not waiting' on rig-in and 'waiting' on rig-over, two adjacent lines. Also ruled operational: a **large shift-landing adjustment is a diagnostic** — a raw total in the middle third of a 12-hr band forces re-opening the build-up rather than booking the pad, and the tiebreak is the direction residual risk points, not which boundary is nearer. All six fixtures replayed, judged, patched and re-promoted; every one of the five swept was behind at least one un-replayed substantive commit. Root-caused the F2 skill-loading mystery: `disable-model-invocation: true` works as designed, and `usadebusk-fieldpm` only appeared to contradict it because its frontmatter was failing to parse (unquoted description containing ': '), silently discarding its flag, status and entire description. Fixed. | Session: F1/F6 re-baseline handoff, expanded to a full battery sweep |
| 2026-07-28 | 04-knowledge/concepts/rfq-intake-protocol.md; 02-facilities/Westlake-Chemical/Westlake-LA/DSP26095.md; 01-context/active-jobs.md; 02-facilities/ExxonMobil/Baytown-TX/F-501.md; 00-inbox/2026-07-28-pasted-emf-quote-number-second-near-miss.md (new) | **An RFQ carrying no bid instructions is a normal request shape, not a missing input** (Jesse, 2026-07-28). Plenty of customers send nothing but "give me an estimate" — no bid package, platform, due date or forms — and that is the whole request: price it and send it. Two consequences written into step 6 of the intake protocol. **The execution date is not a blocker either** — where the customer gave a window rather than a date, the exact date follows the award. And **the absence is not recorded as an open item, data gap, or thing to confirm**; an open-items list that leads with things the customer was never going to send buries the items that are genuinely open and makes a complete bid read as incomplete. The distinction that governs is silence vs. conflict: silence means the standard model applies and is priced without comment, while a *stated* term that conflicts with how USADebusk bills is what the pass exists to catch. | DSP26095 (Westlake South) had sat in the vault as `pending — not yet priced, no rates on file` with the missing commercial package listed as *blocking the quotation*, while the priced quotation and workup already existed in the bid folder. Asked what the vault's most important task was, the stale note was taken at face value and produced a wrong answer. Jesse corrected the premise; the rule is what stops it recurring. |
| 2026-07-29 | 02-facilities/ExxonMobil/Baytown-TX/{DSP26071.md,HU5A-F501-job-sheet.md}; 01-context/active-jobs.md; 06-insights/{2026-07-27-idea-research-quotation-workup-reconciliation-check.md,2026-07-28-idea-research-rollup-per-rig-coilset-grain.md}; 00-inbox/{2026-07-29-build-workup-quotation-regression-check.md,2026-07-29-syncrude-6-ft-hr-fill-flush-question.md} (new); 50-dashboards/health.md | **DSP26071.2 recorded as awarded on a PO, and the two open review notes ruled — decision queue back to zero.** (1) **PO 4411473422 / WO 20961590** received by email from the ExxonMobil Baytown rep. The PO line reads `26HU5A R501 CAT C/O – DEBUSK HYDROBLAST TM` at **$256,250.56** — a hydroblast T&M scope against the HU5A catalyst changeout, not sized to our $60,287.42 pigging quote. The session's first instinct was to raise three commercial questions (shared PO ceiling, which rate schedule governs, cost-bucket routing on a WO tagged hydroblast); **Jesse closed all three — VP-approved, one PO covers both scopes under the same agreement** — and the flags were backed out of the vault rather than left sitting as false open items, per the standing rule that a stated requirement is not a flag. Durable fact kept from the exchange: **ExxonMobil tags this heater `R501` where the vault carries it as F-501**, so returning receipts, FCOs and invoices will not match our tag. Job moved to Awarded / Pre-Execution; USA# still not issued with mob 10 days out. (2) **Quotation-vs-workup reconciliation check — approved with edits.** The recurring-vs-triggered question was already settled by the `~/.claude/regression/` precedent (replay on change, never on a calendar), agreeing with the external golden-master convention. The edit splits the build: the **post-script-edit regression half ships**, reading quotations via `python-docx` directly and never reintroducing the LibreOffice render step, while the **pre-send bid gate stays manual** until the DSP26026-style scope-narrowing rule exists — a large non-mob/demob gap needs a human rule, not a size threshold, before anything gates a submission unattended. Carried as a `revisit-trigger`. Noted for the record: the two `extract_workup.py` defects that note inherited were already fixed by config commit `677d447`, 34 minutes before the seed was even filed. (3) **Per-rig-coilset rollup re-grain — parked.** Blocked on an undecided Lane 4 schema change, not on data volume: the canonical card deliberately holds per-rig splits as freeform prose, and the three sources carrying that field disagree on what it contains (7-1-F-1 full per-coilset per-task, HF-0011 hours-by-mode only, the ingest example a per-rig total). A parser across that recreates a documented maintenance-narrative extraction failure mode for two usable data points. Bundled with the parked Pig Specifications `Condition` column on the same schema trigger. The non-gating Syncrude ~6 ft/hr fill/flush question was split out to `00-inbox/` so it does not wait on the schema decision. Both notes closed `resolved` with `revisit-trigger` set, following the 2026-07-19 rate-model precedent — review notes awaiting decision **2 → 0**, lint holds 0 errors. Revert path: `git revert` this commit. | Jesse, in session: PO email forwarded, then "This has already been approved by our VP"; then "walk me through them" on the two open review notes |
| 2026-07-29 | 02-facilities/ExxonMobil/Baytown-TX/HU5A-F501-job-sheet.md; 01-context/active-jobs.md | **Work sitting in someone else's queue is not an open item.** The F-501 USA# is the ops manager's to create when he has time, and crew assignment and badging are the same shape — Jesse brings each to the session the moment it exists. Both placeholder comment blocks on the job sheet were rewritten from "fill before mobilization" chase-framing to a statement of who owns the item and an explicit instruction not to flag it; `active-jobs.md` names the owner instead of reading as pending work. Third instance in one session of a settled or externally-owned fact being surfaced as an open item — the VP-approved PO was the first two — so the general rule was also written to memory: owned by Jesse and actionable now is an open item, owned by someone else or already ruled on above him is one factual line naming the owner, and a flag already written gets backed out rather than left to decay into false pending work. Proximity to mobilization does not convert another person's task into an escalation. | Jesse, in session: "He will do it when he has time. No need to continue flagging. I'll bring it here the moment it's created. Crew and Badging are the same." |
| 2026-07-29 | `~/.claude/skills/usadebusk-estimating/SKILL.md`; 06-insights/2026-07-27-idea-research-quotation-workup-reconciliation-check.md; 00-inbox/2026-07-29-build-workup-quotation-regression-check.md; 50-dashboards/health.md | **Quotation-vs-workup reconciliation: compare scope before dollars. Gap size is never the instrument.** Jesse's ruling, closing the DSP26026 question that had blocked the pre-send bid gate. The question as inherited — "how large a non-mob/demob gap is acceptable?" — has no answer, because DSP26026's $39,548.16 gap is correct while DSP25142's $90,090 gap is a defect and no threshold separates them. What does separate them is that both documents state scope in structured form. **Matched scope with a totals gap blocks** (nothing explains it — a dropped or mistyped line, the DSP25142 shape). **Narrower scope re-prices**: take the quotation's own hours at the workup's own rates, add its other lines, compare against the quotation's stated total; match passes with a one-line note naming the narrowing, mismatch blocks because the narrowing is hiding an error. **Wider scope blocks unconditionally** — quoting work the estimate never costed is never legitimate. DSP26026 is the worked case in the skill: workup `TA Heaters` at 12/36/12 and $153,715.40 against a submitted quotation covering the 70H1 Coker alone at 6/24/6 and $114,167.24; the hour figures disagree on their face, which is the narrowing signature. Jesse also supplied the base rate — **narrowing happens a few times a year**, so the matched-scope path is the normal one and the re-price path is the exception, which is what makes pass-with-note tolerable rather than noisy. A gate demanding confirmation on every narrowed bid would train the reviewer to click through it, the same failure the mob/demob exemption was written to fix. **Second decision, same session: the approved regression-half build was withdrawn before any code was written.** Asked "should we build it?", recon of `backtest_workup.py` found it already *is* the quotation-vs-workup reconciliation — its expected values are the submitted quotation side read from the submitted PDFs, and `check()` asserts line items, grand total, Pricing Summary box and line-sum-to-total across all three pairs. The research note had recommended the build without verifying what the existing back-test covered. Also corrected: the seed's `python-docx` live-read is the *wrong* design for the regression half (frozen expectations are what golden-master wants; a live parser can break and cannot catch both sides wrong in the same direction) and the *right* one for the pre-send gate, where no frozen expectation exists. Remaining real gap is three lines — `usadebusk-fixture-replay-guard.mjs` maps `usadebusk-estimating` to fixtures f1/f6 but nothing maps `scripts/*` edits to `backtest_workup.py`. Review note's `revisit-trigger` retired per fire → act → remove; pre-send gate now unblocked and spec'd in `00-inbox/`, not yet built. Revert path: `git revert` this vault commit plus the matching config commit. | Jesse, in session: "Should we build it?" → recon → ruled pass-with-note and "occasional — a few times a year" |
| 2026-07-29 | `~/.claude/skills/usadebusk-estimating/SKILL.md`; 02-facilities/Westlake-Chemical/Westlake-LA/DSP26095.md; 02-facilities/ExxonMobil/Baytown-TX/{DSP26071.md,HU5A-F501-job-sheet.md}; 01-context/active-jobs.md | **DSP26095 sent and reconciled clean; two standing template defaults written into the skill so they stop being reported as defects.** First bid run through the scope-before-dollars rule ruled hours earlier: all five line items, the grand total and all four Pricing Summary figures match the workup exactly, and the scope statements agree on both sides (`H-101 TE II EDC | 6 Rig-in | 24 Pig | 6 Rig-out`). Zero gap, matched-scope path, pass. **Honest limitation recorded: a zero-gap matched-scope bid does not exercise the narrowing path at all**, so the rule is validated on its main path and untested on its exception. The pass also produced three flags Jesse struck down, two of which generalize and are now skill content. **(1) The Project Manager field always reads "Travis Trenholm."** A PM cannot be assigned at bid time, so the dispatching ops manager is the permanent template placeholder on every quotation — never an assignment, never superseded, never a thing to confirm. This corrects framing already in the vault: DSP26071's note and job sheet had described the same name as *superseded*, which is what generated the flag today, and both were rewritten. **(2) An execution-plan date that differs from the RFQ's stated date is normal where the customer named a window.** DSP26095 runs 8/31–9/1 against an RFQ headed "September 7 2026" and Westlake settles the date with whoever wins. A wrong *year* — DSP26071.2's 2027 execution plan — is worth one mention; a date inside the customer's own window is not. **(3) The 1-Jul-2026 quotation header date and its 90-day validity were raised and dropped on Jesse's call for this bid.** Fourth, fifth and sixth instance in one session of a normal-by-design artifact being surfaced as a finding — the VP-approved PO and the USA#/crew/badging queue were the first three — so the general rule was consolidated into one memory rather than three. | Jesse, in session: "Ignore the date discrepancy... No need to flag anymore"; "The 'project manager' will always read 'travis trenholm'... No need to flag this anymore"; "The bid has been completed and sent." |
| 2026-07-29 | `~/.claude/hooks/usadebusk-exec-guard.{mjs,test.mjs}` (new); `~/.claude/settings.json` (529ba04); `.claude/settings.local.json` (untracked); 00-inbox/2026-07-19-auto-mode-permission-drift.md | **[gated] Arbitrary-code-execution moved from allow-list hygiene to a PreToolUse hook.** The 2026-07-19 doctor pass pruned the vault allow list 61 → 36 and set a 2026-09-19 re-check; a targeted read today found it back at **59** — roughly two rules/day, saturating in under three weeks, not two months. Three returning entries were the exact hazards that pass removed (`python -c ' *`, `python -`) plus a new `gh repo *` covering delete/create/`edit --visibility public`, so the drift is hazardous, not cosmetic. **Deny rules rejected** as the fix: they are glob matches on the command string and are documented-leaky (`git commit*` misses `git -C <path> commit`; anything denied is reachable via `python -c`). Anthropic's documented pattern for this case is a PreToolUse hook — exit 2 stops the call *before* permission rules are evaluated, so it overrides allow rules auto mode restores. New `usadebusk-exec-guard.mjs` gates `python -c` / `python -` / `node -e\|-p\|--eval` / `gh repo` lifecycle; a confirmation gate, not a ban (`# exec-ok` stands it down, matching the staged-count-guard precedent), fails open on unparseable input, 27 self-tests pass. Allow list pruned 62 → 48 (four hazards, ten dead one-offs). Auto mode **stays on** — its drift is now cosmetic. Sandboxing, the stronger OS-level layer, is unavailable: native Windows is unsupported. The list grew twice more mid-session, once adding `Bash(node -e ...)`, which the new hook now overrides. Revert path: `git revert 529ba04` in the config repo; the local allow list restores by re-approval on prompt. | Session: /doctor follow-up + power-user prior-art search |
| 2026-07-29 | 04-knowledge/vault-agent-loop-spec.md | **[default] Vault Review Loop spec reconciled against four rulings made after it was written.** The note came due (`REVIEW-OVERDUE`, `review_after: 2026-07-26`) and was the only overdue note in the vault; the review found the spec still describing a system from 2026-06-27. **(1) It contradicted the 2026-07-06 facility-data ruling** — it listed `02-facilities/` in its governance scope and blocked "edit heater-card facts" and "mark draft heater cards as reviewed" without approval, while [[knowledge-system-governance]] had moved that content to Lane 1 in full three weeks earlier. A loop firing on this spec would have manufactured review notes for work Jesse had explicitly de-ceremonied. `02-facilities/` is now read-only context here, with the ruling's own carve-out preserved (a card feeding a pending bid *right now* is customer-facing content and stays in scope). **(2) It never mentioned the decision queue**, created 2026-07-05 and since made the system's metered resource; queue and health/lint are now observation targets 1 and 2, and an open row Jesse is ready to decide is the top selection priority. **(3) It never mentioned the Pre-Staging Loop** (2026-07-28), which now does the analysis this spec assigned to the loop itself — recorded up front so a run reads its own job as *applying a prepared proposal* rather than hunting for an item. **(4) Its Staleness Check section was a manual scan for five categories, four of which `tools/vault_lint.py` now implements** (`DEAD-LINK`, `OP-FRONTMATTER`, `REVIEW-OVERDUE`, `ORPHAN`) — rewritten to point at the lint report and to isolate the one category no rule can cover, a claim that is well-formed and wrong. Also added: closing a decision-queue row is blocked without approval (this loop applies what Jesse approved, it does not decide). Nothing here is a new decision except the queue-first selection ordering. Revert path: `git revert` this commit; the prior spec is intact in history. | Lint `REVIEW-OVERDUE` on the spec itself — the first time the rule's overdue nudge was worked rather than noted |
| 2026-07-29 | 04-knowledge/vault-capture-loop-spec.md; tools/{vault_lint.py,vault_index.py,estimating_rollup.py}; tools/fixtures/00-inbox/marker-before-frontmatter.md (new); 10 notes `git mv` 00-inbox/ → archive/; 3 inbox status fields | **[gated] Terminal-Seed Sweep extended to all note types, and the frontmatter blind spot that hid the need for it fixed.** Jesse approved the extension in-session after the gap was measured: a 49-item inbox held 19 terminal-status notes, of which the `type: idea-seed` rule covered 9 and left **10** (`note`, `task`, `capture`, `spec`, `insight`) sitting indefinitely — 6 of them still carrying defer markers, so the Pre-Staging Loop was queued to spend daily runs analyzing closed questions. Renamed **Terminal-Note Sweep**; allowlist and every safety rule unchanged (tracked-only, never rename, never sweep `researched`/`unexplored`, never sweep a `revisit-trigger:` note) and now written on status rather than type. Swept the 10; all moved as git renames, 13 inbound wikilinks intact, inbox 49 → 39. **The enabling fix is the more consequential half.** `parse_frontmatter()` bailed unless line 0 was `---`, but the capture loop writes `<!-- vault-loop: -->` and the pre-staging loop writes `<!-- vault-prestaged: -->` *above* the fence — so **26 of the vault's 202 notes had frontmatter invisible to every frontmatter rule**, all of them in `00-inbox/`, i.e. exactly the notes the loops touch most. STATUS-VOCAB, REVIEW-OVERDUE, SUPERSEDED, OP-FRONTMATTER and CONF-CONFLICT (an *error* rule) were silently skipping them, and a sweep built on the old parser would have missed 6 of its 10 targets. New `frontmatter_start()` tolerates leading blank lines and full-line HTML comments; the same fix applied to the duplicated parsers in `vault_index.py` and `estimating_rollup.py`. Fixture `marker-before-frontmatter.md` added per no-fixture-no-rule; self-test still passes all 14 rules. Surfacing the hidden notes raised 3 real STATUS-VOCAB violations (`closed` ×2, `unfiled` ×1 — none in the allowed vocabulary), normalized to `complete`/`inbox`; lint 37 → 36 warnings net, 0 errors throughout. Revert path: `git revert` this commit restores both the parser and the 10 notes. | Due review of vault-capture-loop-spec.md (review_after 2026-07-30), taken a day early |
| 2026-07-29 | 04-knowledge/{vault-idea-loop-spec,vault-consolidation-loop-spec,vault-skill-drift-loop-spec}.md | **[default] Audited the three loop specs lint would not have surfaced until late October; one live defect found and fixed.** All three were current in substance — none carried the governance contradiction that made the agent-loop spec dangerous — but each had at least one claim that no longer matched the system. **The defect that mattered: the skill-drift loop would have degraded its own 2026-08-01 run.** Its stop condition treated *any* unmerged `drift/` branch as proof of an unactioned backlog and required skipping branch creation. The 2026-07-25 run created a state the spec never modelled — Jesse read the review note, applied F1/F2/F5/F6 to `main` by hand, rejected F3, held F4, and **deliberately retained** `drift/2026-07b` unmerged as the record of what was proposed (the review note strikes through "Discard the branch" to say so). Verified: that branch is unmerged on local and origin, and its review note is `status: decided-blocked` with nothing pending. Three days out, the August run would have read a deliberate archive as a backlog and refused to propose. Fixed by classifying on the **review note's status** rather than the branch's merge state, with a table covering open / terminal / no-note-found, the last treated conservatively as a backlog. **Idea loop:** its Trigger claimed a `scheduled` flag in `tools/vault_health.py` is `True` — no such flag exists or ever did in the version described; `LOOP_HEARTBEATS` is a list of 5-tuples, and the real values are now quoted. Also added the exclusive-ownership rule (pre-staging skips `type: idea-seed` even when it carries a defer marker) which existed only on the pre-staging side, and a Seed Lifecycle section making explicit that `researched` and `gated` are deliberately excluded from the Terminal-Note Sweep because they mean *the decision is not made* — its Blocked table's "the loop doesn't remove it" had become false vault-wide once the sweep shipped. **Consolidation loop:** corrected "`02-facilities/` (capture/session work owns it)" — the capture loop refuses that folder too, so no loop owns it; it is Lane 1 for an interactive session. Also recorded a measured scope-vs-metric mismatch: the loop's "ORPHAN count down" success criterion is scoped to 07/08/09, but 11 of the vault's 12 current orphans sit in `04-knowledge/` (6) and `06-insights/` (5), so a run can succeed while the headline number does not move. Left as an open question, not a defect — several are terminal review notes nothing will naturally link to. Revert path: `git revert` this commit. | Recommended and approved follow-up to the agent- and capture-loop spec reviews; two-for-two on finding live defects in specs that looked fine |
| 2026-07-29 | 00-inbox/2026-07-29-spec-claim-crosscheck-measured-and-rejected.md (new); 04-knowledge/vault-capture-loop-spec.md; 02-facilities/ExxonMobil/Baytown-TX/DSP26085.md | **[default] Lint rule for cross-checking spec claims against code: measured and rejected.** Proposed after the loop-spec audit found the idea spec asserting a `scheduled` flag in `tools/vault_health.py` that never existed. Built as a one-off audit first per back-test-before-build. A broad pass over 465 backticked spans returned ~55 hits, nearly all noise; a tightened pass keeping only the two shapes with any yield — vault-relative paths and lint rule names — checked 278 claims and returned **4 hits, all four false positives**: a rule the note explicitly proposes and says is "Not built"; a path the sentence states *was removed*; a `tools/` path belonging to the Grok repo rather than the vault; and a placeholder inside spec prose. Backticks here carry frontmatter keys, job and quote numbers, format placeholders, other repos' paths, unbuilt proposals, and negative existence claims — telling assertion from negation from proposal is semantic, not lexical, so a rule cannot do it. Same shape as the WORD-DELTA diff-shape gate rejected 2026-07-28. **What did have yield became procedure, not tooling:** the `scheduled`-flag error was found by reading the spec against the code, which the loop-spec review already does; and grepping the ten swept basenames found one live broken path pointer on [[DSP26085]] created by that morning's Terminal-Note Sweep, now fixed as a wikilink. That correction also falsified a safety claim in the capture spec — a plain move keeps *wikilinks* green (lint resolves through `archive/`), but backticked path references dangle and **nothing lints them**, since DEAD-LINK reads only wikilinks and POINTER-DEAD only absolute source paths. The post-sweep basename grep is now a step in the sweep procedure. Historical surfaces (`change-log.md`, dated `06-insights/` notes) are excluded from any such check by design: a stale path there is correct-as-history, and 32 of the 36 raw hits were exactly that. Revert path: `git revert` this commit; nothing was built to remove. | Recommended follow-up to the loop-spec audits: verify each spec's factual claims about `tools/` |
| 2026-07-30 | 04-knowledge/_canonical-job-sheet.md; templates/_job-sheet-template.md; 02-facilities/HF-Sinclair/Artesia-NM/{H-2421.md,USA26040-job-sheet.html,USA26040-job-sheet.pdf}; 04-knowledge/ground-truth/h-2421.md; `~/.claude/skills/usadebusk-{core,estimating}/SKILL.md` | **[default] Job-sheet schema hardened after three corrections in two days — domain rules now require attribution.** Four decisions. **(1) Brand gold is `#FCC30A`.** Resolved by pulling the live usadebusk.com stylesheet: `#FCC30A` appears 20 times, `#F2A900` zero. The printables' `#F2A900` was never a brand color and `usadebusk-core` was right; USA26040's printable corrected and re-rendered (still one page), USA26038's deliberately left as-is (completed job), and the intentional divergence recorded so it is not read as drift. **(2) Scaffolding is an access test, not an elevation.** Required only where launcher/receiver locations are elevated AND lack a sturdy deck with room to change pigs — it is about crew access for pig changes. Elevation alone never implies it: USA26038's H-20 ran launchers 10' up on the deck with none, which was already in the vault and which I misread. Customer provides scaffolding and spool pieces on nearly all jobs (counterexample ~once in 2-3 years); added to `usadebusk-core` Role Boundaries beside Lifting and Adapters, with the proposal-line guidance in estimating §8. **H-2421 has no scaffolding** — the drawing's "scaffold req'd at A,B,C,D" is generic boilerplate, and an earlier commit had written my inference that it was HF Sinclair's spool-bolt-up scope into the heater card as fact. Removed; the ground-truth extraction keeps the notation verbatim with its source cell and gains an uncertainty note. Terminology: **inlets and outlets, never "nozzles"** (heater card 6 hits → 2, both source-drawing titles). **(3) Domain rules require attribution.** The exemplar mixed layout rules (self-correcting — render the page) with domain rules (silent when wrong, propagate to cards and crew sheets) in identical voice, which is what let the scaffold error through. Convention added: a domain rule carries `(Jesse, date)` or a cited source, and an unattributed one is a hypothesis to ask about. Resolved all six unattributed claims — per diem/DEF sourced to estimating+ops and correct; billable-rows-only confirmed; "every job is a mechanical decoke" **stands but its reason was wrong** (it holds because this vault's scope is mechanical decoke/cleaning, not because USADebusk only does that); crew-truck pass allowance **corrected** from a standing rule to a per-quote term — DSP26092 item 16 grants up to two, so USA26040's four passes are a courtesy grant, not a right. **(4) Cross-division scope boundary recorded** in `usadebusk-core`: other divisions run separate scopes and customers often share one PO (ExxonMobil F-501, hydroblast + pigging); that is not a discrepancy and not Jesse's to track — written because I read exactly that shared PO as evidence the decoke-scope rule was broken and raised it as a finding when it wasn't. Also promoted the PM-vs-Day-Supervisor billing rule (read the allocation block; no PM row means no PM billed) from USA26040's paperwork into estimating §Section 4. Revert path: `git revert` the vault commits plus the matching config commits. | Jesse, in session: "Stop calling them 'nozzles'... There's no scaffold on this project"; "Scaffolding is only needed if the launcher / receiver locations are elevated and not on a sturdy deck" |
| 2026-07-31 | 04-knowledge/manual/ (19 notes, new); INDEX.md; 50-dashboards/health.md | **[default] Created the Mechanical Pigging Methodology Manual (`USAD-MAN-PIG-001-REV0`) — the vault's first general, all-jobs pigging document — and opened a `MAN` document series to carry it.** Until now the methodology existed only as internal knowledge notes (`04-knowledge/concepts/`) and agent instruction (`usadebusk-sop`), so every per-job SOP re-explained the method from scratch and there was nothing to attach to a bid or hand a prospect. Five scope decisions, all Jesse's in session. **(1) Audience is customer-facing** — capability document serving both proposal attachment and standalone leave-behind, which set the scrub boundary: no rates, cost, margin, billing constructs (stand-by, PDT, ticket breakdown, receipt-as-billing-artifact), employee names, or job/quote numbers. **(2) Scope is core pigging plus ancillary services** — filtration and waste, initial flush and pitch removal, smart pig support, passivation — not the pigging-vs-steam-air comparison, which would have made it a sales instrument rather than a methods document. **(3) Both gated items excluded:** `SOP-OPS-LODGE-001` is named as available on request but never reproduced, per its hard suppression gate; D7's reversal control-action sequence is omitted entirely because it remains field-unconfirmed, so §9.8 describes reverse running as a capability without asserting a step order. **(4) No durations anywhere** — the `usadebusk-sop` rule wins over `process-flow.md`'s "default 6 hrs"; scheduling belongs to the estimate. Technical time criteria (effluent ≤3–5 s, ILI velocity 1.0–2.0 ft/s) are kept, since those are measurements, not schedule. **(5) New `MAN` series** rather than forcing a client-agnostic manual into the `SOP-DCK-[HeaterTag]` scheme, which is per-job and per-heater by construction; short form `MAN-PIG-001`, leaving room for future non-pigging manuals. Structural rule: **the manual is derivative, never canonical.** Every chapter carries a `**Source:**` line naming the file it restates, and on conflict the source wins and the manual gets corrected. Citing vault-internal sources as wikilinks rather than backticked paths also cleared three standing ORPHAN warnings — `process-flow`, `industry-foundation`, `equipment-library` — which had sat unlinked since the Master Reference decomposition (lint 41 → 38 warnings, 0 errors). Four Mermaid diagrams inline (coil topology, standard flow path, looped jumper-spool circuit, filtration loop); seven illustration briefs seeded as HTML comments for Jesse-authored graphics, invisible in both Obsidian render and DOCX export. **Still open:** `process-flow.md` lines 9 and 31 keep the "default 6 hrs" language that contradicts the SOP skill — the manual sidesteps it by omitting durations, but the contradiction is live in the vault and reframing those lines as estimating-model data needs a separate go-ahead. Revert path: `git revert` this commit and the manual commit. | Jesse, in session: "I think I want to create a new thorough general SOP / pigging how to manual over the whole process" |
| 2026-08-01 | 04-knowledge/concepts/{process-flow.md, industry-foundation.md}; 04-knowledge/pricing/_cost-model.md; 06-insights/2026-08-01-skill-drift-review.md | **[default] Rule adopted: a document or concept written to give a general explanation of a task states no task durations.** A specific figure implies all scopes and rig-in times are the same, and they are not. Closes the item the 2026-07-31 manual entry left open, raised again as V1 by that morning's skill-drift loop. **`process-flow.md` lines 9 and 31** dropped "default 6 hrs" and now carry `usadebusk-sop`'s own construction verbatim — "hours and scheduling belong to the estimate, not this note" — so the two surfaces read as one rule. That also removed a second error: the line named **pass count** as the rig-in driver, which the current estimating model explicitly rejects (tier comes from launcher/receiver elevation and pumper run distance, settled at the job walk). Both lines predate the tiering. Because the ruling is general, a sweep applied it further. **`industry-foundation.md:59`** lost "Duration: typically 4–6 hours" from the passivation list, which now matches `manual/15-ancillary-passivation-stainless.md`'s parameter table exactly — the manual had already dropped it and the concepts note was never updated. **`_cost-model.md:113`** asserted rig-out is "a fixed ~6 hr event on essentially every job," the same claim in its strongest form and wrong against the 4/6/8/12 fallback tiers; reworded rather than deleted so the rate-link anomaly's cost argument survives. **Deliberately left alone:** `decoking-method-comparison.md:26`'s ~18–24 hr coker window is a third-party AFPM/Marathon benchmark, web-verified, load-bearing for the steam-air comparison and protected by that file's own line 48 against uncorroborated substitutes — recorded in the review note so a future sweep does not re-litigate it. **Left open:** `industry-foundation.md:56` says "USADebusk can supply if required" of soda ash while `usadebusk-core` and the manual both say customer scope end to end — Lane 4 domain truth, flagged as an open question, not guessed. Revert path: `git revert` this commit. | Jesse, in session: "No need to mention durations for a document / concept intended to give a general explanation on a task. Giving a specific number would imply all scopes / rig-in times are the same. They are not." |
| 2026-08-01 | 04-knowledge/concepts/industry-foundation.md; 06-insights/2026-08-01-skill-drift-review.md; config repo `main` (merge of `drift/2026-08`) | **[default] Soda-ash supply: generalized documents name no provider, and `usadebusk-core`'s customer-supplies rule is behavioral rather than factual.** The skill-drift run flagged `industry-foundation.md:56` ("USADebusk can supply if required") as contradicting `usadebusk-core:105` ("customer scope, end to end") and the manual, and offered two guesses at the fix — correct the vault note, or soften the skill. **Both were wrong.** Jesse's ruling: don't mention who provides it at all in a generalized document. Both provider sentences removed. The important half is the reason the skill says what it says: **USADebusk has supplied soda ash in the past.** The customer-always-supplies rule was written because without it the model flagged or questioned supply in nearly every piece of work, and the real cases are rare enough — and tangled enough in customer preference — that Jesse wants the conversation avoided rather than surfaced. So the skill is stating an operating rule, not the full factual picture, and **a future drift run that finds historical evidence of USADebusk supplying must not read that as the skill being wrong.** Recorded in the review note's Part 4 for exactly that reason. `manual/15-ancillary-passivation-stainless.md:29` ("customer-supplied and customer-mixed") deliberately left alone — customer-facing deliverable, and there the line functions as a scope statement serving the same goal. Also merged `drift/2026-08` (one commit: the F5 baseline-hash correction in the regression README); branch retained per the vault-wide deletion ban. Revert path: `git revert` this commit; the config merge reverts separately. | Jesse, in session: "In a generalized document, don't mention who provides it... I made the rule that the customer always supplies it because when the rule wasn't in place you flagged or questioned it in almost everything we worked on" |
| 2026-08-01 | 06-insights/ (6 review notes); 50-dashboards/decision-queue.md; 00-inbox/ (3 owed notes new, 5 seeds closed); tools/{vault_health.py, vault_lint.py} | **[default] Cleared the review backlog: six open decisions ruled in one pass, taking the health dashboard's only FAIL row from 7 pending to 0 and the decision queue from 2 open rows to 0.** Five rulings and one correction. **DQ-005 (Lane 4, high risk) — B:** no numeric ft/hr service derate at n=5; the qualitative derate list in `estimating-pricing.md` is untouched and no figure enters any pricing document. C (a two-bucket illustrative derate) was rejected on the note's own counter-argument — HP-0007's 51 ft/hr is *routine*, not hard-service, and would land inside an illustrative "hard ≈ 50" range by coincidence off a two-row bucket. What changed is only the trigger: "is this enough data yet" is now a counted threshold of 10 routine rows instead of a judgment someone re-makes from scratch. **DQ-004 — B:** portfolio revival narrowed to the thesis v2 re-run alone; C/D/F re-verification and findings-routing dropped as bookkeeping with no forcing function. **Coil visualization — Tier 2+3+1 approved**, build deferred; it misses the Aug 4 USA26040 window regardless, but H-2421's card is source-verified against J04917 CC1/RC1 and the heater recurs on DSP26080. Approved partly because it is an extraction QA instrument, not only a crew aid — a renderer cannot place every tube without forcing unresolved geometry into the open. **Replay-ordering dropped, baseline-staleness approved** as a pair, since one option on the first was "fold into the second"; the deciding evidence is that the F1/F6 rebaseline, the exact case the proposed convention covered, still split into two commits under careful manual execution. **Vault stats layer — both tracks parked**, bundled onto the heater-card schema trigger that already carries two other parked schema items. Three approved-but-unexecuted items were filed to `00-inbox/` as owed notes rather than left implicit in resolved review notes. **One tooling change was required by a ruling, not chosen:** Jesse approved a *machine-checkable* trigger for DQ-005, and an unrecognized `[machine: …]` token silently degrades to the event-shaped wording — it would have read as checked without being checked. `vault_health.py` gained a `routine-rows` token and a `count_routine_rows()` that parses the generated rollup by table cell, returning `None` rather than 0 on an unreadable source so a missing file cannot masquerade as "no routine actuals." Separately, `vault_lint.py`'s `WIKILINK_RE` was excluding `|` but not `\` from the target capture, so `[[DSP26071\|DSP26071.2]]` in `active-jobs.md` — correctly escaped, since a bare pipe would split the table row — reported a phantom DEAD-LINK on `DSP26071\`. Revert path: `git revert` this commit; the two tool fixes are independent of the rulings and can be kept. | Jesse, in session: clear the health FAIL first, coil viz decided in-line with the build deferred |
| 2026-08-15 | 02-facilities/ExxonMobil/Baytown-TX/F-501.md; 02-facilities/ExxonMobil/Baytown-TX/USA26041-job-sheet.md; 01-context/active-jobs.md | **F-501 tube geometry, metallurgy and footage replaced with measured values from Steady Flux report 26-0663-002 Rev. A; cross-over added as a section; USA26041 closed out.** The card had carried OD 5.000" / ID 4.635" / wall `(not recorded)` / A335 Gr P9 on all three sections since 2026-07-27, derived from the DSP26071.2 workup and SOP. Every one of those was wrong: true OD is 5.563" throughout, IDs are 4.763 / 4.635 / 4.813 (convection / radiant / Treat Gas), walls are 0.400 / 0.464 / 0.375, metallurgy is ASTM A200 T9 on the alloy sections and **carbon steel** on Treat Gas, and a cross-over section (113" straight + 9.4" bend per pass) was missing entirely. Tube counts (9 / 15 / 16) were correct and are confirmed. **Max pig OD 4.875" is unchanged** — the radiant still governs as the smallest ID — so nothing operational was at risk, and the error was conservative in the safe direction. Measured pigged footage is **3,588 ft against 3,453 ft as bid, 3.9% high**, from two systematic causes: convection length counted straight tube only while radiant length had a bend allowance baked in, and the cross-over was omitted from both. Also recorded: ExxonMobil replaced **all 56 radiant return bends** before pigging (plug headers → U-bends, plus bottom bends cut off and replaced), which the inspection vendor did not know — so the report's headline "45.7% radiant wall loss" (D_4_1_R) and all 56 bend rows measure new steel against the original nominal. The defensible radiant number is A_15_R at 17.1%. Two display-only copy-paste errors in the report itself logged for Rev B. | Steady Flux report received and analysed 2026-08-15, two days after the 2026-08-13 inspection and one day after USA26041 finished. Geometry is Lane 4 domain truth; corrections made on Jesse's explicit in-session approval. |
| 2026-08-15 | 02-facilities/ExxonMobil/Baytown-TX/F-501.md; 02-facilities/ExxonMobil/Baytown-TX/USA26041-job-sheet.md; 01-context/active-jobs.md; 04-knowledge/estimating-actuals-rollup.md | **F-501 radiant U-bends confirmed permanent; USA26041 actuals filed from service receipts; A/B cross-over restriction recorded.** Three facts from Jesse, 2026-08-15. (1) The plug-header removal and bottom-bend replacement are **permanent**, not a decoke rig — the radiant is now a plain 15-tube serpentine with 14 U-bends per pass and future scopes plan against that. (2) **Steady Flux and the ExxonMobil reps already know about the replacement**, so the report's apparent bend/pup wall loss is understood in context on both sides; the earlier "send it back to the vendor" recommendation is withdrawn and the item is explicitly closed on the card so a future reader does not re-raise it as a finding. (3) A **metal restriction in the Pass A/B cross-overs** was called on two shifts and Circuit 1 finished on a 4.75" pig against a computed max of 4.875"; Circuit 2 ran clear. The computed 4.875" is left unchanged — it is correct from tube ID — with the restriction recorded separately as a physical obstruction and a 4.75" planning ceiling on Circuit 1. Actuals from receipts 10780–10786: 7 rig-in / 16 pig / 6 smart pig / 14 rig-out = **43 productive hrs against 48 quoted**, plus 11 hrs ExxonMobil-caused stand-by. Shape matters more than the total — pigging came in 33% under while rig-out ran 75% over and needed a second shift. Condition `first`, n=1, so it does not move the routine baseline. | Jesse supplied the service-receipt folder and confirmed permanence and vendor awareness in session, 2026-08-15. |
| 2026-08-15 | 02-facilities/ExxonMobil/Baytown-TX/DSP26071.md; .../USA26041-job-sheet.md; .../_facility.md; 01-context/active-jobs.md | **Filtration stand-by on the Baytown contract changed from the quoted $35/hr to $150/hr by approved change order; USA26041 closed commercially.** Jesse, 2026-08-15: the project is complete, the change orders are signed and approved, and the stand-by rate was changed to $150 with ExxonMobil rep approval. This resolves the ticket-breakdown reconciliation raised earlier the same day — the workbook's $150 was correct and the vault's $35 was stale, not the other way round. **The quoted schedules are preserved as history and NOT rewritten:** DSP26071's Pricing and Equipment-revenue tables still show $35 × 24 hrs = $840, because that is what went to the customer and it reconstructs the $60,287.42 quote total. What changed is the *governing* rate — the Hourly Rates (T&M) row is struck through to $150, and the facility rate table now records the pair as "$35 quoted → $150 billed." The prior standing instruction "do not correct DSP26071 to $150" is narrowed rather than reversed: it still protects the quoted tables, but no longer governs billing or follow-on quoting. Site-level lesson recorded: $35/hr has now appeared at Baytown as a template artifact (DSP25123), as a genuinely negotiated rate (DSP26071 as quoted), and as a rate later changed away (USA26041 as billed) — the number alone diagnoses nothing, so take the governing figure from the executed contract plus change orders. | Jesse confirmed project completion, signed change orders, and the rate change in session, 2026-08-15, in response to the ticket-breakdown variance flags. |
