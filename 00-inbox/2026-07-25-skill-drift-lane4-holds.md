<!-- vault-loop: operational — skill-drift Lane 4 items held for Jesse (04-knowledge, usadebusk-* skill scope). Defers to the on-demand Agent-Review loop; capture loop cannot write this content. -->
---
type: note
status: open
created: 2026-07-25
tags: [inbox, skill-drift, lane-4, awaiting-decision]
related:
  - "[[2026-07-25-skill-drift-review]]"
  - "[[2026-07-25-f6-divergences-awaiting-adjudication]]"
---

# Skill-drift 2026-07 — four items held for Jesse

The 2026-07-25 skill-drift review is adjudicated and its Decision block is closed (`status: decided-blocked`), so `health.md` no longer counts it. These four items were deliberately not applied and would otherwise have gone quiet with it. Each is either Lane 4 or unverifiable from files.

**1. ~~Does the fleet hold more than one filter press?~~ — ANSWERED AND CLOSED 2026-07-25.** Jesse, in session: the fleet holds **three** presses of the one model, with a rare option to rent a fourth. So "only unit" was a wording bug, not a fact — the sentence meant "only model." Fixed in `usadebusk-estimating:111` (no larger model; more capacity means another press) and recorded in `equipment-library.md`'s Filter Press section. Jesse also ruled explicitly that **equipment availability is his logistics call and is not to be tracked in the vault**; both surfaces now say so outright, so no future audit re-opens this as a data gap. Original finding retained below for the record.

*(original)* **Does the fleet hold more than one filter press?** (F4, the highest-value question here.) `usadebusk-estimating:111` ends "there is no larger press — the filter press in `usadebusk-equipment` is the only unit." Three other surfaces presuppose more than one physical press: the equipment skill's conditional "when a 2nd press is available", estimating's own "one being uncommitted that month across concurrent jobs", and `equipment-library.md:69` headed "Filter Press **#1** specs". The loop proposed rewording "only unit" to "only model in the fleet, so more capacity means a second press, not a bigger one" — which preserves both facts. That reword sits unmerged on `drift/2026-07b` and was **not** applied. If the fleet really does hold exactly one press, the fix runs the other way and it is the *equipment* skill's conditional 2× filtration that is wrong. This one matters because an estimator reading only the estimating skill concludes a second press is impossible rather than conditional, and press capacity against 2× TriMax is the configuration that generated $25,628.17 of billable stand-by on USA26022.

**2. ~~Was USA26038's demob 07-17 or 07-20?~~ — ANSWERED AND CLOSED 2026-07-25.** Jesse: equipment demobbed 07-20, but that is the wrong *concept* to record. There are two demobs — facility demob (equipment off the unit and out of the gate, coinciding with crew demob, after which no charges accrue) and fleet return to Deer Park. **The vault records the first only.** Equipment is often staged near a facility for weeks because another project is about to execute there, so fleet return has no fixed relationship to job end and is not USADeBusk-controlled in a way worth tracking. So the banner's 07-20 was not a stale date, it was concept 2 leaking into a concept 1 field — which also means F3's proposed 07-17 was right for the wrong reason, and rejecting it as "unverifiable" was the correct call on the evidence available. Now defined canonically in `04-knowledge/concepts/field-operations.md` ("Demob — which one the vault records"), with the rule and the banner fix in `usadebusk-fieldpm`. Original finding retained below.

*(original)* **Was USA26038's demob 07-17 or 07-20?** (F3.) `usadebusk-fieldpm`'s dormancy banner says demobbed 2026-07-20. The job report gives execution 2026-07-10 to 2026-07-17 and `active-jobs.md` records Completed 2026-07-17. Nothing in the vault records the physical equipment-return date, so this is not settleable from files — which is why the proposed change to 07-17 was rejected rather than applied. If the last shift was the 17th and the equipment came home on the 20th, both are true and the banner should say so: `job complete 2026-07-17, demobbed 2026-07-20`. One line either way; the skill is dormant so nothing depends on it until the next mobilization.

**3. ~~`estimating-pricing.md:68` names the $58.00 line "Driver Travel".~~ — APPLIED, not held.** (V3.) Initially held as Lane 4 pricing, then released on recon: `change-log.md`'s 2026-07-24 entry already records this rename as your decision — "the `$58.00/hr Driver Travel` rate line was unreachable … it is renamed **Crew Travel (non-driver)**" — and lists `04-knowledge/concepts/estimating-pricing.md` among the files that entry touched. The rename landed in the skill and in `estimating-approach.md` but never in this row. So it is a half-landed approved fix like V2, not a new pricing decision, and it was applied on that basis. Flagging it here anyway because it is a pricing row and you should know it moved. If you disagree, revert that one line.

**4. ~~F1 regression fixture now fails diff key 4.~~ — ANSWERED AND CLOSED 2026-07-25.** Jesse: write the rule. `usadebusk-estimating` now states under Proposal Generation Guardrails that an enumerated list contradicting its own stated count is a flag, not a rounding error — count, compare, stop on mismatch, name both readings, price the swing, carry it to open items. Generalized beyond item 15 to crew-vs-roles, passes-vs-circuits, and drivers-vs-pieces (`drivers = pieces traveling` is an identity, so a disagreement surfaces on the labor side too). Verified by replay (config `98ac964`): the run opened with a flagged INTAKE CONTRADICTION block before pricing anything, carried 5 pieces with its reasoning stated, priced the swing at +$552 across mob and demob, and put it in Section 3's open-items list. Diff key 4 is now satisfied by rule rather than by luck. Original finding retained below.

*(original)* **F1 regression fixture now fails diff key 4.** The post-adjudication replay missed the equipment-piece-count contradiction in intake item 15 — six items enumerated against "(5 pieces traveling)" — and priced five pieces without flagging it, where the frozen baseline catches it, reconciles it, and prices the swing. Not caused by any change in this adjudication: nothing has touched estimating or core intake logic since F1's baseline commit `bb78eb8` except a role line and a billing-math pointer. It reads as model variance against a rule that is implicit rather than written. The README's remedy is to make the implicit rule explicit in the skill, but that is a new commercial-skill edit, so it was left alone. `frozen/` was **not** re-cut. Decide whether to write an intake-consistency rule into `usadebusk-estimating`.

**5. ~~F2's derived-wall divergence — baseline over-strict, or model regression?~~ — ANSWERED AND CLOSED 2026-07-25.** Jesse chose the omit rule over relaxing the baseline. Written into `usadebusk-vault-ingest` under Behavior Rules — **a heater card carries stated values only, never derived ones** — with the specific point that a `(derived)` marker is *not* a licence to write the value, because markers strip in copy-paste and survive into quotes and drawings unattributed, and a card is the vault's record of what the customer's own document says. Canonical home is vault-ingest rather than estimating (a deliberate departure from the original framing: F2 loads core + vault-ingest and never reaches estimating, so a rule written only into estimating would not have closed this). Estimating carries a pointer plus the distinction that matters at the bid desk — an estimate is *supposed* to compute; a card is not the container for the result. Verified by replay (config `98ac964`): Tube Geometry now reads `(not recorded)` on both sections with the 0.237" arithmetic confined to ISSUES. The rule generalized unasked to Max pig OD, which is *stricter* than frozen — worth one look, since max pig OD may be a case where the derivation is canonical rather than advisory. Original finding retained below.

*(original)* **F2's derived-wall divergence — baseline over-strict, or model regression?** The F2 replay wrote the derived wall thickness 0.237" into the Tube Geometry table, marked "(derived)" and listed in its ISSUES block. The frozen baseline's `new_expected_behaviors` requires the opposite — that it be "deliberately NOT written to the card because the source does not state it." Everything else on F2 matched exactly, including the load-bearing Config Rollup arithmetic (60/2,280, 72/2,736, 5,016 ft on six circuits, not three passes). Not caused by anything in this adjudication. The honest read is that this is arguable rather than clearly wrong: the rule exists so a derived number is never later mistaken for a source fact, and marking it "(derived)" addresses that risk directly — so either the baseline should relax to "derived values may appear if explicitly marked," or the skill should state the omit rule outright, since it is currently implicit. Pick one; leaving it implicit is what produced the divergence.

Items 2 and 3 are one-liners. Item 1 is the one with real downstream reach. Items 4 and 5 are both the same shape — a rule the frozen baselines enforce that the skills never actually state.
